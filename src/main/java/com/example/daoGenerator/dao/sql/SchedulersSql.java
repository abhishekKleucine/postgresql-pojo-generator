package com.example.daoGenerator.dao.sql;

/**
 * Enhanced SQL constants for Schedulers entity
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
public final class SchedulersSql {

    public static final String FIND_ALL = """
        SELECT expected_start_date, facilities_id, code, data, use_cases_id, modified_at, description, checklists_id, is_repeated, recurrence_rule, enabled, archived, created_at, checklists_name, created_by, due_date_duration, deprecated_at, name, is_custom_recurrence, due_date_interval, modified_by, id, state, versions_id
        FROM schedulers
        """;

    public static final String COUNT = """
        SELECT COUNT(*) FROM schedulers
        """;

    public static final String FIND_BY_ID = """
        SELECT expected_start_date, facilities_id, code, data, use_cases_id, modified_at, description, checklists_id, is_repeated, recurrence_rule, enabled, archived, created_at, checklists_name, created_by, due_date_duration, deprecated_at, name, is_custom_recurrence, due_date_interval, modified_by, id, state, versions_id
        FROM schedulers
        WHERE id = :id
        """;

    public static final String INSERT = """
        INSERT INTO schedulers (expected_start_date, facilities_id, code, data, use_cases_id, modified_at, description, checklists_id, is_repeated, recurrence_rule, enabled, archived, created_at, checklists_name, created_by, due_date_duration, deprecated_at, name, is_custom_recurrence, due_date_interval, modified_by, state, versions_id)
        VALUES (:expectedStartDate, :facilitiesId, :code, :data::jsonb, :useCasesId, :modifiedAt, :description, :checklistsId, :isRepeated, :recurrenceRule, :enabled, :archived, :createdAt, :checklistsName, :createdBy, :dueDateDuration::jsonb, :deprecatedAt, :name, :isCustomRecurrence, :dueDateInterval, :modifiedBy, :state, :versionsId)
        RETURNING id
        """;

    public static final String UPDATE = """
        UPDATE schedulers SET
            expected_start_date = :expectedStartDate,
            facilities_id = :facilitiesId,
            code = :code,
            data = :data::jsonb,
            use_cases_id = :useCasesId,
            modified_at = :modifiedAt,
            description = :description,
            checklists_id = :checklistsId,
            is_repeated = :isRepeated,
            recurrence_rule = :recurrenceRule,
            enabled = :enabled,
            archived = :archived,
            created_at = :createdAt,
            checklists_name = :checklistsName,
            created_by = :createdBy,
            due_date_duration = :dueDateDuration::jsonb,
            deprecated_at = :deprecatedAt,
            name = :name,
            is_custom_recurrence = :isCustomRecurrence,
            due_date_interval = :dueDateInterval,
            modified_by = :modifiedBy,
            state = :state,
            versions_id = :versionsId
        WHERE id = :id
        """;

    public static final String DELETE_BY_ID = """
        DELETE FROM schedulers
        WHERE id = :id
        """;

    public static final String EXISTS_BY_ID = """
        SELECT COUNT(*) FROM schedulers
        WHERE id = :id
        """;

    public static final String FIND_BY_USE_CASES_ID = """
        SELECT expected_start_date, facilities_id, code, data, use_cases_id, modified_at, description, checklists_id, is_repeated, recurrence_rule, enabled, archived, created_at, checklists_name, created_by, due_date_duration, deprecated_at, name, is_custom_recurrence, due_date_interval, modified_by, id, state, versions_id
        FROM schedulers
        WHERE use_cases_id = :useCasesId
        """;

    public static final String FIND_BY_CHECKLISTS_ID = """
        SELECT expected_start_date, facilities_id, code, data, use_cases_id, modified_at, description, checklists_id, is_repeated, recurrence_rule, enabled, archived, created_at, checklists_name, created_by, due_date_duration, deprecated_at, name, is_custom_recurrence, due_date_interval, modified_by, id, state, versions_id
        FROM schedulers
        WHERE checklists_id = :checklistsId
        """;

    public static final String FIND_BY_FACILITIES_ID = """
        SELECT expected_start_date, facilities_id, code, data, use_cases_id, modified_at, description, checklists_id, is_repeated, recurrence_rule, enabled, archived, created_at, checklists_name, created_by, due_date_duration, deprecated_at, name, is_custom_recurrence, due_date_interval, modified_by, id, state, versions_id
        FROM schedulers
        WHERE facilities_id = :facilitiesId
        """;

    public static final String FIND_BY_VERSIONS_ID = """
        SELECT expected_start_date, facilities_id, code, data, use_cases_id, modified_at, description, checklists_id, is_repeated, recurrence_rule, enabled, archived, created_at, checklists_name, created_by, due_date_duration, deprecated_at, name, is_custom_recurrence, due_date_interval, modified_by, id, state, versions_id
        FROM schedulers
        WHERE versions_id = :versionsId
        """;

    private SchedulersSql() {
        // Utility class
    }
}
