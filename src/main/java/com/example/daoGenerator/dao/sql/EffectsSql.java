package com.example.daoGenerator.dao.sql;

/**
 * Enhanced SQL constants for Effects entity
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
public final class EffectsSql {

    public static final String FIND_ALL = """
        SELECT api_payload, order_tree, modified_at, query, description, actions_id, type, api_method, created_at, archived, api_endpoint, api_headers, created_by, name, modified_by, javascript_enabled, id
        FROM effects
        """;

    public static final String COUNT = """
        SELECT COUNT(*) FROM effects
        """;

    public static final String FIND_BY_ID = """
        SELECT api_payload, order_tree, modified_at, query, description, actions_id, type, api_method, created_at, archived, api_endpoint, api_headers, created_by, name, modified_by, javascript_enabled, id
        FROM effects
        WHERE id = :id
        """;

    public static final String INSERT = """
        INSERT INTO effects (api_payload, order_tree, modified_at, query, description, actions_id, type, api_method, created_at, archived, api_endpoint, api_headers, created_by, name, modified_by, javascript_enabled)
        VALUES (:apiPayload::jsonb, :orderTree, :modifiedAt, :query::jsonb, :description, :actionsId, :type, :apiMethod, :createdAt, :archived, :apiEndpoint::jsonb, :apiHeaders::jsonb, :createdBy, :name, :modifiedBy, :javascriptEnabled)
        RETURNING id
        """;

    public static final String UPDATE = """
        UPDATE effects SET
            api_payload = :apiPayload::jsonb,
            order_tree = :orderTree,
            modified_at = :modifiedAt,
            query = :query::jsonb,
            description = :description,
            actions_id = :actionsId,
            type = :type,
            api_method = :apiMethod,
            created_at = :createdAt,
            archived = :archived,
            api_endpoint = :apiEndpoint::jsonb,
            api_headers = :apiHeaders::jsonb,
            created_by = :createdBy,
            name = :name,
            modified_by = :modifiedBy,
            javascript_enabled = :javascriptEnabled
        WHERE id = :id
        """;

    public static final String DELETE_BY_ID = """
        DELETE FROM effects
        WHERE id = :id
        """;

    public static final String EXISTS_BY_ID = """
        SELECT COUNT(*) FROM effects
        WHERE id = :id
        """;

    public static final String FIND_BY_ACTIONS_ID = """
        SELECT api_payload, order_tree, modified_at, query, description, actions_id, type, api_method, created_at, archived, api_endpoint, api_headers, created_by, name, modified_by, javascript_enabled, id
        FROM effects
        WHERE actions_id = :actionsId
        """;

    public static final String FIND_BY_MODIFIED_BY = """
        SELECT api_payload, order_tree, modified_at, query, description, actions_id, type, api_method, created_at, archived, api_endpoint, api_headers, created_by, name, modified_by, javascript_enabled, id
        FROM effects
        WHERE modified_by = :modifiedBy
        """;

    public static final String FIND_BY_CREATED_BY = """
        SELECT api_payload, order_tree, modified_at, query, description, actions_id, type, api_method, created_at, archived, api_endpoint, api_headers, created_by, name, modified_by, javascript_enabled, id
        FROM effects
        WHERE created_by = :createdBy
        """;

    private EffectsSql() {
        // Utility class
    }
}
