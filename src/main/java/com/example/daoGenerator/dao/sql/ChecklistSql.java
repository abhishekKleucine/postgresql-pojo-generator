package com.example.daoGenerator.dao.sql;

/**
 * Enhanced SQL constants for Checklist entity
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
public final class ChecklistSql {

    public static final String FIND_ALL = """
        SELECT released_at, code, organisations_id, modified_at, released_by, use_cases_id, review_cycle, description, job_log_columns, archived, created_at, created_by, name, is_global, modified_by, color_code, id, state, versions_id
        FROM checklists
        """;

    public static final String COUNT = """
        SELECT COUNT(*) FROM checklists
        """;

    public static final String FIND_BY_ID = """
        SELECT released_at, code, organisations_id, modified_at, released_by, use_cases_id, review_cycle, description, job_log_columns, archived, created_at, created_by, name, is_global, modified_by, color_code, id, state, versions_id
        FROM checklists
        WHERE id = :id
        """;

    public static final String INSERT = """
        INSERT INTO checklists (released_at, code, organisations_id, modified_at, released_by, use_cases_id, review_cycle, description, job_log_columns, archived, created_at, created_by, name, is_global, modified_by, color_code, state, versions_id)
        VALUES (:releasedAt, :code, :organisationsId, :modifiedAt, :releasedBy, :useCasesId, :reviewCycle, :description, :jobLogColumns::jsonb, :archived, :createdAt, :createdBy, :name, :isGlobal, :modifiedBy, :colorCode, :state, :versionsId)
        RETURNING id
        """;

    public static final String UPDATE = """
        UPDATE checklists SET
            released_at = :releasedAt,
            code = :code,
            organisations_id = :organisationsId,
            modified_at = :modifiedAt,
            released_by = :releasedBy,
            use_cases_id = :useCasesId,
            review_cycle = :reviewCycle,
            description = :description,
            job_log_columns = :jobLogColumns::jsonb,
            archived = :archived,
            created_at = :createdAt,
            created_by = :createdBy,
            name = :name,
            is_global = :isGlobal,
            modified_by = :modifiedBy,
            color_code = :colorCode,
            state = :state,
            versions_id = :versionsId
        WHERE id = :id
        """;

    public static final String DELETE_BY_ID = """
        DELETE FROM checklists
        WHERE id = :id
        """;

    public static final String EXISTS_BY_ID = """
        SELECT COUNT(*) FROM checklists
        WHERE id = :id
        """;

    public static final String FIND_BY_USE_CASES_ID = """
        SELECT released_at, code, organisations_id, modified_at, released_by, use_cases_id, review_cycle, description, job_log_columns, archived, created_at, created_by, name, is_global, modified_by, color_code, id, state, versions_id
        FROM checklists
        WHERE use_cases_id = :useCasesId
        """;

    public static final String FIND_BY_RELEASED_BY = """
        SELECT released_at, code, organisations_id, modified_at, released_by, use_cases_id, review_cycle, description, job_log_columns, archived, created_at, created_by, name, is_global, modified_by, color_code, id, state, versions_id
        FROM checklists
        WHERE released_by = :releasedBy
        """;

    public static final String FIND_BY_MODIFIED_BY = """
        SELECT released_at, code, organisations_id, modified_at, released_by, use_cases_id, review_cycle, description, job_log_columns, archived, created_at, created_by, name, is_global, modified_by, color_code, id, state, versions_id
        FROM checklists
        WHERE modified_by = :modifiedBy
        """;

    public static final String FIND_BY_ORGANISATIONS_ID = """
        SELECT released_at, code, organisations_id, modified_at, released_by, use_cases_id, review_cycle, description, job_log_columns, archived, created_at, created_by, name, is_global, modified_by, color_code, id, state, versions_id
        FROM checklists
        WHERE organisations_id = :organisationsId
        """;

    public static final String FIND_BY_CREATED_BY = """
        SELECT released_at, code, organisations_id, modified_at, released_by, use_cases_id, review_cycle, description, job_log_columns, archived, created_at, created_by, name, is_global, modified_by, color_code, id, state, versions_id
        FROM checklists
        WHERE created_by = :createdBy
        """;

    public static final String FIND_BY_VERSIONS_ID = """
        SELECT released_at, code, organisations_id, modified_at, released_by, use_cases_id, review_cycle, description, job_log_columns, archived, created_at, created_by, name, is_global, modified_by, color_code, id, state, versions_id
        FROM checklists
        WHERE versions_id = :versionsId
        """;

    private ChecklistSql() {
        // Utility class
    }
}
