package com.example.daoGenerator.dao.sql;

/**
 * Enhanced SQL constants for Scopes entity
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
public final class ScopesSql {

    public static final String FIND_ALL = """
        SELECT created_at, archived, scope_groups_id, created_by, order_tree, modified_at, name, modified_by, id
        FROM scopes
        """;

    public static final String COUNT = """
        SELECT COUNT(*) FROM scopes
        """;

    public static final String FIND_BY_ID = """
        SELECT created_at, archived, scope_groups_id, created_by, order_tree, modified_at, name, modified_by, id
        FROM scopes
        WHERE id = :id
        """;

    public static final String INSERT = """
        INSERT INTO scopes (created_at, archived, scope_groups_id, created_by, order_tree, modified_at, name, modified_by)
        VALUES (:createdAt, :archived, :scopeGroupsId, :createdBy, :orderTree, :modifiedAt, :name, :modifiedBy)
        RETURNING id
        """;

    public static final String UPDATE = """
        UPDATE scopes SET
            created_at = :createdAt,
            archived = :archived,
            scope_groups_id = :scopeGroupsId,
            created_by = :createdBy,
            order_tree = :orderTree,
            modified_at = :modifiedAt,
            name = :name,
            modified_by = :modifiedBy
        WHERE id = :id
        """;

    public static final String DELETE_BY_ID = """
        DELETE FROM scopes
        WHERE id = :id
        """;

    public static final String EXISTS_BY_ID = """
        SELECT COUNT(*) FROM scopes
        WHERE id = :id
        """;

    public static final String FIND_BY_MODIFIED_BY = """
        SELECT created_at, archived, scope_groups_id, created_by, order_tree, modified_at, name, modified_by, id
        FROM scopes
        WHERE modified_by = :modifiedBy
        """;

    public static final String FIND_BY_SCOPE_GROUPS_ID = """
        SELECT created_at, archived, scope_groups_id, created_by, order_tree, modified_at, name, modified_by, id
        FROM scopes
        WHERE scope_groups_id = :scopeGroupsId
        """;

    public static final String FIND_BY_CREATED_BY = """
        SELECT created_at, archived, scope_groups_id, created_by, order_tree, modified_at, name, modified_by, id
        FROM scopes
        WHERE created_by = :createdBy
        """;

    private ScopesSql() {
        // Utility class
    }
}
