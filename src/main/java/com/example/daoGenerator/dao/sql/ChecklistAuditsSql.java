package com.example.daoGenerator.dao.sql;

/**
 * SQL constants for ChecklistAudits entity
 * Generated by JDBC DAO Generator
 */
public final class ChecklistAuditsSql {

    public static final String FIND_BY_ID = """
        SELECT triggered_for, organisations_id, tasks_id, action, checklists_id, details, id, triggered_at, stages_id, triggered_by
        FROM checklist_audits
        WHERE id = :id
        """;

    public static final String FIND_ALL = """
        SELECT triggered_for, organisations_id, tasks_id, action, checklists_id, details, id, triggered_at, stages_id, triggered_by
        FROM checklist_audits
        """;

    public static final String INSERT = """
        INSERT INTO checklist_audits (triggered_for, organisations_id, tasks_id, action, checklists_id, details, triggered_at, stages_id, triggered_by)
        VALUES (:triggeredFor, :organisationsId, :tasksId, :action, :checklistsId, :details, :triggeredAt, :stagesId, :triggeredBy)
        RETURNING id
        """;

    public static final String UPDATE = """
        UPDATE checklist_audits SET
            triggered_for = :triggeredFor,
            organisations_id = :organisationsId,
            tasks_id = :tasksId,
            action = :action,
            checklists_id = :checklistsId,
            details = :details,
            triggered_at = :triggeredAt,
            stages_id = :stagesId,
            triggered_by = :triggeredBy
        WHERE id = :id
        """;

    public static final String DELETE_BY_ID = """
        DELETE FROM checklist_audits
        WHERE id = :id
        """;

    public static final String EXISTS_BY_ID = """
        SELECT COUNT(*) FROM checklist_audits
        WHERE id = :id
        """;

    public static final String COUNT = """
        SELECT COUNT(*) FROM checklist_audits
        """;

    private ChecklistAuditsSql() {
        // Utility class
    }
}
