package com.example.daoGenerator.dao.sql;

/**
 * Enhanced SQL constants for Corrector entity
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
public final class CorrectorSql {

    public static final String FIND_ALL = """
        SELECT corrections_id, action_performed, created_at, user_groups_id, created_by, modified_at, users_id, modified_by, id
        FROM correctors
        """;

    public static final String COUNT = """
        SELECT COUNT(*) FROM correctors
        """;

    public static final String FIND_BY_ID = """
        SELECT corrections_id, action_performed, created_at, user_groups_id, created_by, modified_at, users_id, modified_by, id
        FROM correctors
        WHERE id = :id
        """;

    public static final String INSERT = """
        INSERT INTO correctors (corrections_id, action_performed, created_at, user_groups_id, created_by, modified_at, users_id, modified_by)
        VALUES (:correctionsId, :actionPerformed, :createdAt, :userGroupsId, :createdBy, :modifiedAt, :usersId, :modifiedBy)
        RETURNING id
        """;

    public static final String UPDATE = """
        UPDATE correctors SET
            corrections_id = :correctionsId,
            action_performed = :actionPerformed,
            created_at = :createdAt,
            user_groups_id = :userGroupsId,
            created_by = :createdBy,
            modified_at = :modifiedAt,
            users_id = :usersId,
            modified_by = :modifiedBy
        WHERE id = :id
        """;

    public static final String DELETE_BY_ID = """
        DELETE FROM correctors
        WHERE id = :id
        """;

    public static final String EXISTS_BY_ID = """
        SELECT COUNT(*) FROM correctors
        WHERE id = :id
        """;

    public static final String FIND_BY_USER_GROUPS_ID = """
        SELECT corrections_id, action_performed, created_at, user_groups_id, created_by, modified_at, users_id, modified_by, id
        FROM correctors
        WHERE user_groups_id = :userGroupsId
        """;

    public static final String FIND_BY_CORRECTIONS_ID = """
        SELECT corrections_id, action_performed, created_at, user_groups_id, created_by, modified_at, users_id, modified_by, id
        FROM correctors
        WHERE corrections_id = :correctionsId
        """;

    public static final String FIND_BY_USERS_ID = """
        SELECT corrections_id, action_performed, created_at, user_groups_id, created_by, modified_at, users_id, modified_by, id
        FROM correctors
        WHERE users_id = :usersId
        """;

    private CorrectorSql() {
        // Utility class
    }
}
