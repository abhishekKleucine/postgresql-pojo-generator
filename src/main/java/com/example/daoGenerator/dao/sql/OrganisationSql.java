package com.example.daoGenerator.dao.sql;

/**
 * Enhanced SQL constants for Organisation entity
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
public final class OrganisationSql {

    public static final String FIND_ALL = """
        SELECT archived, created_at, fqdn, created_by, modified_at, is_master, name, modified_by, id
        FROM organisations
        """;

    public static final String COUNT = """
        SELECT COUNT(*) FROM organisations
        """;

    public static final String FIND_BY_ID = """
        SELECT archived, created_at, fqdn, created_by, modified_at, is_master, name, modified_by, id
        FROM organisations
        WHERE id = :id
        """;

    public static final String INSERT = """
        INSERT INTO organisations (archived, created_at, fqdn, created_by, modified_at, is_master, name, modified_by)
        VALUES (:archived, :createdAt, :fqdn, :createdBy, :modifiedAt, :isMaster, :name, :modifiedBy)
        RETURNING id
        """;

    public static final String UPDATE = """
        UPDATE organisations SET
            archived = :archived,
            created_at = :createdAt,
            fqdn = :fqdn,
            created_by = :createdBy,
            modified_at = :modifiedAt,
            is_master = :isMaster,
            name = :name,
            modified_by = :modifiedBy
        WHERE id = :id
        """;

    public static final String DELETE_BY_ID = """
        DELETE FROM organisations
        WHERE id = :id
        """;

    public static final String EXISTS_BY_ID = """
        SELECT COUNT(*) FROM organisations
        WHERE id = :id
        """;

    private OrganisationSql() {
        // Utility class
    }
}
