package com.example.daoGenerator.dao.sql;

/**
 * Enhanced SQL constants for RelationValue entity
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
public final class RelationValueSql {

    public static final String FIND_ALL = """
        SELECT relations_id, modified_at, object_type_display_name, collection, object_type_external_id, created_at, created_by, object_display_name, modified_by, id, jobs_id, object_id, object_external_id
        FROM relation_values
        """;

    public static final String COUNT = """
        SELECT COUNT(*) FROM relation_values
        """;

    public static final String FIND_BY_ID = """
        SELECT relations_id, modified_at, object_type_display_name, collection, object_type_external_id, created_at, created_by, object_display_name, modified_by, id, jobs_id, object_id, object_external_id
        FROM relation_values
        WHERE id = :id
        """;

    public static final String INSERT = """
        INSERT INTO relation_values (relations_id, modified_at, object_type_display_name, collection, object_type_external_id, created_at, created_by, object_display_name, modified_by, jobs_id, object_id, object_external_id)
        VALUES (:relationsId, :modifiedAt, :objectTypeDisplayName, :collection, :objectTypeExternalId, :createdAt, :createdBy, :objectDisplayName, :modifiedBy, :jobsId, :objectId, :objectExternalId)
        RETURNING id
        """;

    public static final String UPDATE = """
        UPDATE relation_values SET
            relations_id = :relationsId,
            modified_at = :modifiedAt,
            object_type_display_name = :objectTypeDisplayName,
            collection = :collection,
            object_type_external_id = :objectTypeExternalId,
            created_at = :createdAt,
            created_by = :createdBy,
            object_display_name = :objectDisplayName,
            modified_by = :modifiedBy,
            jobs_id = :jobsId,
            object_id = :objectId,
            object_external_id = :objectExternalId
        WHERE id = :id
        """;

    public static final String DELETE_BY_ID = """
        DELETE FROM relation_values
        WHERE id = :id
        """;

    public static final String EXISTS_BY_ID = """
        SELECT COUNT(*) FROM relation_values
        WHERE id = :id
        """;

    public static final String FIND_BY_RELATIONS_ID = """
        SELECT relations_id, modified_at, object_type_display_name, collection, object_type_external_id, created_at, created_by, object_display_name, modified_by, id, jobs_id, object_id, object_external_id
        FROM relation_values
        WHERE relations_id = :relationsId
        """;

    public static final String FIND_BY_JOBS_ID = """
        SELECT relations_id, modified_at, object_type_display_name, collection, object_type_external_id, created_at, created_by, object_display_name, modified_by, id, jobs_id, object_id, object_external_id
        FROM relation_values
        WHERE jobs_id = :jobsId
        """;

    private RelationValueSql() {
        // Utility class
    }
}
