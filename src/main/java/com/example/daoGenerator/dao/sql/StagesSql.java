package com.example.daoGenerator.dao.sql;

/**
 * Enhanced SQL constants for Stages entity
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
public final class StagesSql {

    public static final String FIND_ALL = """
        SELECT archived, created_at, order_tree, created_by, modified_at, name, checklists_id, modified_by, id
        FROM stages
        """;

    public static final String COUNT = """
        SELECT COUNT(*) FROM stages
        """;

    public static final String FIND_BY_ID = """
        SELECT archived, created_at, order_tree, created_by, modified_at, name, checklists_id, modified_by, id
        FROM stages
        WHERE id = :id
        """;

    public static final String INSERT = """
        INSERT INTO stages (archived, created_at, order_tree, created_by, modified_at, name, checklists_id, modified_by)
        VALUES (:archived, :createdAt, :orderTree, :createdBy, :modifiedAt, :name, :checklistsId, :modifiedBy)
        RETURNING id
        """;

    public static final String UPDATE = """
        UPDATE stages SET
            archived = :archived,
            created_at = :createdAt,
            order_tree = :orderTree,
            created_by = :createdBy,
            modified_at = :modifiedAt,
            name = :name,
            checklists_id = :checklistsId,
            modified_by = :modifiedBy
        WHERE id = :id
        """;

    public static final String DELETE_BY_ID = """
        DELETE FROM stages
        WHERE id = :id
        """;

    public static final String EXISTS_BY_ID = """
        SELECT COUNT(*) FROM stages
        WHERE id = :id
        """;

    public static final String FIND_BY_CHECKLISTS_ID = """
        SELECT archived, created_at, order_tree, created_by, modified_at, name, checklists_id, modified_by, id
        FROM stages
        WHERE checklists_id = :checklistsId
        """;

    public static final String FIND_BY_MODIFIED_BY = """
        SELECT archived, created_at, order_tree, created_by, modified_at, name, checklists_id, modified_by, id
        FROM stages
        WHERE modified_by = :modifiedBy
        """;

    public static final String FIND_BY_CREATED_BY = """
        SELECT archived, created_at, order_tree, created_by, modified_at, name, checklists_id, modified_by, id
        FROM stages
        WHERE created_by = :createdBy
        """;

    private StagesSql() {
        // Utility class
    }
}
