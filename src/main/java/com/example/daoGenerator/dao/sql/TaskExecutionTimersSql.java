package com.example.daoGenerator.dao.sql;

/**
 * Enhanced SQL constants for TaskExecutionTimers entity
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
public final class TaskExecutionTimersSql {

    public static final String FIND_ALL = """
        SELECT created_at, reason, created_by, modified_at, resumed_at, comment, paused_at, modified_by, id, task_executions_id
        FROM task_execution_timers
        """;

    public static final String COUNT = """
        SELECT COUNT(*) FROM task_execution_timers
        """;

    public static final String FIND_BY_ID = """
        SELECT created_at, reason, created_by, modified_at, resumed_at, comment, paused_at, modified_by, id, task_executions_id
        FROM task_execution_timers
        WHERE id = :id
        """;

    public static final String INSERT = """
        INSERT INTO task_execution_timers (created_at, reason, created_by, modified_at, resumed_at, comment, paused_at, modified_by, task_executions_id)
        VALUES (:createdAt, :reason, :createdBy, :modifiedAt, :resumedAt, :comment, :pausedAt, :modifiedBy, :taskExecutionsId)
        RETURNING id
        """;

    public static final String UPDATE = """
        UPDATE task_execution_timers SET
            created_at = :createdAt,
            reason = :reason,
            created_by = :createdBy,
            modified_at = :modifiedAt,
            resumed_at = :resumedAt,
            comment = :comment,
            paused_at = :pausedAt,
            modified_by = :modifiedBy,
            task_executions_id = :taskExecutionsId
        WHERE id = :id
        """;

    public static final String DELETE_BY_ID = """
        DELETE FROM task_execution_timers
        WHERE id = :id
        """;

    public static final String EXISTS_BY_ID = """
        SELECT COUNT(*) FROM task_execution_timers
        WHERE id = :id
        """;

    public static final String FIND_BY_TASK_EXECUTIONS_ID = """
        SELECT created_at, reason, created_by, modified_at, resumed_at, comment, paused_at, modified_by, id, task_executions_id
        FROM task_execution_timers
        WHERE task_executions_id = :taskExecutionsId
        """;

    public static final String FIND_BY_MODIFIED_BY = """
        SELECT created_at, reason, created_by, modified_at, resumed_at, comment, paused_at, modified_by, id, task_executions_id
        FROM task_execution_timers
        WHERE modified_by = :modifiedBy
        """;

    public static final String FIND_BY_CREATED_BY = """
        SELECT created_at, reason, created_by, modified_at, resumed_at, comment, paused_at, modified_by, id, task_executions_id
        FROM task_execution_timers
        WHERE created_by = :createdBy
        """;

    private TaskExecutionTimersSql() {
        // Utility class
    }
}
