package com.example.daoGenerator.dao.sql;

/**
 * SQL constants for Versions entity
 * Generated by JDBC DAO Generator
 */
public final class VersionsSql {

    public static final String FIND_BY_ID = """
        SELECT versioned_at, created_at, parent, created_by, modified_at, deprecated_at, ancestor, self, modified_by, id, type, version
        FROM versions
        WHERE id = :id
        """;

    public static final String FIND_ALL = """
        SELECT versioned_at, created_at, parent, created_by, modified_at, deprecated_at, ancestor, self, modified_by, id, type, version
        FROM versions
        """;

    public static final String INSERT = """
        INSERT INTO versions (versioned_at, created_at, parent, created_by, modified_at, deprecated_at, ancestor, self, modified_by, type, version)
        VALUES (:versionedAt, :createdAt, :parent, :createdBy, :modifiedAt, :deprecatedAt, :ancestor, :self, :modifiedBy, :type, :version)
        RETURNING id
        """;

    public static final String UPDATE = """
        UPDATE versions SET
            versioned_at = :versionedAt,
            created_at = :createdAt,
            parent = :parent,
            created_by = :createdBy,
            modified_at = :modifiedAt,
            deprecated_at = :deprecatedAt,
            ancestor = :ancestor,
            self = :self,
            modified_by = :modifiedBy,
            type = :type,
            version = :version
        WHERE id = :id
        """;

    public static final String DELETE_BY_ID = """
        DELETE FROM versions
        WHERE id = :id
        """;

    public static final String EXISTS_BY_ID = """
        SELECT COUNT(*) FROM versions
        WHERE id = :id
        """;

    public static final String COUNT = """
        SELECT COUNT(*) FROM versions
        """;

    public static final String FIND_BY_MODIFIED_BY = """
        SELECT versioned_at, created_at, parent, created_by, modified_at, deprecated_at, ancestor, self, modified_by, id, type, version
        FROM versions
        WHERE modified_by = :modifiedBy
        """;

    public static final String FIND_BY_CREATED_BY = """
        SELECT versioned_at, created_at, parent, created_by, modified_at, deprecated_at, ancestor, self, modified_by, id, type, version
        FROM versions
        WHERE created_by = :createdBy
        """;

    private VersionsSql() {
        // Utility class
    }
}
