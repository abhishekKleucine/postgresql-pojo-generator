package com.example.daoGenerator.dao.sql;

/**
 * Enhanced SQL constants for Tokens entity
 * Key Type: CUSTOM_SINGLE_KEY
 * Generated by Enhanced JDBC DAO Generator
 */
public final class TokensSql {

    public static final String FIND_ALL = """
        SELECT users_id, expiration, type, token
        FROM tokens
        """;

    public static final String COUNT = """
        SELECT COUNT(*) FROM tokens
        """;

    public static final String FIND_BY_TOKEN = """
        SELECT users_id, expiration, type, token
        FROM tokens
        WHERE token = :token
        """;

    public static final String INSERT = """
        INSERT INTO tokens (users_id, expiration, type, token)
        VALUES (:usersId, :expiration, :type, :token)
        """;

    public static final String UPDATE = """
        UPDATE tokens SET
            users_id = :usersId,
            expiration = :expiration,
            type = :type,
            token = :token
        WHERE token = :token
        """;

    public static final String DELETE_BY_TOKEN = """
        DELETE FROM tokens
        WHERE token = :token
        """;

    public static final String EXISTS_BY_TOKEN = """
        SELECT COUNT(*) FROM tokens
        WHERE token = :token
        """;

    private TokensSql() {
        // Utility class
    }
}
