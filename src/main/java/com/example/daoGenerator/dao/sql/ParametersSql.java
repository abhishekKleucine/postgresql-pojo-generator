package com.example.daoGenerator.dao.sql;

/**
 * SQL constants for Parameters entity
 * Generated by JDBC DAO Generator
 */
public final class ParametersSql {

    public static final String FIND_BY_ID = """
        SELECT metadata, data, hidden, order_tree, modified_at, description, checklists_id, rules, label, type, auto_initialize, archived, created_at, created_by, target_entity_type, tasks_id, is_auto_initialized, modified_by, verification_type, id, validations, is_mandatory
        FROM parameters
        WHERE id = :id
        """;

    public static final String FIND_ALL = """
        SELECT metadata, data, hidden, order_tree, modified_at, description, checklists_id, rules, label, type, auto_initialize, archived, created_at, created_by, target_entity_type, tasks_id, is_auto_initialized, modified_by, verification_type, id, validations, is_mandatory
        FROM parameters
        """;

    public static final String INSERT = """
        INSERT INTO parameters (metadata, data, hidden, order_tree, modified_at, description, checklists_id, rules, label, type, auto_initialize, archived, created_at, created_by, target_entity_type, tasks_id, is_auto_initialized, modified_by, verification_type, validations, is_mandatory)
        VALUES (:metadata::jsonb, :data::jsonb, :hidden, :orderTree, :modifiedAt, :description, :checklistsId, :rules::jsonb, :label, :type, :autoInitialize::jsonb, :archived, :createdAt, :createdBy, :targetEntityType, :tasksId, :isAutoInitialized, :modifiedBy, :verificationType, :validations::jsonb, :isMandatory)
        RETURNING id
        """;

    public static final String UPDATE = """
        UPDATE parameters SET
            metadata = :metadata::jsonb,
            data = :data::jsonb,
            hidden = :hidden,
            order_tree = :orderTree,
            modified_at = :modifiedAt,
            description = :description,
            checklists_id = :checklistsId,
            rules = :rules::jsonb,
            label = :label,
            type = :type,
            auto_initialize = :autoInitialize::jsonb,
            archived = :archived,
            created_at = :createdAt,
            created_by = :createdBy,
            target_entity_type = :targetEntityType,
            tasks_id = :tasksId,
            is_auto_initialized = :isAutoInitialized,
            modified_by = :modifiedBy,
            verification_type = :verificationType,
            validations = :validations::jsonb,
            is_mandatory = :isMandatory
        WHERE id = :id
        """;

    public static final String DELETE_BY_ID = """
        DELETE FROM parameters
        WHERE id = :id
        """;

    public static final String EXISTS_BY_ID = """
        SELECT COUNT(*) FROM parameters
        WHERE id = :id
        """;

    public static final String COUNT = """
        SELECT COUNT(*) FROM parameters
        """;

    public static final String FIND_BY_TASKS_ID = """
        SELECT metadata, data, hidden, order_tree, modified_at, description, checklists_id, rules, label, type, auto_initialize, archived, created_at, created_by, target_entity_type, tasks_id, is_auto_initialized, modified_by, verification_type, id, validations, is_mandatory
        FROM parameters
        WHERE tasks_id = :tasksId
        """;

    public static final String FIND_BY_CHECKLISTS_ID = """
        SELECT metadata, data, hidden, order_tree, modified_at, description, checklists_id, rules, label, type, auto_initialize, archived, created_at, created_by, target_entity_type, tasks_id, is_auto_initialized, modified_by, verification_type, id, validations, is_mandatory
        FROM parameters
        WHERE checklists_id = :checklistsId
        """;

    public static final String FIND_BY_MODIFIED_BY = """
        SELECT metadata, data, hidden, order_tree, modified_at, description, checklists_id, rules, label, type, auto_initialize, archived, created_at, created_by, target_entity_type, tasks_id, is_auto_initialized, modified_by, verification_type, id, validations, is_mandatory
        FROM parameters
        WHERE modified_by = :modifiedBy
        """;

    public static final String FIND_BY_CREATED_BY = """
        SELECT metadata, data, hidden, order_tree, modified_at, description, checklists_id, rules, label, type, auto_initialize, archived, created_at, created_by, target_entity_type, tasks_id, is_auto_initialized, modified_by, verification_type, id, validations, is_mandatory
        FROM parameters
        WHERE created_by = :createdBy
        """;

    private ParametersSql() {
        // Utility class
    }
}
