package com.example.daoGenerator.dao.sql;

/**
 * Enhanced SQL constants for Medias entity
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
public final class MediasSql {

    public static final String FIND_ALL = """
        SELECT organisations_id, modified_at, description, type, archived, created_at, filename, created_by, relative_path, name, modified_by, id, original_filename
        FROM medias
        """;

    public static final String COUNT = """
        SELECT COUNT(*) FROM medias
        """;

    public static final String FIND_BY_ID = """
        SELECT organisations_id, modified_at, description, type, archived, created_at, filename, created_by, relative_path, name, modified_by, id, original_filename
        FROM medias
        WHERE id = :id
        """;

    public static final String INSERT = """
        INSERT INTO medias (organisations_id, modified_at, description, type, archived, created_at, filename, created_by, relative_path, name, modified_by, original_filename)
        VALUES (:organisationsId, :modifiedAt, :description, :type, :archived, :createdAt, :filename, :createdBy, :relativePath, :name, :modifiedBy, :originalFilename)
        RETURNING id
        """;

    public static final String UPDATE = """
        UPDATE medias SET
            organisations_id = :organisationsId,
            modified_at = :modifiedAt,
            description = :description,
            type = :type,
            archived = :archived,
            created_at = :createdAt,
            filename = :filename,
            created_by = :createdBy,
            relative_path = :relativePath,
            name = :name,
            modified_by = :modifiedBy,
            original_filename = :originalFilename
        WHERE id = :id
        """;

    public static final String DELETE_BY_ID = """
        DELETE FROM medias
        WHERE id = :id
        """;

    public static final String EXISTS_BY_ID = """
        SELECT COUNT(*) FROM medias
        WHERE id = :id
        """;

    public static final String FIND_BY_MODIFIED_BY = """
        SELECT organisations_id, modified_at, description, type, archived, created_at, filename, created_by, relative_path, name, modified_by, id, original_filename
        FROM medias
        WHERE modified_by = :modifiedBy
        """;

    public static final String FIND_BY_ORGANISATIONS_ID = """
        SELECT organisations_id, modified_at, description, type, archived, created_at, filename, created_by, relative_path, name, modified_by, id, original_filename
        FROM medias
        WHERE organisations_id = :organisationsId
        """;

    public static final String FIND_BY_CREATED_BY = """
        SELECT organisations_id, modified_at, description, type, archived, created_at, filename, created_by, relative_path, name, modified_by, id, original_filename
        FROM medias
        WHERE created_by = :createdBy
        """;

    private MediasSql() {
        // Utility class
    }
}
