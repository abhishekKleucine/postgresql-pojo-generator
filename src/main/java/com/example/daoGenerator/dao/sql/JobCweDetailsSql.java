package com.example.daoGenerator.dao.sql;

/**
 * Enhanced SQL constants for JobCweDetails entity
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
public final class JobCweDetailsSql {

    public static final String FIND_ALL = """
        SELECT reason, created_at, created_by, modified_at, comment, modified_by, id, jobs_id
        FROM job_cwe_details
        """;

    public static final String COUNT = """
        SELECT COUNT(*) FROM job_cwe_details
        """;

    public static final String FIND_BY_ID = """
        SELECT reason, created_at, created_by, modified_at, comment, modified_by, id, jobs_id
        FROM job_cwe_details
        WHERE id = :id
        """;

    public static final String INSERT = """
        INSERT INTO job_cwe_details (reason, created_at, created_by, modified_at, comment, modified_by, jobs_id)
        VALUES (:reason, :createdAt, :createdBy, :modifiedAt, :comment, :modifiedBy, :jobsId)
        RETURNING id
        """;

    public static final String UPDATE = """
        UPDATE job_cwe_details SET
            reason = :reason,
            created_at = :createdAt,
            created_by = :createdBy,
            modified_at = :modifiedAt,
            comment = :comment,
            modified_by = :modifiedBy,
            jobs_id = :jobsId
        WHERE id = :id
        """;

    public static final String DELETE_BY_ID = """
        DELETE FROM job_cwe_details
        WHERE id = :id
        """;

    public static final String EXISTS_BY_ID = """
        SELECT COUNT(*) FROM job_cwe_details
        WHERE id = :id
        """;

    public static final String FIND_BY_MODIFIED_BY = """
        SELECT reason, created_at, created_by, modified_at, comment, modified_by, id, jobs_id
        FROM job_cwe_details
        WHERE modified_by = :modifiedBy
        """;

    public static final String FIND_BY_JOBS_ID = """
        SELECT reason, created_at, created_by, modified_at, comment, modified_by, id, jobs_id
        FROM job_cwe_details
        WHERE jobs_id = :jobsId
        """;

    public static final String FIND_BY_CREATED_BY = """
        SELECT reason, created_at, created_by, modified_at, comment, modified_by, id, jobs_id
        FROM job_cwe_details
        WHERE created_by = :createdBy
        """;

    private JobCweDetailsSql() {
        // Utility class
    }
}
