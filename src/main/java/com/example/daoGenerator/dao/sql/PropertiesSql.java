package com.example.daoGenerator.dao.sql;

/**
 * Enhanced SQL constants for Properties entity
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
public final class PropertiesSql {

    public static final String FIND_ALL = """
        SELECT order_tree, use_cases_id, modified_at, label, type, archived, created_at, created_by, name, is_global, modified_by, id, place_holder
        FROM properties
        """;

    public static final String COUNT = """
        SELECT COUNT(*) FROM properties
        """;

    public static final String FIND_BY_ID = """
        SELECT order_tree, use_cases_id, modified_at, label, type, archived, created_at, created_by, name, is_global, modified_by, id, place_holder
        FROM properties
        WHERE id = :id
        """;

    public static final String INSERT = """
        INSERT INTO properties (order_tree, use_cases_id, modified_at, label, type, archived, created_at, created_by, name, is_global, modified_by, place_holder)
        VALUES (:orderTree, :useCasesId, :modifiedAt, :label, :type, :archived, :createdAt, :createdBy, :name, :isGlobal, :modifiedBy, :placeHolder)
        RETURNING id
        """;

    public static final String UPDATE = """
        UPDATE properties SET
            order_tree = :orderTree,
            use_cases_id = :useCasesId,
            modified_at = :modifiedAt,
            label = :label,
            type = :type,
            archived = :archived,
            created_at = :createdAt,
            created_by = :createdBy,
            name = :name,
            is_global = :isGlobal,
            modified_by = :modifiedBy,
            place_holder = :placeHolder
        WHERE id = :id
        """;

    public static final String DELETE_BY_ID = """
        DELETE FROM properties
        WHERE id = :id
        """;

    public static final String EXISTS_BY_ID = """
        SELECT COUNT(*) FROM properties
        WHERE id = :id
        """;

    public static final String FIND_BY_USE_CASES_ID = """
        SELECT order_tree, use_cases_id, modified_at, label, type, archived, created_at, created_by, name, is_global, modified_by, id, place_holder
        FROM properties
        WHERE use_cases_id = :useCasesId
        """;

    public static final String FIND_BY_MODIFIED_BY = """
        SELECT order_tree, use_cases_id, modified_at, label, type, archived, created_at, created_by, name, is_global, modified_by, id, place_holder
        FROM properties
        WHERE modified_by = :modifiedBy
        """;

    public static final String FIND_BY_CREATED_BY = """
        SELECT order_tree, use_cases_id, modified_at, label, type, archived, created_at, created_by, name, is_global, modified_by, id, place_holder
        FROM properties
        WHERE created_by = :createdBy
        """;

    private PropertiesSql() {
        // Utility class
    }
}
