package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.TempParameterValuesDao;
import com.example.pojogenerator.pojos.TempParameterValues;
import com.example.daoGenerator.dao.mapper.TempParameterValuesRowMapper;
import com.example.daoGenerator.dao.sql.TempParameterValuesSql;

/**
 * JDBC implementation of TempParameterValuesDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcTempParameterValuesDao implements TempParameterValuesDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final TempParameterValuesRowMapper rowMapper;

    public JdbcTempParameterValuesDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new TempParameterValuesRowMapper();
    }

    @Override
    public Optional<TempParameterValues> findById(Long id) {
        try {
            TempParameterValues result = jdbcTemplate.queryForObject(
                TempParameterValuesSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<TempParameterValues> findAll() {
        return jdbcTemplate.query(TempParameterValuesSql.FIND_ALL, rowMapper);
    }

    @Override
    public TempParameterValues save(TempParameterValues entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private TempParameterValues insert(TempParameterValues entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            TempParameterValuesSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private TempParameterValues update(TempParameterValues entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            TempParameterValuesSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(TempParameterValues entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("reason", entity.getReason());
        params.addValue("hidden", entity.getHidden());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("verified", entity.getVerified());
        params.addValue("parametersId", entity.getParametersId());
        params.addValue("clientEpoch", entity.getClientEpoch());
        params.addValue("version", entity.getVersion());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("hasVariations", entity.getHasVariations());
        if (entity.getImpactedBy() != null) {
            params.addValue("impactedBy", entity.getImpactedBy().toString());
        } else {
            params.addValue("impactedBy", null);
        }
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("state", entity.getState());
        params.addValue("taskExecutionsId", entity.getTaskExecutionsId());
        if (entity.getChoices() != null) {
            params.addValue("choices", entity.getChoices().toString());
        } else {
            params.addValue("choices", null);
        }
        params.addValue("value", entity.getValue());
        params.addValue("jobsId", entity.getJobsId());
        params.addValue("parameterValueApprovalId", entity.getParameterValueApprovalId());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(TempParameterValuesSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            TempParameterValuesSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(TempParameterValuesSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<TempParameterValues> findByParametersId(Long parametersId) {
        return jdbcTemplate.query(
            TempParameterValuesSql.FIND_BY_PARAMETERS_ID,
            Map.of("parametersId", parametersId),
            rowMapper
        );
    }

    @Override
    public List<TempParameterValues> findByTaskExecutionsId(Long taskExecutionsId) {
        return jdbcTemplate.query(
            TempParameterValuesSql.FIND_BY_TASK_EXECUTIONS_ID,
            Map.of("taskExecutionsId", taskExecutionsId),
            rowMapper
        );
    }

    @Override
    public List<TempParameterValues> findByParameterValueApprovalId(Long parameterValueApprovalId) {
        return jdbcTemplate.query(
            TempParameterValuesSql.FIND_BY_PARAMETER_VALUE_APPROVAL_ID,
            Map.of("parameterValueApprovalId", parameterValueApprovalId),
            rowMapper
        );
    }

    @Override
    public List<TempParameterValues> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            TempParameterValuesSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<TempParameterValues> findByJobsId(Long jobsId) {
        return jdbcTemplate.query(
            TempParameterValuesSql.FIND_BY_JOBS_ID,
            Map.of("jobsId", jobsId),
            rowMapper
        );
    }

    @Override
    public List<TempParameterValues> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            TempParameterValuesSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

}
