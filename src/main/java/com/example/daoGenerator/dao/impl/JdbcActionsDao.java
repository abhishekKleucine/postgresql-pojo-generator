package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ActionsDao;
import com.example.pojogenerator.pojos.Actions;
import com.example.daoGenerator.dao.mapper.ActionsRowMapper;
import com.example.daoGenerator.dao.sql.ActionsSql;

/**
 * JDBC implementation of ActionsDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcActionsDao implements ActionsDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ActionsRowMapper rowMapper;

    public JdbcActionsDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ActionsRowMapper();
    }

    @Override
    public Optional<Actions> findById(Long id) {
        try {
            Actions result = jdbcTemplate.queryForObject(
                ActionsSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<Actions> findAll() {
        return jdbcTemplate.query(ActionsSql.FIND_ALL, rowMapper);
    }

    @Override
    public Actions save(Actions entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private Actions insert(Actions entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ActionsSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private Actions update(Actions entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            ActionsSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(Actions entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("code", entity.getCode());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("description", entity.getDescription());
        params.addValue("checklistsId", entity.getChecklistsId());
        params.addValue("successMessage", entity.getSuccessMessage());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("archived", entity.getArchived());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("name", entity.getName());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("triggerType", entity.getTriggerType());
        params.addValue("triggerEntityId", entity.getTriggerEntityId());
        params.addValue("failureMessage", entity.getFailureMessage());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ActionsSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ActionsSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ActionsSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<Actions> findByChecklistsId(Long checklistsId) {
        return jdbcTemplate.query(
            ActionsSql.FIND_BY_CHECKLISTS_ID,
            Map.of("checklistsId", checklistsId),
            rowMapper
        );
    }

    @Override
    public List<Actions> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            ActionsSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<Actions> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            ActionsSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

}
