package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ProcessPermissionDao;
import com.example.pojogenerator.pojos.ProcessPermission;
import com.example.daoGenerator.dao.mapper.ProcessPermissionRowMapper;
import com.example.daoGenerator.dao.sql.ProcessPermissionSql;

/**
 * Enhanced JDBC implementation of ProcessPermissionDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcProcessPermissionDao implements ProcessPermissionDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ProcessPermissionRowMapper rowMapper;

    public JdbcProcessPermissionDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ProcessPermissionRowMapper();
    }

    @Override
    public Optional<ProcessPermission> findById(Long id) {
        try {
            ProcessPermission result = jdbcTemplate.queryForObject(
                ProcessPermissionSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ProcessPermissionSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ProcessPermissionSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<ProcessPermission> findAll() {
        return jdbcTemplate.query(ProcessPermissionSql.FIND_ALL, rowMapper);
    }

    @Override
    public ProcessPermission save(ProcessPermission entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ProcessPermission insert(ProcessPermission entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ProcessPermissionSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ProcessPermission update(ProcessPermission entity) {
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(ProcessPermissionSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ProcessPermissionSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    private MapSqlParameterSource createParameterMap(ProcessPermission entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("description", entity.getDescription());
        params.addValue("id", entity.getId());
        params.addValue("type", entity.getType());

        return params;
    }

}
