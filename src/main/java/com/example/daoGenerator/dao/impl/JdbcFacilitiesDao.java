package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.FacilitiesDao;
import com.example.pojogenerator.pojos.Facilities;
import com.example.daoGenerator.dao.mapper.FacilitiesRowMapper;
import com.example.daoGenerator.dao.sql.FacilitiesSql;

/**
 * JDBC implementation of FacilitiesDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcFacilitiesDao implements FacilitiesDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final FacilitiesRowMapper rowMapper;

    public JdbcFacilitiesDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new FacilitiesRowMapper();
    }

    @Override
    public Optional<Facilities> findById(Long id) {
        try {
            Facilities result = jdbcTemplate.queryForObject(
                FacilitiesSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<Facilities> findAll() {
        return jdbcTemplate.query(FacilitiesSql.FIND_ALL, rowMapper);
    }

    @Override
    public Facilities save(Facilities entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private Facilities insert(Facilities entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            FacilitiesSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private Facilities update(Facilities entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            FacilitiesSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(Facilities entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("organisationsId", entity.getOrganisationsId());
        params.addValue("dateFormat", entity.getDateFormat());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("timeZone", entity.getTimeZone());
        params.addValue("logoUrl", entity.getLogoUrl());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("archived", entity.getArchived());
        params.addValue("dateTimeFormat", entity.getDateTimeFormat());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("timeFormat", entity.getTimeFormat());
        params.addValue("name", entity.getName());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(FacilitiesSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            FacilitiesSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(FacilitiesSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<Facilities> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            FacilitiesSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<Facilities> findByOrganisationsId(Long organisationsId) {
        return jdbcTemplate.query(
            FacilitiesSql.FIND_BY_ORGANISATIONS_ID,
            Map.of("organisationsId", organisationsId),
            rowMapper
        );
    }

    @Override
    public List<Facilities> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            FacilitiesSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

}
