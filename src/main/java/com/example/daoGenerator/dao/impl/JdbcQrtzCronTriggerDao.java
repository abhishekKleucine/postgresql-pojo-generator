package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.QrtzCronTriggerDao;
import com.example.pojogenerator.pojos.QrtzCronTrigger;
import com.example.daoGenerator.dao.mapper.QrtzCronTriggerRowMapper;
import com.example.daoGenerator.dao.sql.QrtzCronTriggerSql;

/**
 * Enhanced JDBC implementation of QrtzCronTriggerDao
 * Key Type: COMPOSITE_KEY
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcQrtzCronTriggerDao implements QrtzCronTriggerDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final QrtzCronTriggerRowMapper rowMapper;

    public JdbcQrtzCronTriggerDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new QrtzCronTriggerRowMapper();
    }

    @Override
    public Optional<QrtzCronTrigger> findByTriggerNameAndSchedNameAndTriggerGroup(String triggerName, String schedName, String triggerGroup) {
        try {
            QrtzCronTrigger result = jdbcTemplate.queryForObject(
                QrtzCronTriggerSql.FIND_BY_TRIGGER_NAME_AND_SCHED_NAME_AND_TRIGGER_GROUP,
                Map.of("triggerName", triggerName, "schedName", schedName, "triggerGroup", triggerGroup),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteByTriggerNameAndSchedNameAndTriggerGroup(String triggerName, String schedName, String triggerGroup) {
        jdbcTemplate.update(QrtzCronTriggerSql.DELETE_BY_TRIGGER_NAME_AND_SCHED_NAME_AND_TRIGGER_GROUP, Map.of("triggerName", triggerName, "schedName", schedName, "triggerGroup", triggerGroup));
    }

    @Override
    public boolean existsByTriggerNameAndSchedNameAndTriggerGroup(String triggerName, String schedName, String triggerGroup) {
        Integer count = jdbcTemplate.queryForObject(
            QrtzCronTriggerSql.EXISTS_BY_TRIGGER_NAME_AND_SCHED_NAME_AND_TRIGGER_GROUP,
            Map.of("triggerName", triggerName, "schedName", schedName, "triggerGroup", triggerGroup),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<QrtzCronTrigger> findAll() {
        return jdbcTemplate.query(QrtzCronTriggerSql.FIND_ALL, rowMapper);
    }

    @Override
    public QrtzCronTrigger save(QrtzCronTrigger entity) {
        if (existsByTriggerNameAndSchedNameAndTriggerGroup(entity.getTriggerName(), entity.getSchedName(), entity.getTriggerGroup())) {
            return update(entity);
        } else {
            return insert(entity);
        }
    }

    private QrtzCronTrigger insert(QrtzCronTrigger entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();

        MapSqlParameterSource params = createParameterMap(entity);
        jdbcTemplate.update(QrtzCronTriggerSql.INSERT, params);
        return entity;
    }

    private QrtzCronTrigger update(QrtzCronTrigger entity) {
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(QrtzCronTriggerSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(QrtzCronTriggerSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<QrtzCronTrigger> findByTriggerName(Long triggerName) {
        return jdbcTemplate.query(
            QrtzCronTriggerSql.FIND_BY_TRIGGER_NAME,
            Map.of("triggerName", triggerName),
            rowMapper
        );
    }

    @Override
    public List<QrtzCronTrigger> findBySchedName(Long schedName) {
        return jdbcTemplate.query(
            QrtzCronTriggerSql.FIND_BY_SCHED_NAME,
            Map.of("schedName", schedName),
            rowMapper
        );
    }

    @Override
    public List<QrtzCronTrigger> findByTriggerGroup(Long triggerGroup) {
        return jdbcTemplate.query(
            QrtzCronTriggerSql.FIND_BY_TRIGGER_GROUP,
            Map.of("triggerGroup", triggerGroup),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(QrtzCronTrigger entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("cronExpression", entity.getCronExpression());
        params.addValue("triggerGroup", entity.getTriggerGroup());
        params.addValue("triggerName", entity.getTriggerName());
        params.addValue("timeZoneId", entity.getTimeZoneId());
        params.addValue("schedName", entity.getSchedName());

        return params;
    }

}
