package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.RelationValueDao;
import com.example.pojogenerator.pojos.RelationValue;
import com.example.daoGenerator.dao.mapper.RelationValueRowMapper;
import com.example.daoGenerator.dao.sql.RelationValueSql;

/**
 * Enhanced JDBC implementation of RelationValueDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcRelationValueDao implements RelationValueDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final RelationValueRowMapper rowMapper;

    public JdbcRelationValueDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new RelationValueRowMapper();
    }

    @Override
    public Optional<RelationValue> findById(Long id) {
        try {
            RelationValue result = jdbcTemplate.queryForObject(
                RelationValueSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(RelationValueSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            RelationValueSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<RelationValue> findAll() {
        return jdbcTemplate.query(RelationValueSql.FIND_ALL, rowMapper);
    }

    @Override
    public RelationValue save(RelationValue entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private RelationValue insert(RelationValue entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            RelationValueSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private RelationValue update(RelationValue entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(RelationValueSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(RelationValueSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<RelationValue> findByRelationsId(Long relationsId) {
        return jdbcTemplate.query(
            RelationValueSql.FIND_BY_RELATIONS_ID,
            Map.of("relationsId", relationsId),
            rowMapper
        );
    }

    @Override
    public List<RelationValue> findByJobsId(Long jobsId) {
        return jdbcTemplate.query(
            RelationValueSql.FIND_BY_JOBS_ID,
            Map.of("jobsId", jobsId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(RelationValue entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("relationsId", entity.getRelationsId());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("objectTypeDisplayName", entity.getObjectTypeDisplayName());
        params.addValue("collection", entity.getCollection());
        params.addValue("objectTypeExternalId", entity.getObjectTypeExternalId());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("objectDisplayName", entity.getObjectDisplayName());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("jobsId", entity.getJobsId());
        params.addValue("objectId", entity.getObjectId());
        params.addValue("objectExternalId", entity.getObjectExternalId());

        return params;
    }

}
