package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.CorrectorsDao;
import com.example.pojogenerator.pojos.Correctors;
import com.example.daoGenerator.dao.mapper.CorrectorsRowMapper;
import com.example.daoGenerator.dao.sql.CorrectorsSql;

/**
 * JDBC implementation of CorrectorsDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcCorrectorsDao implements CorrectorsDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final CorrectorsRowMapper rowMapper;

    public JdbcCorrectorsDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new CorrectorsRowMapper();
    }

    @Override
    public Optional<Correctors> findById(Long id) {
        try {
            Correctors result = jdbcTemplate.queryForObject(
                CorrectorsSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<Correctors> findAll() {
        return jdbcTemplate.query(CorrectorsSql.FIND_ALL, rowMapper);
    }

    @Override
    public Correctors save(Correctors entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private Correctors insert(Correctors entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            CorrectorsSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private Correctors update(Correctors entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            CorrectorsSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(Correctors entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("correctionsId", entity.getCorrectionsId());
        params.addValue("actionPerformed", entity.getActionPerformed());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("userGroupsId", entity.getUserGroupsId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("usersId", entity.getUsersId());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(CorrectorsSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            CorrectorsSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(CorrectorsSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<Correctors> findByUserGroupsId(Long userGroupsId) {
        return jdbcTemplate.query(
            CorrectorsSql.FIND_BY_USER_GROUPS_ID,
            Map.of("userGroupsId", userGroupsId),
            rowMapper
        );
    }

    @Override
    public List<Correctors> findByCorrectionsId(Long correctionsId) {
        return jdbcTemplate.query(
            CorrectorsSql.FIND_BY_CORRECTIONS_ID,
            Map.of("correctionsId", correctionsId),
            rowMapper
        );
    }

    @Override
    public List<Correctors> findByUsersId(Long usersId) {
        return jdbcTemplate.query(
            CorrectorsSql.FIND_BY_USERS_ID,
            Map.of("usersId", usersId),
            rowMapper
        );
    }

}
