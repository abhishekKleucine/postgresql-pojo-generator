package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.UserGroupAuditsDao;
import com.example.pojogenerator.pojos.UserGroupAudits;
import com.example.daoGenerator.dao.mapper.UserGroupAuditsRowMapper;
import com.example.daoGenerator.dao.sql.UserGroupAuditsSql;

/**
 * Enhanced JDBC implementation of UserGroupAuditsDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcUserGroupAuditsDao implements UserGroupAuditsDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final UserGroupAuditsRowMapper rowMapper;

    public JdbcUserGroupAuditsDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new UserGroupAuditsRowMapper();
    }

    @Override
    public Optional<UserGroupAudits> findById(Long id) {
        try {
            UserGroupAudits result = jdbcTemplate.queryForObject(
                UserGroupAuditsSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(UserGroupAuditsSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            UserGroupAuditsSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<UserGroupAudits> findAll() {
        return jdbcTemplate.query(UserGroupAuditsSql.FIND_ALL, rowMapper);
    }

    @Override
    public UserGroupAudits save(UserGroupAudits entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private UserGroupAudits insert(UserGroupAudits entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            UserGroupAuditsSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private UserGroupAudits update(UserGroupAudits entity) {
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(UserGroupAuditsSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(UserGroupAuditsSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<UserGroupAudits> findByUserGroupsId(Long userGroupsId) {
        return jdbcTemplate.query(
            UserGroupAuditsSql.FIND_BY_USER_GROUPS_ID,
            Map.of("userGroupsId", userGroupsId),
            rowMapper
        );
    }

    @Override
    public List<UserGroupAudits> findByOrganisationsId(Long organisationsId) {
        return jdbcTemplate.query(
            UserGroupAuditsSql.FIND_BY_ORGANISATIONS_ID,
            Map.of("organisationsId", organisationsId),
            rowMapper
        );
    }

    @Override
    public List<UserGroupAudits> findByFacilitiesId(Long facilitiesId) {
        return jdbcTemplate.query(
            UserGroupAuditsSql.FIND_BY_FACILITIES_ID,
            Map.of("facilitiesId", facilitiesId),
            rowMapper
        );
    }

    @Override
    public List<UserGroupAudits> findByTriggeredBy(Long triggeredBy) {
        return jdbcTemplate.query(
            UserGroupAuditsSql.FIND_BY_TRIGGERED_BY,
            Map.of("triggeredBy", triggeredBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(UserGroupAudits entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("facilitiesId", entity.getFacilitiesId());
        params.addValue("userGroupsId", entity.getUserGroupsId());
        params.addValue("organisationsId", entity.getOrganisationsId());
        params.addValue("details", entity.getDetails());
        params.addValue("id", entity.getId());
        params.addValue("triggeredAt", entity.getTriggeredAt());
        params.addValue("triggeredBy", entity.getTriggeredBy());

        return params;
    }

}
