package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.VersionDao;
import com.example.pojogenerator.pojos.Version;
import com.example.daoGenerator.dao.mapper.VersionRowMapper;
import com.example.daoGenerator.dao.sql.VersionSql;

/**
 * Enhanced JDBC implementation of VersionDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcVersionDao implements VersionDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final VersionRowMapper rowMapper;

    public JdbcVersionDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new VersionRowMapper();
    }

    @Override
    public Optional<Version> findById(Long id) {
        try {
            Version result = jdbcTemplate.queryForObject(
                VersionSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(VersionSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            VersionSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<Version> findAll() {
        return jdbcTemplate.query(VersionSql.FIND_ALL, rowMapper);
    }

    @Override
    public Version save(Version entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private Version insert(Version entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            VersionSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private Version update(Version entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(VersionSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(VersionSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<Version> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            VersionSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<Version> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            VersionSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(Version entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("versionedAt", entity.getVersionedAt());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("parent", entity.getParent());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("deprecatedAt", entity.getDeprecatedAt());
        params.addValue("ancestor", entity.getAncestor());
        params.addValue("self", entity.getSelf());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("type", entity.getType());
        params.addValue("version", entity.getVersion());

        return params;
    }

}
