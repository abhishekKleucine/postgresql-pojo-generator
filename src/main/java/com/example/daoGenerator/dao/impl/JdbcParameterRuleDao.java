package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ParameterRuleDao;
import com.example.pojogenerator.pojos.ParameterRule;
import com.example.daoGenerator.dao.mapper.ParameterRuleRowMapper;
import com.example.daoGenerator.dao.sql.ParameterRuleSql;

/**
 * Enhanced JDBC implementation of ParameterRuleDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcParameterRuleDao implements ParameterRuleDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ParameterRuleRowMapper rowMapper;

    public JdbcParameterRuleDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ParameterRuleRowMapper();
    }

    @Override
    public Optional<ParameterRule> findById(Long id) {
        try {
            ParameterRule result = jdbcTemplate.queryForObject(
                ParameterRuleSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ParameterRuleSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ParameterRuleSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<ParameterRule> findAll() {
        return jdbcTemplate.query(ParameterRuleSql.FIND_ALL, rowMapper);
    }

    @Override
    public ParameterRule save(ParameterRule entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ParameterRule insert(ParameterRule entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ParameterRuleSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ParameterRule update(ParameterRule entity) {
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(ParameterRuleSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ParameterRuleSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    private MapSqlParameterSource createParameterMap(ParameterRule entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("input", entity.getInput());
        params.addValue("visibility", entity.getVisibility());
        params.addValue("id", entity.getId());
        params.addValue("rulesId", entity.getRulesId());
        params.addValue("operator", entity.getOperator());

        return params;
    }

}
