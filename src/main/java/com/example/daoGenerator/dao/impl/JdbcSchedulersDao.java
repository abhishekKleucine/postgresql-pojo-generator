package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.SchedulersDao;
import com.example.pojogenerator.pojos.Schedulers;
import com.example.daoGenerator.dao.mapper.SchedulersRowMapper;
import com.example.daoGenerator.dao.sql.SchedulersSql;

/**
 * Enhanced JDBC implementation of SchedulersDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcSchedulersDao implements SchedulersDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final SchedulersRowMapper rowMapper;

    public JdbcSchedulersDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new SchedulersRowMapper();
    }

    @Override
    public Optional<Schedulers> findById(Long id) {
        try {
            Schedulers result = jdbcTemplate.queryForObject(
                SchedulersSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(SchedulersSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            SchedulersSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<Schedulers> findAll() {
        return jdbcTemplate.query(SchedulersSql.FIND_ALL, rowMapper);
    }

    @Override
    public Schedulers save(Schedulers entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private Schedulers insert(Schedulers entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            SchedulersSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private Schedulers update(Schedulers entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(SchedulersSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(SchedulersSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<Schedulers> findByUseCasesId(Long useCasesId) {
        return jdbcTemplate.query(
            SchedulersSql.FIND_BY_USE_CASES_ID,
            Map.of("useCasesId", useCasesId),
            rowMapper
        );
    }

    @Override
    public List<Schedulers> findByChecklistsId(Long checklistsId) {
        return jdbcTemplate.query(
            SchedulersSql.FIND_BY_CHECKLISTS_ID,
            Map.of("checklistsId", checklistsId),
            rowMapper
        );
    }

    @Override
    public List<Schedulers> findByFacilitiesId(Long facilitiesId) {
        return jdbcTemplate.query(
            SchedulersSql.FIND_BY_FACILITIES_ID,
            Map.of("facilitiesId", facilitiesId),
            rowMapper
        );
    }

    @Override
    public List<Schedulers> findByVersionsId(Long versionsId) {
        return jdbcTemplate.query(
            SchedulersSql.FIND_BY_VERSIONS_ID,
            Map.of("versionsId", versionsId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(Schedulers entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("expectedStartDate", entity.getExpectedStartDate());
        params.addValue("facilitiesId", entity.getFacilitiesId());
        params.addValue("code", entity.getCode());
        if (entity.getData() != null) {
            params.addValue("data", entity.getData().toString());
        } else {
            params.addValue("data", null);
        }
        params.addValue("useCasesId", entity.getUseCasesId());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("description", entity.getDescription());
        params.addValue("checklistsId", entity.getChecklistsId());
        params.addValue("isRepeated", entity.getIsRepeated());
        params.addValue("recurrenceRule", entity.getRecurrenceRule());
        params.addValue("enabled", entity.getEnabled());
        params.addValue("archived", entity.getArchived());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("checklistsName", entity.getChecklistsName());
        params.addValue("createdBy", entity.getCreatedBy());
        if (entity.getDueDateDuration() != null) {
            params.addValue("dueDateDuration", entity.getDueDateDuration().toString());
        } else {
            params.addValue("dueDateDuration", null);
        }
        params.addValue("deprecatedAt", entity.getDeprecatedAt());
        params.addValue("name", entity.getName());
        params.addValue("isCustomRecurrence", entity.getIsCustomRecurrence());
        params.addValue("dueDateInterval", entity.getDueDateInterval());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("state", entity.getState());
        params.addValue("versionsId", entity.getVersionsId());

        return params;
    }

}
