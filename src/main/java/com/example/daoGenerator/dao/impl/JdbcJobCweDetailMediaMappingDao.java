package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.JobCweDetailMediaMappingDao;
import com.example.pojogenerator.pojos.JobCweDetailMediaMapping;
import com.example.daoGenerator.dao.mapper.JobCweDetailMediaMappingRowMapper;
import com.example.daoGenerator.dao.sql.JobCweDetailMediaMappingSql;

/**
 * JDBC implementation of JobCweDetailMediaMappingDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcJobCweDetailMediaMappingDao implements JobCweDetailMediaMappingDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final JobCweDetailMediaMappingRowMapper rowMapper;

    public JdbcJobCweDetailMediaMappingDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new JobCweDetailMediaMappingRowMapper();
    }

    @Override
    public Optional<JobCweDetailMediaMapping> findById(Long id) {
        try {
            JobCweDetailMediaMapping result = jdbcTemplate.queryForObject(
                JobCweDetailMediaMappingSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<JobCweDetailMediaMapping> findAll() {
        return jdbcTemplate.query(JobCweDetailMediaMappingSql.FIND_ALL, rowMapper);
    }

    @Override
    public JobCweDetailMediaMapping save(JobCweDetailMediaMapping entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private JobCweDetailMediaMapping insert(JobCweDetailMediaMapping entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            JobCweDetailMediaMappingSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private JobCweDetailMediaMapping update(JobCweDetailMediaMapping entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            JobCweDetailMediaMappingSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(JobCweDetailMediaMapping entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("jobCweDetailsId", entity.getJobCweDetailsId());
        params.addValue("mediasId", entity.getMediasId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("modifiedBy", entity.getModifiedBy());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(JobCweDetailMediaMappingSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            JobCweDetailMediaMappingSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(JobCweDetailMediaMappingSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<JobCweDetailMediaMapping> findByJobCweDetailsId(Long jobCweDetailsId) {
        return jdbcTemplate.query(
            JobCweDetailMediaMappingSql.FIND_BY_JOB_CWE_DETAILS_ID,
            Map.of("jobCweDetailsId", jobCweDetailsId),
            rowMapper
        );
    }

    @Override
    public List<JobCweDetailMediaMapping> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            JobCweDetailMediaMappingSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<JobCweDetailMediaMapping> findByMediasId(Long mediasId) {
        return jdbcTemplate.query(
            JobCweDetailMediaMappingSql.FIND_BY_MEDIAS_ID,
            Map.of("mediasId", mediasId),
            rowMapper
        );
    }

    @Override
    public List<JobCweDetailMediaMapping> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            JobCweDetailMediaMappingSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

}
