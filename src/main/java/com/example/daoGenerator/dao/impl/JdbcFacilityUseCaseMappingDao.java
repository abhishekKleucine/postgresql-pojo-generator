package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.FacilityUseCaseMappingDao;
import com.example.pojogenerator.pojos.FacilityUseCaseMapping;
import com.example.daoGenerator.dao.mapper.FacilityUseCaseMappingRowMapper;
import com.example.daoGenerator.dao.sql.FacilityUseCaseMappingSql;

/**
 * JDBC implementation of FacilityUseCaseMappingDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcFacilityUseCaseMappingDao implements FacilityUseCaseMappingDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final FacilityUseCaseMappingRowMapper rowMapper;

    public JdbcFacilityUseCaseMappingDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new FacilityUseCaseMappingRowMapper();
    }

    @Override
    public Optional<FacilityUseCaseMapping> findById(Long id) {
        try {
            FacilityUseCaseMapping result = jdbcTemplate.queryForObject(
                FacilityUseCaseMappingSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<FacilityUseCaseMapping> findAll() {
        return jdbcTemplate.query(FacilityUseCaseMappingSql.FIND_ALL, rowMapper);
    }

    @Override
    public FacilityUseCaseMapping save(FacilityUseCaseMapping entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private FacilityUseCaseMapping insert(FacilityUseCaseMapping entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            FacilityUseCaseMappingSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private FacilityUseCaseMapping update(FacilityUseCaseMapping entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            FacilityUseCaseMappingSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(FacilityUseCaseMapping entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("facilitiesId", entity.getFacilitiesId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("useCasesId", entity.getUseCasesId());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("quota", entity.getQuota());
        params.addValue("modifiedBy", entity.getModifiedBy());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(FacilityUseCaseMappingSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            FacilityUseCaseMappingSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(FacilityUseCaseMappingSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<FacilityUseCaseMapping> findByUseCasesId(Long useCasesId) {
        return jdbcTemplate.query(
            FacilityUseCaseMappingSql.FIND_BY_USE_CASES_ID,
            Map.of("useCasesId", useCasesId),
            rowMapper
        );
    }

    @Override
    public List<FacilityUseCaseMapping> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            FacilityUseCaseMappingSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<FacilityUseCaseMapping> findByFacilitiesId(Long facilitiesId) {
        return jdbcTemplate.query(
            FacilityUseCaseMappingSql.FIND_BY_FACILITIES_ID,
            Map.of("facilitiesId", facilitiesId),
            rowMapper
        );
    }

    @Override
    public List<FacilityUseCaseMapping> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            FacilityUseCaseMappingSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

}
