package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ChecklistAuditsDao;
import com.example.pojogenerator.pojos.ChecklistAudits;
import com.example.daoGenerator.dao.mapper.ChecklistAuditsRowMapper;
import com.example.daoGenerator.dao.sql.ChecklistAuditsSql;

/**
 * JDBC implementation of ChecklistAuditsDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcChecklistAuditsDao implements ChecklistAuditsDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ChecklistAuditsRowMapper rowMapper;

    public JdbcChecklistAuditsDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ChecklistAuditsRowMapper();
    }

    @Override
    public Optional<ChecklistAudits> findById(Long id) {
        try {
            ChecklistAudits result = jdbcTemplate.queryForObject(
                ChecklistAuditsSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<ChecklistAudits> findAll() {
        return jdbcTemplate.query(ChecklistAuditsSql.FIND_ALL, rowMapper);
    }

    @Override
    public ChecklistAudits save(ChecklistAudits entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ChecklistAudits insert(ChecklistAudits entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ChecklistAuditsSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ChecklistAudits update(ChecklistAudits entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            ChecklistAuditsSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(ChecklistAudits entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("triggeredFor", entity.getTriggeredFor());
        params.addValue("organisationsId", entity.getOrganisationsId());
        params.addValue("tasksId", entity.getTasksId());
        params.addValue("action", entity.getAction());
        params.addValue("checklistsId", entity.getChecklistsId());
        params.addValue("details", entity.getDetails());
        params.addValue("id", entity.getId());
        params.addValue("triggeredAt", entity.getTriggeredAt());
        params.addValue("stagesId", entity.getStagesId());
        params.addValue("triggeredBy", entity.getTriggeredBy());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ChecklistAuditsSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ChecklistAuditsSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ChecklistAuditsSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

}
