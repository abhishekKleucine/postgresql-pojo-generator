package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.LicenseDao;
import com.example.pojogenerator.pojos.License;
import com.example.daoGenerator.dao.mapper.LicenseRowMapper;
import com.example.daoGenerator.dao.sql.LicenseSql;

/**
 * Enhanced JDBC implementation of LicenseDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcLicenseDao implements LicenseDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final LicenseRowMapper rowMapper;

    public JdbcLicenseDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new LicenseRowMapper();
    }

    @Override
    public Optional<License> findById(Long id) {
        try {
            License result = jdbcTemplate.queryForObject(
                LicenseSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(LicenseSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            LicenseSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<License> findAll() {
        return jdbcTemplate.query(LicenseSql.FIND_ALL, rowMapper);
    }

    @Override
    public License save(License entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private License insert(License entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            LicenseSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private License update(License entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(LicenseSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(LicenseSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<License> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            LicenseSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<License> findByOrganisationsId(Long organisationsId) {
        return jdbcTemplate.query(
            LicenseSql.FIND_BY_ORGANISATIONS_ID,
            Map.of("organisationsId", organisationsId),
            rowMapper
        );
    }

    @Override
    public List<License> findByFacilitiesId(Long facilitiesId) {
        return jdbcTemplate.query(
            LicenseSql.FIND_BY_FACILITIES_ID,
            Map.of("facilitiesId", facilitiesId),
            rowMapper
        );
    }

    @Override
    public List<License> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            LicenseSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(License entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("paymentDone", entity.getPaymentDone());
        params.addValue("gracePeriod", entity.getGracePeriod());
        params.addValue("facilitiesId", entity.getFacilitiesId());
        params.addValue("product", entity.getProduct());
        params.addValue("workflow", entity.getWorkflow());
        params.addValue("organisationsId", entity.getOrganisationsId());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("type", entity.getType());
        params.addValue("archived", entity.getArchived());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("subscriptionPeriod", entity.getSubscriptionPeriod());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("subscriptionRenewalDate", entity.getSubscriptionRenewalDate());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("subscriptionStartDate", entity.getSubscriptionStartDate());
        params.addValue("intimateBefore", entity.getIntimateBefore());

        return params;
    }

}
