package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ChallengeQuestionsDao;
import com.example.pojogenerator.pojos.ChallengeQuestions;
import com.example.daoGenerator.dao.mapper.ChallengeQuestionsRowMapper;
import com.example.daoGenerator.dao.sql.ChallengeQuestionsSql;

/**
 * Enhanced JDBC implementation of ChallengeQuestionsDao
 * Key Type: CUSTOM_SINGLE_KEY
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcChallengeQuestionsDao implements ChallengeQuestionsDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ChallengeQuestionsRowMapper rowMapper;

    public JdbcChallengeQuestionsDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ChallengeQuestionsRowMapper();
    }

    @Override
    public Optional<ChallengeQuestions> findById(Integer id) {
        try {
            ChallengeQuestions result = jdbcTemplate.queryForObject(
                ChallengeQuestionsSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Integer id) {
        jdbcTemplate.update(ChallengeQuestionsSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Integer id) {
        Integer count = jdbcTemplate.queryForObject(
            ChallengeQuestionsSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<ChallengeQuestions> findAll() {
        return jdbcTemplate.query(ChallengeQuestionsSql.FIND_ALL, rowMapper);
    }

    @Override
    public ChallengeQuestions save(ChallengeQuestions entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ChallengeQuestions insert(ChallengeQuestions entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        jdbcTemplate.update(ChallengeQuestionsSql.INSERT, params);
        return entity;
    }

    private ChallengeQuestions update(ChallengeQuestions entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(ChallengeQuestionsSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ChallengeQuestionsSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    private MapSqlParameterSource createParameterMap(ChallengeQuestions entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("archived", entity.getArchived());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("question", entity.getQuestion());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());

        return params;
    }

}
