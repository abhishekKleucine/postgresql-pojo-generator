package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.EmailTemplateDao;
import com.example.pojogenerator.pojos.EmailTemplate;
import com.example.daoGenerator.dao.mapper.EmailTemplateRowMapper;
import com.example.daoGenerator.dao.sql.EmailTemplateSql;

/**
 * Enhanced JDBC implementation of EmailTemplateDao
 * Key Type: NO_PRIMARY_KEY
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcEmailTemplateDao implements EmailTemplateDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final EmailTemplateRowMapper rowMapper;

    public JdbcEmailTemplateDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new EmailTemplateRowMapper();
    }

    @Override
    public List<EmailTemplate> findAll() {
        return jdbcTemplate.query(EmailTemplateSql.FIND_ALL, rowMapper);
    }

    @Override
    public EmailTemplate save(EmailTemplate entity) {
        // No primary key - always insert
        return insert(entity);
    }

    private EmailTemplate insert(EmailTemplate entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();

        MapSqlParameterSource params = createParameterMap(entity);
        jdbcTemplate.update(EmailTemplateSql.INSERT, params);
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(EmailTemplateSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    private MapSqlParameterSource createParameterMap(EmailTemplate entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("archived", entity.getArchived());
        params.addValue("name", entity.getName());
        params.addValue("id", entity.getId());
        params.addValue("content", entity.getContent());

        return params;
    }

}
