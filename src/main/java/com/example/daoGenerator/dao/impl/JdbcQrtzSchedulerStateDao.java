package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.QrtzSchedulerStateDao;
import com.example.pojogenerator.pojos.QrtzSchedulerState;
import com.example.daoGenerator.dao.mapper.QrtzSchedulerStateRowMapper;
import com.example.daoGenerator.dao.sql.QrtzSchedulerStateSql;

/**
 * JDBC implementation of QrtzSchedulerStateDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcQrtzSchedulerStateDao implements QrtzSchedulerStateDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final QrtzSchedulerStateRowMapper rowMapper;

    public JdbcQrtzSchedulerStateDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new QrtzSchedulerStateRowMapper();
    }

    @Override
    public Optional<QrtzSchedulerState> findById(Long id) {
        try {
            QrtzSchedulerState result = jdbcTemplate.queryForObject(
                QrtzSchedulerStateSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<QrtzSchedulerState> findAll() {
        return jdbcTemplate.query(QrtzSchedulerStateSql.FIND_ALL, rowMapper);
    }

    @Override
    public QrtzSchedulerState save(QrtzSchedulerState entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private QrtzSchedulerState insert(QrtzSchedulerState entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            QrtzSchedulerStateSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private QrtzSchedulerState update(QrtzSchedulerState entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            QrtzSchedulerStateSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(QrtzSchedulerState entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("checkinInterval", entity.getCheckinInterval());
        params.addValue("lastCheckinTime", entity.getLastCheckinTime());
        params.addValue("instanceName", entity.getInstanceName());
        params.addValue("schedName", entity.getSchedName());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(QrtzSchedulerStateSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            QrtzSchedulerStateSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(QrtzSchedulerStateSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

}
