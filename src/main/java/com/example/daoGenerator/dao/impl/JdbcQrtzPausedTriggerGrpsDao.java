package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.QrtzPausedTriggerGrpsDao;
import com.example.pojogenerator.pojos.QrtzPausedTriggerGrps;
import com.example.daoGenerator.dao.mapper.QrtzPausedTriggerGrpsRowMapper;
import com.example.daoGenerator.dao.sql.QrtzPausedTriggerGrpsSql;

/**
 * Enhanced JDBC implementation of QrtzPausedTriggerGrpsDao
 * Key Type: COMPOSITE_KEY
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcQrtzPausedTriggerGrpsDao implements QrtzPausedTriggerGrpsDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final QrtzPausedTriggerGrpsRowMapper rowMapper;

    public JdbcQrtzPausedTriggerGrpsDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new QrtzPausedTriggerGrpsRowMapper();
    }

    @Override
    public Optional<QrtzPausedTriggerGrps> findBySchedNameAndTriggerGroup(String schedName, String triggerGroup) {
        try {
            QrtzPausedTriggerGrps result = jdbcTemplate.queryForObject(
                QrtzPausedTriggerGrpsSql.FIND_BY_SCHED_NAME_AND_TRIGGER_GROUP,
                Map.of("schedName", schedName, "triggerGroup", triggerGroup),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteBySchedNameAndTriggerGroup(String schedName, String triggerGroup) {
        jdbcTemplate.update(QrtzPausedTriggerGrpsSql.DELETE_BY_SCHED_NAME_AND_TRIGGER_GROUP, Map.of("schedName", schedName, "triggerGroup", triggerGroup));
    }

    @Override
    public boolean existsBySchedNameAndTriggerGroup(String schedName, String triggerGroup) {
        Integer count = jdbcTemplate.queryForObject(
            QrtzPausedTriggerGrpsSql.EXISTS_BY_SCHED_NAME_AND_TRIGGER_GROUP,
            Map.of("schedName", schedName, "triggerGroup", triggerGroup),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<QrtzPausedTriggerGrps> findAll() {
        return jdbcTemplate.query(QrtzPausedTriggerGrpsSql.FIND_ALL, rowMapper);
    }

    @Override
    public QrtzPausedTriggerGrps save(QrtzPausedTriggerGrps entity) {
        if (existsBySchedNameAndTriggerGroup(entity.getSchedName(), entity.getTriggerGroup())) {
            return update(entity);
        } else {
            return insert(entity);
        }
    }

    private QrtzPausedTriggerGrps insert(QrtzPausedTriggerGrps entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();

        MapSqlParameterSource params = createParameterMap(entity);
        jdbcTemplate.update(QrtzPausedTriggerGrpsSql.INSERT, params);
        return entity;
    }

    private QrtzPausedTriggerGrps update(QrtzPausedTriggerGrps entity) {
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(QrtzPausedTriggerGrpsSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(QrtzPausedTriggerGrpsSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    private MapSqlParameterSource createParameterMap(QrtzPausedTriggerGrps entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("triggerGroup", entity.getTriggerGroup());
        params.addValue("schedName", entity.getSchedName());

        return params;
    }

}
