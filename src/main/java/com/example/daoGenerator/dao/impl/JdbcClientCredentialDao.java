package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ClientCredentialDao;
import com.example.pojogenerator.pojos.ClientCredential;
import com.example.daoGenerator.dao.mapper.ClientCredentialRowMapper;
import com.example.daoGenerator.dao.sql.ClientCredentialSql;

/**
 * Enhanced JDBC implementation of ClientCredentialDao
 * Key Type: CUSTOM_SINGLE_KEY
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcClientCredentialDao implements ClientCredentialDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ClientCredentialRowMapper rowMapper;

    public JdbcClientCredentialDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ClientCredentialRowMapper();
    }

    @Override
    public Optional<ClientCredential> findById(String id) {
        try {
            ClientCredential result = jdbcTemplate.queryForObject(
                ClientCredentialSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(String id) {
        jdbcTemplate.update(ClientCredentialSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(String id) {
        Integer count = jdbcTemplate.queryForObject(
            ClientCredentialSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<ClientCredential> findAll() {
        return jdbcTemplate.query(ClientCredentialSql.FIND_ALL, rowMapper);
    }

    @Override
    public ClientCredential save(ClientCredential entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ClientCredential insert(ClientCredential entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();

        MapSqlParameterSource params = createParameterMap(entity);
        jdbcTemplate.update(ClientCredentialSql.INSERT, params);
        return entity;
    }

    private ClientCredential update(ClientCredential entity) {
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(ClientCredentialSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ClientCredentialSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    private MapSqlParameterSource createParameterMap(ClientCredential entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("archived", entity.getArchived());
        params.addValue("clientName", entity.getClientName());
        params.addValue("clientDescription", entity.getClientDescription());
        params.addValue("clientSecret", entity.getClientSecret());
        params.addValue("id", entity.getId());

        return params;
    }

}
