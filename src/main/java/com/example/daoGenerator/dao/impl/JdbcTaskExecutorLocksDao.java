package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.TaskExecutorLocksDao;
import com.example.pojogenerator.pojos.TaskExecutorLocks;
import com.example.daoGenerator.dao.mapper.TaskExecutorLocksRowMapper;
import com.example.daoGenerator.dao.sql.TaskExecutorLocksSql;

/**
 * JDBC implementation of TaskExecutorLocksDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcTaskExecutorLocksDao implements TaskExecutorLocksDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final TaskExecutorLocksRowMapper rowMapper;

    public JdbcTaskExecutorLocksDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new TaskExecutorLocksRowMapper();
    }

    @Override
    public Optional<TaskExecutorLocks> findById(Long id) {
        try {
            TaskExecutorLocks result = jdbcTemplate.queryForObject(
                TaskExecutorLocksSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<TaskExecutorLocks> findAll() {
        return jdbcTemplate.query(TaskExecutorLocksSql.FIND_ALL, rowMapper);
    }

    @Override
    public TaskExecutorLocks save(TaskExecutorLocks entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private TaskExecutorLocks insert(TaskExecutorLocks entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            TaskExecutorLocksSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private TaskExecutorLocks update(TaskExecutorLocks entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            TaskExecutorLocksSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(TaskExecutorLocks entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("tasksId", entity.getTasksId());
        params.addValue("referencedTasksId", entity.getReferencedTasksId());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("lockType", entity.getLockType());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(TaskExecutorLocksSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            TaskExecutorLocksSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(TaskExecutorLocksSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<TaskExecutorLocks> findByTasksId(Long tasksId) {
        return jdbcTemplate.query(
            TaskExecutorLocksSql.FIND_BY_TASKS_ID,
            Map.of("tasksId", tasksId),
            rowMapper
        );
    }

    @Override
    public List<TaskExecutorLocks> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            TaskExecutorLocksSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<TaskExecutorLocks> findByReferencedTasksId(Long referencedTasksId) {
        return jdbcTemplate.query(
            TaskExecutorLocksSql.FIND_BY_REFERENCED_TASKS_ID,
            Map.of("referencedTasksId", referencedTasksId),
            rowMapper
        );
    }

    @Override
    public List<TaskExecutorLocks> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            TaskExecutorLocksSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

}
