package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.EffectsDao;
import com.example.pojogenerator.pojos.Effects;
import com.example.daoGenerator.dao.mapper.EffectsRowMapper;
import com.example.daoGenerator.dao.sql.EffectsSql;

/**
 * JDBC implementation of EffectsDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcEffectsDao implements EffectsDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final EffectsRowMapper rowMapper;

    public JdbcEffectsDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new EffectsRowMapper();
    }

    @Override
    public Optional<Effects> findById(Long id) {
        try {
            Effects result = jdbcTemplate.queryForObject(
                EffectsSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<Effects> findAll() {
        return jdbcTemplate.query(EffectsSql.FIND_ALL, rowMapper);
    }

    @Override
    public Effects save(Effects entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private Effects insert(Effects entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            EffectsSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private Effects update(Effects entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            EffectsSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(Effects entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        if (entity.getApiPayload() != null) {
            params.addValue("apiPayload", entity.getApiPayload().toString());
        } else {
            params.addValue("apiPayload", null);
        }
        params.addValue("orderTree", entity.getOrderTree());
        params.addValue("modifiedAt", entity.getModifiedAt());
        if (entity.getQuery() != null) {
            params.addValue("query", entity.getQuery().toString());
        } else {
            params.addValue("query", null);
        }
        params.addValue("description", entity.getDescription());
        params.addValue("actionsId", entity.getActionsId());
        params.addValue("type", entity.getType());
        params.addValue("apiMethod", entity.getApiMethod());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("archived", entity.getArchived());
        if (entity.getApiEndpoint() != null) {
            params.addValue("apiEndpoint", entity.getApiEndpoint().toString());
        } else {
            params.addValue("apiEndpoint", null);
        }
        if (entity.getApiHeaders() != null) {
            params.addValue("apiHeaders", entity.getApiHeaders().toString());
        } else {
            params.addValue("apiHeaders", null);
        }
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("name", entity.getName());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("javascriptEnabled", entity.getJavascriptEnabled());
        params.addValue("id", entity.getId());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(EffectsSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            EffectsSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(EffectsSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<Effects> findByActionsId(Long actionsId) {
        return jdbcTemplate.query(
            EffectsSql.FIND_BY_ACTIONS_ID,
            Map.of("actionsId", actionsId),
            rowMapper
        );
    }

    @Override
    public List<Effects> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            EffectsSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<Effects> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            EffectsSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

}
