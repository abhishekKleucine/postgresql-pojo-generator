package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ProcessPermissionsDao;
import com.example.pojogenerator.pojos.ProcessPermissions;
import com.example.daoGenerator.dao.mapper.ProcessPermissionsRowMapper;
import com.example.daoGenerator.dao.sql.ProcessPermissionsSql;

/**
 * JDBC implementation of ProcessPermissionsDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcProcessPermissionsDao implements ProcessPermissionsDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ProcessPermissionsRowMapper rowMapper;

    public JdbcProcessPermissionsDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ProcessPermissionsRowMapper();
    }

    @Override
    public Optional<ProcessPermissions> findById(Long id) {
        try {
            ProcessPermissions result = jdbcTemplate.queryForObject(
                ProcessPermissionsSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<ProcessPermissions> findAll() {
        return jdbcTemplate.query(ProcessPermissionsSql.FIND_ALL, rowMapper);
    }

    @Override
    public ProcessPermissions save(ProcessPermissions entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ProcessPermissions insert(ProcessPermissions entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ProcessPermissionsSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ProcessPermissions update(ProcessPermissions entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            ProcessPermissionsSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(ProcessPermissions entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("description", entity.getDescription());
        params.addValue("id", entity.getId());
        params.addValue("type", entity.getType());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ProcessPermissionsSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ProcessPermissionsSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ProcessPermissionsSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

}
