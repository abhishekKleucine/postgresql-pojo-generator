package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ChecklistAuditDao;
import com.example.pojogenerator.pojos.ChecklistAudit;
import com.example.daoGenerator.dao.mapper.ChecklistAuditRowMapper;
import com.example.daoGenerator.dao.sql.ChecklistAuditSql;

/**
 * Enhanced JDBC implementation of ChecklistAuditDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcChecklistAuditDao implements ChecklistAuditDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ChecklistAuditRowMapper rowMapper;

    public JdbcChecklistAuditDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ChecklistAuditRowMapper();
    }

    @Override
    public Optional<ChecklistAudit> findById(Long id) {
        try {
            ChecklistAudit result = jdbcTemplate.queryForObject(
                ChecklistAuditSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ChecklistAuditSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ChecklistAuditSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<ChecklistAudit> findAll() {
        return jdbcTemplate.query(ChecklistAuditSql.FIND_ALL, rowMapper);
    }

    @Override
    public ChecklistAudit save(ChecklistAudit entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ChecklistAudit insert(ChecklistAudit entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ChecklistAuditSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ChecklistAudit update(ChecklistAudit entity) {
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(ChecklistAuditSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ChecklistAuditSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    private MapSqlParameterSource createParameterMap(ChecklistAudit entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("triggeredFor", entity.getTriggeredFor());
        params.addValue("organisationsId", entity.getOrganisationsId());
        params.addValue("tasksId", entity.getTasksId());
        params.addValue("action", entity.getAction());
        params.addValue("checklistsId", entity.getChecklistsId());
        params.addValue("details", entity.getDetails());
        params.addValue("id", entity.getId());
        params.addValue("triggeredAt", entity.getTriggeredAt());
        params.addValue("stagesId", entity.getStagesId());
        params.addValue("triggeredBy", entity.getTriggeredBy());

        return params;
    }

}
