package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.UserAuditDao;
import com.example.pojogenerator.pojos.UserAudit;
import com.example.daoGenerator.dao.mapper.UserAuditRowMapper;
import com.example.daoGenerator.dao.sql.UserAuditSql;

/**
 * Enhanced JDBC implementation of UserAuditDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcUserAuditDao implements UserAuditDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final UserAuditRowMapper rowMapper;

    public JdbcUserAuditDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new UserAuditRowMapper();
    }

    @Override
    public Optional<UserAudit> findById(Long id) {
        try {
            UserAudit result = jdbcTemplate.queryForObject(
                UserAuditSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(UserAuditSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            UserAuditSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<UserAudit> findAll() {
        return jdbcTemplate.query(UserAuditSql.FIND_ALL, rowMapper);
    }

    @Override
    public UserAudit save(UserAudit entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private UserAudit insert(UserAudit entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            UserAuditSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private UserAudit update(UserAudit entity) {
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(UserAuditSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(UserAuditSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    private MapSqlParameterSource createParameterMap(UserAudit entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("severity", entity.getSeverity());
        params.addValue("organisationsId", entity.getOrganisationsId());
        params.addValue("action", entity.getAction());
        params.addValue("details", entity.getDetails());
        params.addValue("id", entity.getId());
        params.addValue("triggeredAt", entity.getTriggeredAt());
        params.addValue("facilityIds", entity.getFacilityIds());
        params.addValue("triggeredBy", entity.getTriggeredBy());

        return params;
    }

}
