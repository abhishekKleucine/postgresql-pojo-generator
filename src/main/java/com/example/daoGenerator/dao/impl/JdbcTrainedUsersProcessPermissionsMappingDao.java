package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.TrainedUsersProcessPermissionsMappingDao;
import com.example.pojogenerator.pojos.TrainedUsersProcessPermissionsMapping;
import com.example.daoGenerator.dao.mapper.TrainedUsersProcessPermissionsMappingRowMapper;
import com.example.daoGenerator.dao.sql.TrainedUsersProcessPermissionsMappingSql;

/**
 * Enhanced JDBC implementation of TrainedUsersProcessPermissionsMappingDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcTrainedUsersProcessPermissionsMappingDao implements TrainedUsersProcessPermissionsMappingDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final TrainedUsersProcessPermissionsMappingRowMapper rowMapper;

    public JdbcTrainedUsersProcessPermissionsMappingDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new TrainedUsersProcessPermissionsMappingRowMapper();
    }

    @Override
    public Optional<TrainedUsersProcessPermissionsMapping> findById(Long id) {
        try {
            TrainedUsersProcessPermissionsMapping result = jdbcTemplate.queryForObject(
                TrainedUsersProcessPermissionsMappingSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(TrainedUsersProcessPermissionsMappingSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            TrainedUsersProcessPermissionsMappingSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<TrainedUsersProcessPermissionsMapping> findAll() {
        return jdbcTemplate.query(TrainedUsersProcessPermissionsMappingSql.FIND_ALL, rowMapper);
    }

    @Override
    public TrainedUsersProcessPermissionsMapping save(TrainedUsersProcessPermissionsMapping entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private TrainedUsersProcessPermissionsMapping insert(TrainedUsersProcessPermissionsMapping entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            TrainedUsersProcessPermissionsMappingSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private TrainedUsersProcessPermissionsMapping update(TrainedUsersProcessPermissionsMapping entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(TrainedUsersProcessPermissionsMappingSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(TrainedUsersProcessPermissionsMappingSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<TrainedUsersProcessPermissionsMapping> findByProcessPermissionsId(Long processPermissionsId) {
        return jdbcTemplate.query(
            TrainedUsersProcessPermissionsMappingSql.FIND_BY_PROCESS_PERMISSIONS_ID,
            Map.of("processPermissionsId", processPermissionsId),
            rowMapper
        );
    }

    @Override
    public List<TrainedUsersProcessPermissionsMapping> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            TrainedUsersProcessPermissionsMappingSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<TrainedUsersProcessPermissionsMapping> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            TrainedUsersProcessPermissionsMappingSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    @Override
    public List<TrainedUsersProcessPermissionsMapping> findByTrainedUsersId(Long trainedUsersId) {
        return jdbcTemplate.query(
            TrainedUsersProcessPermissionsMappingSql.FIND_BY_TRAINED_USERS_ID,
            Map.of("trainedUsersId", trainedUsersId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(TrainedUsersProcessPermissionsMapping entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("trainedUsersId", entity.getTrainedUsersId());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("processPermissionsId", entity.getProcessPermissionsId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());

        return params;
    }

}
