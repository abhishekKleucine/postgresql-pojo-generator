package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.TempParameterVerificationsDao;
import com.example.pojogenerator.pojos.TempParameterVerifications;
import com.example.daoGenerator.dao.mapper.TempParameterVerificationsRowMapper;
import com.example.daoGenerator.dao.sql.TempParameterVerificationsSql;

/**
 * JDBC implementation of TempParameterVerificationsDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcTempParameterVerificationsDao implements TempParameterVerificationsDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final TempParameterVerificationsRowMapper rowMapper;

    public JdbcTempParameterVerificationsDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new TempParameterVerificationsRowMapper();
    }

    @Override
    public Optional<TempParameterVerifications> findById(Long id) {
        try {
            TempParameterVerifications result = jdbcTemplate.queryForObject(
                TempParameterVerificationsSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<TempParameterVerifications> findAll() {
        return jdbcTemplate.query(TempParameterVerificationsSql.FIND_ALL, rowMapper);
    }

    @Override
    public TempParameterVerifications save(TempParameterVerifications entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private TempParameterVerifications insert(TempParameterVerifications entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            TempParameterVerificationsSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private TempParameterVerifications update(TempParameterVerifications entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            TempParameterVerificationsSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(TempParameterVerifications entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("comments", entity.getComments());
        params.addValue("verificationStatus", entity.getVerificationStatus());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("tempParameterValuesId", entity.getTempParameterValuesId());
        params.addValue("userGroupsId", entity.getUserGroupsId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("usersId", entity.getUsersId());
        params.addValue("verificationType", entity.getVerificationType());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("jobsId", entity.getJobsId());
        params.addValue("isBulk", entity.getIsBulk());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(TempParameterVerificationsSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            TempParameterVerificationsSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(TempParameterVerificationsSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<TempParameterVerifications> findByUserGroupsId(Long userGroupsId) {
        return jdbcTemplate.query(
            TempParameterVerificationsSql.FIND_BY_USER_GROUPS_ID,
            Map.of("userGroupsId", userGroupsId),
            rowMapper
        );
    }

    @Override
    public List<TempParameterVerifications> findByTempParameterValuesId(Long tempParameterValuesId) {
        return jdbcTemplate.query(
            TempParameterVerificationsSql.FIND_BY_TEMP_PARAMETER_VALUES_ID,
            Map.of("tempParameterValuesId", tempParameterValuesId),
            rowMapper
        );
    }

    @Override
    public List<TempParameterVerifications> findByJobsId(Long jobsId) {
        return jdbcTemplate.query(
            TempParameterVerificationsSql.FIND_BY_JOBS_ID,
            Map.of("jobsId", jobsId),
            rowMapper
        );
    }

}
