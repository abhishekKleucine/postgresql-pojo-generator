package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.QrtzTriggerDao;
import com.example.pojogenerator.pojos.QrtzTrigger;
import com.example.daoGenerator.dao.mapper.QrtzTriggerRowMapper;
import com.example.daoGenerator.dao.sql.QrtzTriggerSql;

/**
 * Enhanced JDBC implementation of QrtzTriggerDao
 * Key Type: COMPOSITE_KEY
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcQrtzTriggerDao implements QrtzTriggerDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final QrtzTriggerRowMapper rowMapper;

    public JdbcQrtzTriggerDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new QrtzTriggerRowMapper();
    }

    @Override
    public Optional<QrtzTrigger> findByTriggerNameAndSchedNameAndTriggerGroup(String triggerName, String schedName, String triggerGroup) {
        try {
            QrtzTrigger result = jdbcTemplate.queryForObject(
                QrtzTriggerSql.FIND_BY_TRIGGER_NAME_AND_SCHED_NAME_AND_TRIGGER_GROUP,
                Map.of("triggerName", triggerName, "schedName", schedName, "triggerGroup", triggerGroup),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteByTriggerNameAndSchedNameAndTriggerGroup(String triggerName, String schedName, String triggerGroup) {
        jdbcTemplate.update(QrtzTriggerSql.DELETE_BY_TRIGGER_NAME_AND_SCHED_NAME_AND_TRIGGER_GROUP, Map.of("triggerName", triggerName, "schedName", schedName, "triggerGroup", triggerGroup));
    }

    @Override
    public boolean existsByTriggerNameAndSchedNameAndTriggerGroup(String triggerName, String schedName, String triggerGroup) {
        Integer count = jdbcTemplate.queryForObject(
            QrtzTriggerSql.EXISTS_BY_TRIGGER_NAME_AND_SCHED_NAME_AND_TRIGGER_GROUP,
            Map.of("triggerName", triggerName, "schedName", schedName, "triggerGroup", triggerGroup),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<QrtzTrigger> findAll() {
        return jdbcTemplate.query(QrtzTriggerSql.FIND_ALL, rowMapper);
    }

    @Override
    public QrtzTrigger save(QrtzTrigger entity) {
        if (existsByTriggerNameAndSchedNameAndTriggerGroup(entity.getTriggerName(), entity.getSchedName(), entity.getTriggerGroup())) {
            return update(entity);
        } else {
            return insert(entity);
        }
    }

    private QrtzTrigger insert(QrtzTrigger entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();

        MapSqlParameterSource params = createParameterMap(entity);
        jdbcTemplate.update(QrtzTriggerSql.INSERT, params);
        return entity;
    }

    private QrtzTrigger update(QrtzTrigger entity) {
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(QrtzTriggerSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(QrtzTriggerSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<QrtzTrigger> findBySchedName(Long schedName) {
        return jdbcTemplate.query(
            QrtzTriggerSql.FIND_BY_SCHED_NAME,
            Map.of("schedName", schedName),
            rowMapper
        );
    }

    @Override
    public List<QrtzTrigger> findByJobName(Long jobName) {
        return jdbcTemplate.query(
            QrtzTriggerSql.FIND_BY_JOB_NAME,
            Map.of("jobName", jobName),
            rowMapper
        );
    }

    @Override
    public List<QrtzTrigger> findByJobGroup(Long jobGroup) {
        return jdbcTemplate.query(
            QrtzTriggerSql.FIND_BY_JOB_GROUP,
            Map.of("jobGroup", jobGroup),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(QrtzTrigger entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("jobName", entity.getJobName());
        params.addValue("prevFireTime", entity.getPrevFireTime());
        params.addValue("triggerState", entity.getTriggerState());
        params.addValue("jobData", entity.getJobData());
        params.addValue("triggerName", entity.getTriggerName());
        params.addValue("nextFireTime", entity.getNextFireTime());
        params.addValue("description", entity.getDescription());
        params.addValue("jobGroup", entity.getJobGroup());
        params.addValue("priority", entity.getPriority());
        params.addValue("calendarName", entity.getCalendarName());
        params.addValue("triggerGroup", entity.getTriggerGroup());
        params.addValue("misfireInstr", entity.getMisfireInstr());
        params.addValue("startTime", entity.getStartTime());
        params.addValue("schedName", entity.getSchedName());
        params.addValue("triggerType", entity.getTriggerType());
        params.addValue("endTime", entity.getEndTime());

        return params;
    }

}
