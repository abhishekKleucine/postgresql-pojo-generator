package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.VariationMediaMappingDao;
import com.example.pojogenerator.pojos.VariationMediaMapping;
import com.example.daoGenerator.dao.mapper.VariationMediaMappingRowMapper;
import com.example.daoGenerator.dao.sql.VariationMediaMappingSql;

/**
 * Enhanced JDBC implementation of VariationMediaMappingDao
 * Key Type: COMPOSITE_KEY
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcVariationMediaMappingDao implements VariationMediaMappingDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final VariationMediaMappingRowMapper rowMapper;

    public JdbcVariationMediaMappingDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new VariationMediaMappingRowMapper();
    }

    @Override
    public Optional<VariationMediaMapping> findByVariationsIdAndMediasId(Long variationsId, Long mediasId) {
        try {
            VariationMediaMapping result = jdbcTemplate.queryForObject(
                VariationMediaMappingSql.FIND_BY_VARIATIONS_ID_AND_MEDIAS_ID,
                Map.of("variationsId", variationsId, "mediasId", mediasId),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteByVariationsIdAndMediasId(Long variationsId, Long mediasId) {
        jdbcTemplate.update(VariationMediaMappingSql.DELETE_BY_VARIATIONS_ID_AND_MEDIAS_ID, Map.of("variationsId", variationsId, "mediasId", mediasId));
    }

    @Override
    public boolean existsByVariationsIdAndMediasId(Long variationsId, Long mediasId) {
        Integer count = jdbcTemplate.queryForObject(
            VariationMediaMappingSql.EXISTS_BY_VARIATIONS_ID_AND_MEDIAS_ID,
            Map.of("variationsId", variationsId, "mediasId", mediasId),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<VariationMediaMapping> findAll() {
        return jdbcTemplate.query(VariationMediaMappingSql.FIND_ALL, rowMapper);
    }

    @Override
    public VariationMediaMapping save(VariationMediaMapping entity) {
        if (existsByVariationsIdAndMediasId(entity.getVariationsId(), entity.getMediasId())) {
            return update(entity);
        } else {
            return insert(entity);
        }
    }

    private VariationMediaMapping insert(VariationMediaMapping entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        jdbcTemplate.update(VariationMediaMappingSql.INSERT, params);
        return entity;
    }

    private VariationMediaMapping update(VariationMediaMapping entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(VariationMediaMappingSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(VariationMediaMappingSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<VariationMediaMapping> findByVariationsId(Long variationsId) {
        return jdbcTemplate.query(
            VariationMediaMappingSql.FIND_BY_VARIATIONS_ID,
            Map.of("variationsId", variationsId),
            rowMapper
        );
    }

    @Override
    public List<VariationMediaMapping> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            VariationMediaMappingSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<VariationMediaMapping> findByMediasId(Long mediasId) {
        return jdbcTemplate.query(
            VariationMediaMappingSql.FIND_BY_MEDIAS_ID,
            Map.of("mediasId", mediasId),
            rowMapper
        );
    }

    @Override
    public List<VariationMediaMapping> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            VariationMediaMappingSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(VariationMediaMapping entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("archived", entity.getArchived());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("mediasId", entity.getMediasId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("variationsId", entity.getVariationsId());
        params.addValue("modifiedBy", entity.getModifiedBy());

        return params;
    }

}
