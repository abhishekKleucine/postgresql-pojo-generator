package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.UsersDao;
import com.example.pojogenerator.pojos.Users;
import com.example.daoGenerator.dao.mapper.UsersRowMapper;
import com.example.daoGenerator.dao.sql.UsersSql;

/**
 * Enhanced JDBC implementation of UsersDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcUsersDao implements UsersDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final UsersRowMapper rowMapper;

    public JdbcUsersDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new UsersRowMapper();
    }

    @Override
    public Optional<Users> findById(Long id) {
        try {
            Users result = jdbcTemplate.queryForObject(
                UsersSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(UsersSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            UsersSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<Users> findAll() {
        return jdbcTemplate.query(UsersSql.FIND_ALL, rowMapper);
    }

    @Override
    public Users save(Users entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private Users insert(Users entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            UsersSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private Users update(Users entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(UsersSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(UsersSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<Users> findByChallengeQuestionsId(Long challengeQuestionsId) {
        return jdbcTemplate.query(
            UsersSql.FIND_BY_CHALLENGE_QUESTIONS_ID,
            Map.of("challengeQuestionsId", challengeQuestionsId),
            rowMapper
        );
    }

    @Override
    public List<Users> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            UsersSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<Users> findByOrganisationsId(Long organisationsId) {
        return jdbcTemplate.query(
            UsersSql.FIND_BY_ORGANISATIONS_ID,
            Map.of("organisationsId", organisationsId),
            rowMapper
        );
    }

    @Override
    public List<Users> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            UsersSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(Users entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("lastName", entity.getLastName());
        params.addValue("organisationsId", entity.getOrganisationsId());
        params.addValue("failedAdditionalVerificationAttempts", entity.getFailedAdditionalVerificationAttempts());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("failedChallengeQuestionAttempts", entity.getFailedChallengeQuestionAttempts());
        params.addValue("lockedAt", entity.getLockedAt());
        params.addValue("employeeId", entity.getEmployeeId());
        params.addValue("failedLoginAttempts", entity.getFailedLoginAttempts());
        params.addValue("type", entity.getType());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("firstName", entity.getFirstName());
        params.addValue("archived", entity.getArchived());
        params.addValue("passwordUpdatedAt", entity.getPasswordUpdatedAt());
        params.addValue("challengeQuestionsAnswer", entity.getChallengeQuestionsAnswer());
        params.addValue("password", entity.getPassword());
        params.addValue("challengeQuestionsId", entity.getChallengeQuestionsId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("isSystemUser", entity.getIsSystemUser());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("state", entity.getState());
        params.addValue("department", entity.getDepartment());
        params.addValue("email", entity.getEmail());
        params.addValue("username", entity.getUsername());

        return params;
    }

}
