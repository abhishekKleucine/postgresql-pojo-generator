package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.UserGroupMembersDao;
import com.example.pojogenerator.pojos.UserGroupMembers;
import com.example.daoGenerator.dao.mapper.UserGroupMembersRowMapper;
import com.example.daoGenerator.dao.sql.UserGroupMembersSql;

/**
 * Enhanced JDBC implementation of UserGroupMembersDao
 * Key Type: COMPOSITE_KEY
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcUserGroupMembersDao implements UserGroupMembersDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final UserGroupMembersRowMapper rowMapper;

    public JdbcUserGroupMembersDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new UserGroupMembersRowMapper();
    }

    @Override
    public Optional<UserGroupMembers> findByUsersIdAndGroupsId(Long usersId, Long groupsId) {
        try {
            UserGroupMembers result = jdbcTemplate.queryForObject(
                UserGroupMembersSql.FIND_BY_USERS_ID_AND_GROUPS_ID,
                Map.of("usersId", usersId, "groupsId", groupsId),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteByUsersIdAndGroupsId(Long usersId, Long groupsId) {
        jdbcTemplate.update(UserGroupMembersSql.DELETE_BY_USERS_ID_AND_GROUPS_ID, Map.of("usersId", usersId, "groupsId", groupsId));
    }

    @Override
    public boolean existsByUsersIdAndGroupsId(Long usersId, Long groupsId) {
        Integer count = jdbcTemplate.queryForObject(
            UserGroupMembersSql.EXISTS_BY_USERS_ID_AND_GROUPS_ID,
            Map.of("usersId", usersId, "groupsId", groupsId),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<UserGroupMembers> findAll() {
        return jdbcTemplate.query(UserGroupMembersSql.FIND_ALL, rowMapper);
    }

    @Override
    public UserGroupMembers save(UserGroupMembers entity) {
        if (existsByUsersIdAndGroupsId(entity.getUsersId(), entity.getGroupsId())) {
            return update(entity);
        } else {
            return insert(entity);
        }
    }

    private UserGroupMembers insert(UserGroupMembers entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        jdbcTemplate.update(UserGroupMembersSql.INSERT, params);
        return entity;
    }

    private UserGroupMembers update(UserGroupMembers entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(UserGroupMembersSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(UserGroupMembersSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<UserGroupMembers> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            UserGroupMembersSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<UserGroupMembers> findByUsersId(Long usersId) {
        return jdbcTemplate.query(
            UserGroupMembersSql.FIND_BY_USERS_ID,
            Map.of("usersId", usersId),
            rowMapper
        );
    }

    @Override
    public List<UserGroupMembers> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            UserGroupMembersSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    @Override
    public List<UserGroupMembers> findByGroupsId(Long groupsId) {
        return jdbcTemplate.query(
            UserGroupMembersSql.FIND_BY_GROUPS_ID,
            Map.of("groupsId", groupsId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(UserGroupMembers entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("groupsId", entity.getGroupsId());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("usersId", entity.getUsersId());
        params.addValue("modifiedBy", entity.getModifiedBy());

        return params;
    }

}
