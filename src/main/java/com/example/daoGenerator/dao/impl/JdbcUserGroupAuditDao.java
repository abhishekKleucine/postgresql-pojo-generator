package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.UserGroupAuditDao;
import com.example.pojogenerator.pojos.UserGroupAudit;
import com.example.daoGenerator.dao.mapper.UserGroupAuditRowMapper;
import com.example.daoGenerator.dao.sql.UserGroupAuditSql;

/**
 * Enhanced JDBC implementation of UserGroupAuditDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcUserGroupAuditDao implements UserGroupAuditDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final UserGroupAuditRowMapper rowMapper;

    public JdbcUserGroupAuditDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new UserGroupAuditRowMapper();
    }

    @Override
    public Optional<UserGroupAudit> findById(Long id) {
        try {
            UserGroupAudit result = jdbcTemplate.queryForObject(
                UserGroupAuditSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(UserGroupAuditSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            UserGroupAuditSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<UserGroupAudit> findAll() {
        return jdbcTemplate.query(UserGroupAuditSql.FIND_ALL, rowMapper);
    }

    @Override
    public UserGroupAudit save(UserGroupAudit entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private UserGroupAudit insert(UserGroupAudit entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            UserGroupAuditSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private UserGroupAudit update(UserGroupAudit entity) {
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(UserGroupAuditSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(UserGroupAuditSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<UserGroupAudit> findByUserGroupsId(Long userGroupsId) {
        return jdbcTemplate.query(
            UserGroupAuditSql.FIND_BY_USER_GROUPS_ID,
            Map.of("userGroupsId", userGroupsId),
            rowMapper
        );
    }

    @Override
    public List<UserGroupAudit> findByOrganisationsId(Long organisationsId) {
        return jdbcTemplate.query(
            UserGroupAuditSql.FIND_BY_ORGANISATIONS_ID,
            Map.of("organisationsId", organisationsId),
            rowMapper
        );
    }

    @Override
    public List<UserGroupAudit> findByFacilitiesId(Long facilitiesId) {
        return jdbcTemplate.query(
            UserGroupAuditSql.FIND_BY_FACILITIES_ID,
            Map.of("facilitiesId", facilitiesId),
            rowMapper
        );
    }

    @Override
    public List<UserGroupAudit> findByTriggeredBy(Long triggeredBy) {
        return jdbcTemplate.query(
            UserGroupAuditSql.FIND_BY_TRIGGERED_BY,
            Map.of("triggeredBy", triggeredBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(UserGroupAudit entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("facilitiesId", entity.getFacilitiesId());
        params.addValue("userGroupsId", entity.getUserGroupsId());
        params.addValue("organisationsId", entity.getOrganisationsId());
        params.addValue("details", entity.getDetails());
        params.addValue("id", entity.getId());
        params.addValue("triggeredAt", entity.getTriggeredAt());
        params.addValue("triggeredBy", entity.getTriggeredBy());

        return params;
    }

}
