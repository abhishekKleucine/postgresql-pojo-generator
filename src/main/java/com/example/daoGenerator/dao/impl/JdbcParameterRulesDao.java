package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ParameterRulesDao;
import com.example.pojogenerator.pojos.ParameterRules;
import com.example.daoGenerator.dao.mapper.ParameterRulesRowMapper;
import com.example.daoGenerator.dao.sql.ParameterRulesSql;

/**
 * JDBC implementation of ParameterRulesDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcParameterRulesDao implements ParameterRulesDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ParameterRulesRowMapper rowMapper;

    public JdbcParameterRulesDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ParameterRulesRowMapper();
    }

    @Override
    public Optional<ParameterRules> findById(Long id) {
        try {
            ParameterRules result = jdbcTemplate.queryForObject(
                ParameterRulesSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<ParameterRules> findAll() {
        return jdbcTemplate.query(ParameterRulesSql.FIND_ALL, rowMapper);
    }

    @Override
    public ParameterRules save(ParameterRules entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ParameterRules insert(ParameterRules entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ParameterRulesSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ParameterRules update(ParameterRules entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            ParameterRulesSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(ParameterRules entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("input", entity.getInput());
        params.addValue("visibility", entity.getVisibility());
        params.addValue("id", entity.getId());
        params.addValue("rulesId", entity.getRulesId());
        params.addValue("operator", entity.getOperator());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ParameterRulesSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ParameterRulesSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ParameterRulesSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

}
