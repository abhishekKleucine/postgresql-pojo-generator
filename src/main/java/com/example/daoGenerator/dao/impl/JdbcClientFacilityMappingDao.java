package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ClientFacilityMappingDao;
import com.example.pojogenerator.pojos.ClientFacilityMapping;
import com.example.daoGenerator.dao.mapper.ClientFacilityMappingRowMapper;
import com.example.daoGenerator.dao.sql.ClientFacilityMappingSql;

/**
 * Enhanced JDBC implementation of ClientFacilityMappingDao
 * Key Type: COMPOSITE_KEY
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcClientFacilityMappingDao implements ClientFacilityMappingDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ClientFacilityMappingRowMapper rowMapper;

    public JdbcClientFacilityMappingDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ClientFacilityMappingRowMapper();
    }

    @Override
    public Optional<ClientFacilityMapping> findByFacilitiesIdAndClientId(Long facilitiesId, String clientId) {
        try {
            ClientFacilityMapping result = jdbcTemplate.queryForObject(
                ClientFacilityMappingSql.FIND_BY_FACILITIES_ID_AND_CLIENT_ID,
                Map.of("facilitiesId", facilitiesId, "clientId", clientId),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteByFacilitiesIdAndClientId(Long facilitiesId, String clientId) {
        jdbcTemplate.update(ClientFacilityMappingSql.DELETE_BY_FACILITIES_ID_AND_CLIENT_ID, Map.of("facilitiesId", facilitiesId, "clientId", clientId));
    }

    @Override
    public boolean existsByFacilitiesIdAndClientId(Long facilitiesId, String clientId) {
        Integer count = jdbcTemplate.queryForObject(
            ClientFacilityMappingSql.EXISTS_BY_FACILITIES_ID_AND_CLIENT_ID,
            Map.of("facilitiesId", facilitiesId, "clientId", clientId),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<ClientFacilityMapping> findAll() {
        return jdbcTemplate.query(ClientFacilityMappingSql.FIND_ALL, rowMapper);
    }

    @Override
    public ClientFacilityMapping save(ClientFacilityMapping entity) {
        if (existsByFacilitiesIdAndClientId(entity.getFacilitiesId(), entity.getClientId())) {
            return update(entity);
        } else {
            return insert(entity);
        }
    }

    private ClientFacilityMapping insert(ClientFacilityMapping entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        jdbcTemplate.update(ClientFacilityMappingSql.INSERT, params);
        return entity;
    }

    private ClientFacilityMapping update(ClientFacilityMapping entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(ClientFacilityMappingSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ClientFacilityMappingSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<ClientFacilityMapping> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            ClientFacilityMappingSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<ClientFacilityMapping> findByFacilitiesId(Long facilitiesId) {
        return jdbcTemplate.query(
            ClientFacilityMappingSql.FIND_BY_FACILITIES_ID,
            Map.of("facilitiesId", facilitiesId),
            rowMapper
        );
    }

    @Override
    public List<ClientFacilityMapping> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            ClientFacilityMappingSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    @Override
    public List<ClientFacilityMapping> findByClientId(Long clientId) {
        return jdbcTemplate.query(
            ClientFacilityMappingSql.FIND_BY_CLIENT_ID,
            Map.of("clientId", clientId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(ClientFacilityMapping entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("facilitiesId", entity.getFacilitiesId());
        params.addValue("clientId", entity.getClientId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("modifiedBy", entity.getModifiedBy());

        return params;
    }

}
