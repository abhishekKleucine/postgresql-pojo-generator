package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.QrtzBlobTriggersDao;
import com.example.pojogenerator.pojos.QrtzBlobTriggers;
import com.example.daoGenerator.dao.mapper.QrtzBlobTriggersRowMapper;
import com.example.daoGenerator.dao.sql.QrtzBlobTriggersSql;

/**
 * JDBC implementation of QrtzBlobTriggersDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcQrtzBlobTriggersDao implements QrtzBlobTriggersDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final QrtzBlobTriggersRowMapper rowMapper;

    public JdbcQrtzBlobTriggersDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new QrtzBlobTriggersRowMapper();
    }

    @Override
    public Optional<QrtzBlobTriggers> findById(Long id) {
        try {
            QrtzBlobTriggers result = jdbcTemplate.queryForObject(
                QrtzBlobTriggersSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<QrtzBlobTriggers> findAll() {
        return jdbcTemplate.query(QrtzBlobTriggersSql.FIND_ALL, rowMapper);
    }

    @Override
    public QrtzBlobTriggers save(QrtzBlobTriggers entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private QrtzBlobTriggers insert(QrtzBlobTriggers entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            QrtzBlobTriggersSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private QrtzBlobTriggers update(QrtzBlobTriggers entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            QrtzBlobTriggersSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(QrtzBlobTriggers entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("triggerGroup", entity.getTriggerGroup());
        params.addValue("blobData", entity.getBlobData());
        params.addValue("triggerName", entity.getTriggerName());
        params.addValue("schedName", entity.getSchedName());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(QrtzBlobTriggersSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            QrtzBlobTriggersSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(QrtzBlobTriggersSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<QrtzBlobTriggers> findByTriggerName(Long triggerName) {
        return jdbcTemplate.query(
            QrtzBlobTriggersSql.FIND_BY_TRIGGER_NAME,
            Map.of("triggerName", triggerName),
            rowMapper
        );
    }

    @Override
    public List<QrtzBlobTriggers> findBySchedName(Long schedName) {
        return jdbcTemplate.query(
            QrtzBlobTriggersSql.FIND_BY_SCHED_NAME,
            Map.of("schedName", schedName),
            rowMapper
        );
    }

    @Override
    public List<QrtzBlobTriggers> findByTriggerGroup(Long triggerGroup) {
        return jdbcTemplate.query(
            QrtzBlobTriggersSql.FIND_BY_TRIGGER_GROUP,
            Map.of("triggerGroup", triggerGroup),
            rowMapper
        );
    }

}
