package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.AutoInitializedParameterDao;
import com.example.pojogenerator.pojos.AutoInitializedParameter;
import com.example.daoGenerator.dao.mapper.AutoInitializedParameterRowMapper;
import com.example.daoGenerator.dao.sql.AutoInitializedParameterSql;

/**
 * Enhanced JDBC implementation of AutoInitializedParameterDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcAutoInitializedParameterDao implements AutoInitializedParameterDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final AutoInitializedParameterRowMapper rowMapper;

    public JdbcAutoInitializedParameterDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new AutoInitializedParameterRowMapper();
    }

    @Override
    public Optional<AutoInitializedParameter> findById(Long id) {
        try {
            AutoInitializedParameter result = jdbcTemplate.queryForObject(
                AutoInitializedParameterSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(AutoInitializedParameterSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            AutoInitializedParameterSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<AutoInitializedParameter> findAll() {
        return jdbcTemplate.query(AutoInitializedParameterSql.FIND_ALL, rowMapper);
    }

    @Override
    public AutoInitializedParameter save(AutoInitializedParameter entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private AutoInitializedParameter insert(AutoInitializedParameter entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            AutoInitializedParameterSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private AutoInitializedParameter update(AutoInitializedParameter entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(AutoInitializedParameterSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(AutoInitializedParameterSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<AutoInitializedParameter> findByReferencedParametersId(Long referencedParametersId) {
        return jdbcTemplate.query(
            AutoInitializedParameterSql.FIND_BY_REFERENCED_PARAMETERS_ID,
            Map.of("referencedParametersId", referencedParametersId),
            rowMapper
        );
    }

    @Override
    public List<AutoInitializedParameter> findByChecklistsId(Long checklistsId) {
        return jdbcTemplate.query(
            AutoInitializedParameterSql.FIND_BY_CHECKLISTS_ID,
            Map.of("checklistsId", checklistsId),
            rowMapper
        );
    }

    @Override
    public List<AutoInitializedParameter> findByAutoInitializedParametersId(Long autoInitializedParametersId) {
        return jdbcTemplate.query(
            AutoInitializedParameterSql.FIND_BY_AUTO_INITIALIZED_PARAMETERS_ID,
            Map.of("autoInitializedParametersId", autoInitializedParametersId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(AutoInitializedParameter entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("referencedParametersId", entity.getReferencedParametersId());
        params.addValue("checklistsId", entity.getChecklistsId());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("autoInitializedParametersId", entity.getAutoInitializedParametersId());

        return params;
    }

}
