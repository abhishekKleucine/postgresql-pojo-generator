package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.QrtzTriggersDao;
import com.example.pojogenerator.pojos.QrtzTriggers;
import com.example.daoGenerator.dao.mapper.QrtzTriggersRowMapper;
import com.example.daoGenerator.dao.sql.QrtzTriggersSql;

/**
 * JDBC implementation of QrtzTriggersDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcQrtzTriggersDao implements QrtzTriggersDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final QrtzTriggersRowMapper rowMapper;

    public JdbcQrtzTriggersDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new QrtzTriggersRowMapper();
    }

    @Override
    public Optional<QrtzTriggers> findById(Long id) {
        try {
            QrtzTriggers result = jdbcTemplate.queryForObject(
                QrtzTriggersSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<QrtzTriggers> findAll() {
        return jdbcTemplate.query(QrtzTriggersSql.FIND_ALL, rowMapper);
    }

    @Override
    public QrtzTriggers save(QrtzTriggers entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private QrtzTriggers insert(QrtzTriggers entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            QrtzTriggersSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private QrtzTriggers update(QrtzTriggers entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            QrtzTriggersSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(QrtzTriggers entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("jobName", entity.getJobName());
        params.addValue("prevFireTime", entity.getPrevFireTime());
        params.addValue("triggerState", entity.getTriggerState());
        params.addValue("jobData", entity.getJobData());
        params.addValue("triggerName", entity.getTriggerName());
        params.addValue("nextFireTime", entity.getNextFireTime());
        params.addValue("description", entity.getDescription());
        params.addValue("jobGroup", entity.getJobGroup());
        params.addValue("priority", entity.getPriority());
        params.addValue("calendarName", entity.getCalendarName());
        params.addValue("triggerGroup", entity.getTriggerGroup());
        params.addValue("misfireInstr", entity.getMisfireInstr());
        params.addValue("startTime", entity.getStartTime());
        params.addValue("schedName", entity.getSchedName());
        params.addValue("triggerType", entity.getTriggerType());
        params.addValue("endTime", entity.getEndTime());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(QrtzTriggersSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            QrtzTriggersSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(QrtzTriggersSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<QrtzTriggers> findBySchedName(Long schedName) {
        return jdbcTemplate.query(
            QrtzTriggersSql.FIND_BY_SCHED_NAME,
            Map.of("schedName", schedName),
            rowMapper
        );
    }

    @Override
    public List<QrtzTriggers> findByJobName(Long jobName) {
        return jdbcTemplate.query(
            QrtzTriggersSql.FIND_BY_JOB_NAME,
            Map.of("jobName", jobName),
            rowMapper
        );
    }

    @Override
    public List<QrtzTriggers> findByJobGroup(Long jobGroup) {
        return jdbcTemplate.query(
            QrtzTriggersSql.FIND_BY_JOB_GROUP,
            Map.of("jobGroup", jobGroup),
            rowMapper
        );
    }

}
