package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ExceptionReviewerDao;
import com.example.pojogenerator.pojos.ExceptionReviewer;
import com.example.daoGenerator.dao.mapper.ExceptionReviewerRowMapper;
import com.example.daoGenerator.dao.sql.ExceptionReviewerSql;

/**
 * Enhanced JDBC implementation of ExceptionReviewerDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcExceptionReviewerDao implements ExceptionReviewerDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ExceptionReviewerRowMapper rowMapper;

    public JdbcExceptionReviewerDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ExceptionReviewerRowMapper();
    }

    @Override
    public Optional<ExceptionReviewer> findById(Long id) {
        try {
            ExceptionReviewer result = jdbcTemplate.queryForObject(
                ExceptionReviewerSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ExceptionReviewerSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ExceptionReviewerSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<ExceptionReviewer> findAll() {
        return jdbcTemplate.query(ExceptionReviewerSql.FIND_ALL, rowMapper);
    }

    @Override
    public ExceptionReviewer save(ExceptionReviewer entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ExceptionReviewer insert(ExceptionReviewer entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ExceptionReviewerSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ExceptionReviewer update(ExceptionReviewer entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(ExceptionReviewerSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ExceptionReviewerSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<ExceptionReviewer> findByExceptionsId(Long exceptionsId) {
        return jdbcTemplate.query(
            ExceptionReviewerSql.FIND_BY_EXCEPTIONS_ID,
            Map.of("exceptionsId", exceptionsId),
            rowMapper
        );
    }

    @Override
    public List<ExceptionReviewer> findByUserGroupsId(Long userGroupsId) {
        return jdbcTemplate.query(
            ExceptionReviewerSql.FIND_BY_USER_GROUPS_ID,
            Map.of("userGroupsId", userGroupsId),
            rowMapper
        );
    }

    @Override
    public List<ExceptionReviewer> findByUsersId(Long usersId) {
        return jdbcTemplate.query(
            ExceptionReviewerSql.FIND_BY_USERS_ID,
            Map.of("usersId", usersId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(ExceptionReviewer entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("actionPerformed", entity.getActionPerformed());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("userGroupsId", entity.getUserGroupsId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("usersId", entity.getUsersId());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("exceptionsId", entity.getExceptionsId());

        return params;
    }

}
