package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.QrtzBlobTriggerDao;
import com.example.pojogenerator.pojos.QrtzBlobTrigger;
import com.example.daoGenerator.dao.mapper.QrtzBlobTriggerRowMapper;
import com.example.daoGenerator.dao.sql.QrtzBlobTriggerSql;

/**
 * Enhanced JDBC implementation of QrtzBlobTriggerDao
 * Key Type: COMPOSITE_KEY
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcQrtzBlobTriggerDao implements QrtzBlobTriggerDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final QrtzBlobTriggerRowMapper rowMapper;

    public JdbcQrtzBlobTriggerDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new QrtzBlobTriggerRowMapper();
    }

    @Override
    public Optional<QrtzBlobTrigger> findByTriggerNameAndSchedNameAndTriggerGroup(String triggerName, String schedName, String triggerGroup) {
        try {
            QrtzBlobTrigger result = jdbcTemplate.queryForObject(
                QrtzBlobTriggerSql.FIND_BY_TRIGGER_NAME_AND_SCHED_NAME_AND_TRIGGER_GROUP,
                Map.of("triggerName", triggerName, "schedName", schedName, "triggerGroup", triggerGroup),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteByTriggerNameAndSchedNameAndTriggerGroup(String triggerName, String schedName, String triggerGroup) {
        jdbcTemplate.update(QrtzBlobTriggerSql.DELETE_BY_TRIGGER_NAME_AND_SCHED_NAME_AND_TRIGGER_GROUP, Map.of("triggerName", triggerName, "schedName", schedName, "triggerGroup", triggerGroup));
    }

    @Override
    public boolean existsByTriggerNameAndSchedNameAndTriggerGroup(String triggerName, String schedName, String triggerGroup) {
        Integer count = jdbcTemplate.queryForObject(
            QrtzBlobTriggerSql.EXISTS_BY_TRIGGER_NAME_AND_SCHED_NAME_AND_TRIGGER_GROUP,
            Map.of("triggerName", triggerName, "schedName", schedName, "triggerGroup", triggerGroup),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<QrtzBlobTrigger> findAll() {
        return jdbcTemplate.query(QrtzBlobTriggerSql.FIND_ALL, rowMapper);
    }

    @Override
    public QrtzBlobTrigger save(QrtzBlobTrigger entity) {
        if (existsByTriggerNameAndSchedNameAndTriggerGroup(entity.getTriggerName(), entity.getSchedName(), entity.getTriggerGroup())) {
            return update(entity);
        } else {
            return insert(entity);
        }
    }

    private QrtzBlobTrigger insert(QrtzBlobTrigger entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();

        MapSqlParameterSource params = createParameterMap(entity);
        jdbcTemplate.update(QrtzBlobTriggerSql.INSERT, params);
        return entity;
    }

    private QrtzBlobTrigger update(QrtzBlobTrigger entity) {
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(QrtzBlobTriggerSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(QrtzBlobTriggerSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<QrtzBlobTrigger> findByTriggerName(Long triggerName) {
        return jdbcTemplate.query(
            QrtzBlobTriggerSql.FIND_BY_TRIGGER_NAME,
            Map.of("triggerName", triggerName),
            rowMapper
        );
    }

    @Override
    public List<QrtzBlobTrigger> findBySchedName(Long schedName) {
        return jdbcTemplate.query(
            QrtzBlobTriggerSql.FIND_BY_SCHED_NAME,
            Map.of("schedName", schedName),
            rowMapper
        );
    }

    @Override
    public List<QrtzBlobTrigger> findByTriggerGroup(Long triggerGroup) {
        return jdbcTemplate.query(
            QrtzBlobTriggerSql.FIND_BY_TRIGGER_GROUP,
            Map.of("triggerGroup", triggerGroup),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(QrtzBlobTrigger entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("triggerGroup", entity.getTriggerGroup());
        params.addValue("blobData", entity.getBlobData());
        params.addValue("triggerName", entity.getTriggerName());
        params.addValue("schedName", entity.getSchedName());

        return params;
    }

}
