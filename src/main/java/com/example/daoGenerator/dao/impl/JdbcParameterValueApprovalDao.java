package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ParameterValueApprovalDao;
import com.example.pojogenerator.pojos.ParameterValueApproval;
import com.example.daoGenerator.dao.mapper.ParameterValueApprovalRowMapper;
import com.example.daoGenerator.dao.sql.ParameterValueApprovalSql;

/**
 * Enhanced JDBC implementation of ParameterValueApprovalDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcParameterValueApprovalDao implements ParameterValueApprovalDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ParameterValueApprovalRowMapper rowMapper;

    public JdbcParameterValueApprovalDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ParameterValueApprovalRowMapper();
    }

    @Override
    public Optional<ParameterValueApproval> findById(Long id) {
        try {
            ParameterValueApproval result = jdbcTemplate.queryForObject(
                ParameterValueApprovalSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ParameterValueApprovalSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ParameterValueApprovalSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<ParameterValueApproval> findAll() {
        return jdbcTemplate.query(ParameterValueApprovalSql.FIND_ALL, rowMapper);
    }

    @Override
    public ParameterValueApproval save(ParameterValueApproval entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ParameterValueApproval insert(ParameterValueApproval entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ParameterValueApprovalSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ParameterValueApproval update(ParameterValueApproval entity) {
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(ParameterValueApprovalSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ParameterValueApprovalSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<ParameterValueApproval> findByUsersId(Long usersId) {
        return jdbcTemplate.query(
            ParameterValueApprovalSql.FIND_BY_USERS_ID,
            Map.of("usersId", usersId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(ParameterValueApproval entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("usersId", entity.getUsersId());
        params.addValue("id", entity.getId());
        params.addValue("state", entity.getState());

        return params;
    }

}
