package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.UserGroupsDao;
import com.example.pojogenerator.pojos.UserGroups;
import com.example.daoGenerator.dao.mapper.UserGroupsRowMapper;
import com.example.daoGenerator.dao.sql.UserGroupsSql;

/**
 * JDBC implementation of UserGroupsDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcUserGroupsDao implements UserGroupsDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final UserGroupsRowMapper rowMapper;

    public JdbcUserGroupsDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new UserGroupsRowMapper();
    }

    @Override
    public Optional<UserGroups> findById(Long id) {
        try {
            UserGroups result = jdbcTemplate.queryForObject(
                UserGroupsSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<UserGroups> findAll() {
        return jdbcTemplate.query(UserGroupsSql.FIND_ALL, rowMapper);
    }

    @Override
    public UserGroups save(UserGroups entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private UserGroups insert(UserGroups entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            UserGroupsSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private UserGroups update(UserGroups entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            UserGroupsSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(UserGroups entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("facilityId", entity.getFacilityId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("name", entity.getName());
        params.addValue("description", entity.getDescription());
        params.addValue("active", entity.getActive());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(UserGroupsSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            UserGroupsSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(UserGroupsSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<UserGroups> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            UserGroupsSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<UserGroups> findByFacilityId(Long facilityId) {
        return jdbcTemplate.query(
            UserGroupsSql.FIND_BY_FACILITY_ID,
            Map.of("facilityId", facilityId),
            rowMapper
        );
    }

    @Override
    public List<UserGroups> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            UserGroupsSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

}
