package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.JobAnnotationMediaMappingDao;
import com.example.pojogenerator.pojos.JobAnnotationMediaMapping;
import com.example.daoGenerator.dao.mapper.JobAnnotationMediaMappingRowMapper;
import com.example.daoGenerator.dao.sql.JobAnnotationMediaMappingSql;

/**
 * JDBC implementation of JobAnnotationMediaMappingDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcJobAnnotationMediaMappingDao implements JobAnnotationMediaMappingDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final JobAnnotationMediaMappingRowMapper rowMapper;

    public JdbcJobAnnotationMediaMappingDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new JobAnnotationMediaMappingRowMapper();
    }

    @Override
    public Optional<JobAnnotationMediaMapping> findById(Long id) {
        try {
            JobAnnotationMediaMapping result = jdbcTemplate.queryForObject(
                JobAnnotationMediaMappingSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<JobAnnotationMediaMapping> findAll() {
        return jdbcTemplate.query(JobAnnotationMediaMappingSql.FIND_ALL, rowMapper);
    }

    @Override
    public JobAnnotationMediaMapping save(JobAnnotationMediaMapping entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private JobAnnotationMediaMapping insert(JobAnnotationMediaMapping entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            JobAnnotationMediaMappingSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private JobAnnotationMediaMapping update(JobAnnotationMediaMapping entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            JobAnnotationMediaMappingSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(JobAnnotationMediaMapping entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("archived", entity.getArchived());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("mediasId", entity.getMediasId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("jobAnnotationsId", entity.getJobAnnotationsId());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("jobsId", entity.getJobsId());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(JobAnnotationMediaMappingSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            JobAnnotationMediaMappingSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(JobAnnotationMediaMappingSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<JobAnnotationMediaMapping> findByJobAnnotationsId(Long jobAnnotationsId) {
        return jdbcTemplate.query(
            JobAnnotationMediaMappingSql.FIND_BY_JOB_ANNOTATIONS_ID,
            Map.of("jobAnnotationsId", jobAnnotationsId),
            rowMapper
        );
    }

    @Override
    public List<JobAnnotationMediaMapping> findByMediasId(Long mediasId) {
        return jdbcTemplate.query(
            JobAnnotationMediaMappingSql.FIND_BY_MEDIAS_ID,
            Map.of("mediasId", mediasId),
            rowMapper
        );
    }

    @Override
    public List<JobAnnotationMediaMapping> findByJobsId(Long jobsId) {
        return jdbcTemplate.query(
            JobAnnotationMediaMappingSql.FIND_BY_JOBS_ID,
            Map.of("jobsId", jobsId),
            rowMapper
        );
    }

}
