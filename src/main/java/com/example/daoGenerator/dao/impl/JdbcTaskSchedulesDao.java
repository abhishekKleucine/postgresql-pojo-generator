package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.TaskSchedulesDao;
import com.example.pojogenerator.pojos.TaskSchedules;
import com.example.daoGenerator.dao.mapper.TaskSchedulesRowMapper;
import com.example.daoGenerator.dao.sql.TaskSchedulesSql;

/**
 * Enhanced JDBC implementation of TaskSchedulesDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcTaskSchedulesDao implements TaskSchedulesDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final TaskSchedulesRowMapper rowMapper;

    public JdbcTaskSchedulesDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new TaskSchedulesRowMapper();
    }

    @Override
    public Optional<TaskSchedules> findById(Long id) {
        try {
            TaskSchedules result = jdbcTemplate.queryForObject(
                TaskSchedulesSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(TaskSchedulesSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            TaskSchedulesSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<TaskSchedules> findAll() {
        return jdbcTemplate.query(TaskSchedulesSql.FIND_ALL, rowMapper);
    }

    @Override
    public TaskSchedules save(TaskSchedules entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private TaskSchedules insert(TaskSchedules entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            TaskSchedulesSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private TaskSchedules update(TaskSchedules entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(TaskSchedulesSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(TaskSchedulesSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<TaskSchedules> findByReferencedTasksId(Long referencedTasksId) {
        return jdbcTemplate.query(
            TaskSchedulesSql.FIND_BY_REFERENCED_TASKS_ID,
            Map.of("referencedTasksId", referencedTasksId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(TaskSchedules entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("condition", entity.getCondition());
        params.addValue("startDateInterval", entity.getStartDateInterval());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("referencedTasksId", entity.getReferencedTasksId());
        params.addValue("modifiedAt", entity.getModifiedAt());
        if (entity.getDueDateDuration() != null) {
            params.addValue("dueDateDuration", entity.getDueDateDuration().toString());
        } else {
            params.addValue("dueDateDuration", null);
        }
        params.addValue("dueDateInterval", entity.getDueDateInterval());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("type", entity.getType());
        if (entity.getStartDateDuration() != null) {
            params.addValue("startDateDuration", entity.getStartDateDuration().toString());
        } else {
            params.addValue("startDateDuration", null);
        }

        return params;
    }

}
