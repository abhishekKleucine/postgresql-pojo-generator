package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ParameterValueApprovalsDao;
import com.example.pojogenerator.pojos.ParameterValueApprovals;
import com.example.daoGenerator.dao.mapper.ParameterValueApprovalsRowMapper;
import com.example.daoGenerator.dao.sql.ParameterValueApprovalsSql;

/**
 * JDBC implementation of ParameterValueApprovalsDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcParameterValueApprovalsDao implements ParameterValueApprovalsDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ParameterValueApprovalsRowMapper rowMapper;

    public JdbcParameterValueApprovalsDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ParameterValueApprovalsRowMapper();
    }

    @Override
    public Optional<ParameterValueApprovals> findById(Long id) {
        try {
            ParameterValueApprovals result = jdbcTemplate.queryForObject(
                ParameterValueApprovalsSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<ParameterValueApprovals> findAll() {
        return jdbcTemplate.query(ParameterValueApprovalsSql.FIND_ALL, rowMapper);
    }

    @Override
    public ParameterValueApprovals save(ParameterValueApprovals entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ParameterValueApprovals insert(ParameterValueApprovals entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ParameterValueApprovalsSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ParameterValueApprovals update(ParameterValueApprovals entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            ParameterValueApprovalsSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(ParameterValueApprovals entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("usersId", entity.getUsersId());
        params.addValue("id", entity.getId());
        params.addValue("state", entity.getState());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ParameterValueApprovalsSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ParameterValueApprovalsSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ParameterValueApprovalsSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<ParameterValueApprovals> findByUsersId(Long usersId) {
        return jdbcTemplate.query(
            ParameterValueApprovalsSql.FIND_BY_USERS_ID,
            Map.of("usersId", usersId),
            rowMapper
        );
    }

}
