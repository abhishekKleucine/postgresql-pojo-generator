package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ScopeGroupsDao;
import com.example.pojogenerator.pojos.ScopeGroups;
import com.example.daoGenerator.dao.mapper.ScopeGroupsRowMapper;
import com.example.daoGenerator.dao.sql.ScopeGroupsSql;

/**
 * JDBC implementation of ScopeGroupsDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcScopeGroupsDao implements ScopeGroupsDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ScopeGroupsRowMapper rowMapper;

    public JdbcScopeGroupsDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ScopeGroupsRowMapper();
    }

    @Override
    public Optional<ScopeGroups> findById(Long id) {
        try {
            ScopeGroups result = jdbcTemplate.queryForObject(
                ScopeGroupsSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<ScopeGroups> findAll() {
        return jdbcTemplate.query(ScopeGroupsSql.FIND_ALL, rowMapper);
    }

    @Override
    public ScopeGroups save(ScopeGroups entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ScopeGroups insert(ScopeGroups entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ScopeGroupsSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ScopeGroups update(ScopeGroups entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            ScopeGroupsSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(ScopeGroups entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("archived", entity.getArchived());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("orderTree", entity.getOrderTree());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("name", entity.getName());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("servicesId", entity.getServicesId());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ScopeGroupsSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ScopeGroupsSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ScopeGroupsSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<ScopeGroups> findByServicesId(Long servicesId) {
        return jdbcTemplate.query(
            ScopeGroupsSql.FIND_BY_SERVICES_ID,
            Map.of("servicesId", servicesId),
            rowMapper
        );
    }

    @Override
    public List<ScopeGroups> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            ScopeGroupsSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<ScopeGroups> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            ScopeGroupsSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

}
