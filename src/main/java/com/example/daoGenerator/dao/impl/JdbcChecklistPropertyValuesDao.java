package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ChecklistPropertyValuesDao;
import com.example.pojogenerator.pojos.ChecklistPropertyValues;
import com.example.daoGenerator.dao.mapper.ChecklistPropertyValuesRowMapper;
import com.example.daoGenerator.dao.sql.ChecklistPropertyValuesSql;

/**
 * JDBC implementation of ChecklistPropertyValuesDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcChecklistPropertyValuesDao implements ChecklistPropertyValuesDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ChecklistPropertyValuesRowMapper rowMapper;

    public JdbcChecklistPropertyValuesDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ChecklistPropertyValuesRowMapper();
    }

    @Override
    public Optional<ChecklistPropertyValues> findById(Long id) {
        try {
            ChecklistPropertyValues result = jdbcTemplate.queryForObject(
                ChecklistPropertyValuesSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<ChecklistPropertyValues> findAll() {
        return jdbcTemplate.query(ChecklistPropertyValuesSql.FIND_ALL, rowMapper);
    }

    @Override
    public ChecklistPropertyValues save(ChecklistPropertyValues entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ChecklistPropertyValues insert(ChecklistPropertyValues entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ChecklistPropertyValuesSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ChecklistPropertyValues update(ChecklistPropertyValues entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            ChecklistPropertyValuesSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(ChecklistPropertyValues entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("checklistsId", entity.getChecklistsId());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("facilityUseCasePropertyMappingId", entity.getFacilityUseCasePropertyMappingId());
        params.addValue("value", entity.getValue());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ChecklistPropertyValuesSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ChecklistPropertyValuesSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ChecklistPropertyValuesSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<ChecklistPropertyValues> findByChecklistsId(Long checklistsId) {
        return jdbcTemplate.query(
            ChecklistPropertyValuesSql.FIND_BY_CHECKLISTS_ID,
            Map.of("checklistsId", checklistsId),
            rowMapper
        );
    }

    @Override
    public List<ChecklistPropertyValues> findByFacilityUseCasePropertyMappingId(Long facilityUseCasePropertyMappingId) {
        return jdbcTemplate.query(
            ChecklistPropertyValuesSql.FIND_BY_FACILITY_USE_CASE_PROPERTY_MAPPING_ID,
            Map.of("facilityUseCasePropertyMappingId", facilityUseCasePropertyMappingId),
            rowMapper
        );
    }

    @Override
    public List<ChecklistPropertyValues> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            ChecklistPropertyValuesSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<ChecklistPropertyValues> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            ChecklistPropertyValuesSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

}
