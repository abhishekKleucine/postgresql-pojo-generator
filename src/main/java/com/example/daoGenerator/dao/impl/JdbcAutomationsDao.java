package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.AutomationsDao;
import com.example.pojogenerator.pojos.Automations;
import com.example.daoGenerator.dao.mapper.AutomationsRowMapper;
import com.example.daoGenerator.dao.sql.AutomationsSql;

/**
 * JDBC implementation of AutomationsDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcAutomationsDao implements AutomationsDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final AutomationsRowMapper rowMapper;

    public JdbcAutomationsDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new AutomationsRowMapper();
    }

    @Override
    public Optional<Automations> findById(Long id) {
        try {
            Automations result = jdbcTemplate.queryForObject(
                AutomationsSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<Automations> findAll() {
        return jdbcTemplate.query(AutomationsSql.FIND_ALL, rowMapper);
    }

    @Override
    public Automations save(Automations entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private Automations insert(Automations entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            AutomationsSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private Automations update(Automations entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            AutomationsSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(Automations entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("actionType", entity.getActionType());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("archived", entity.getArchived());
        params.addValue("targetEntityType", entity.getTargetEntityType());
        params.addValue("createdBy", entity.getCreatedBy());
        if (entity.getTriggerDetails() != null) {
            params.addValue("triggerDetails", entity.getTriggerDetails().toString());
        } else {
            params.addValue("triggerDetails", null);
        }
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("triggerType", entity.getTriggerType());
        if (entity.getActionDetails() != null) {
            params.addValue("actionDetails", entity.getActionDetails().toString());
        } else {
            params.addValue("actionDetails", null);
        }
        params.addValue("type", entity.getType());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(AutomationsSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            AutomationsSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(AutomationsSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

}
