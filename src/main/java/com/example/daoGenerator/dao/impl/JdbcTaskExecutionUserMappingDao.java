package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.TaskExecutionUserMappingDao;
import com.example.pojogenerator.pojos.TaskExecutionUserMapping;
import com.example.daoGenerator.dao.mapper.TaskExecutionUserMappingRowMapper;
import com.example.daoGenerator.dao.sql.TaskExecutionUserMappingSql;

/**
 * JDBC implementation of TaskExecutionUserMappingDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcTaskExecutionUserMappingDao implements TaskExecutionUserMappingDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final TaskExecutionUserMappingRowMapper rowMapper;

    public JdbcTaskExecutionUserMappingDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new TaskExecutionUserMappingRowMapper();
    }

    @Override
    public Optional<TaskExecutionUserMapping> findById(Long id) {
        try {
            TaskExecutionUserMapping result = jdbcTemplate.queryForObject(
                TaskExecutionUserMappingSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<TaskExecutionUserMapping> findAll() {
        return jdbcTemplate.query(TaskExecutionUserMappingSql.FIND_ALL, rowMapper);
    }

    @Override
    public TaskExecutionUserMapping save(TaskExecutionUserMapping entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private TaskExecutionUserMapping insert(TaskExecutionUserMapping entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            TaskExecutionUserMappingSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private TaskExecutionUserMapping update(TaskExecutionUserMapping entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            TaskExecutionUserMappingSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(TaskExecutionUserMapping entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("actionPerformed", entity.getActionPerformed());
        params.addValue("userGroupsId", entity.getUserGroupsId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("usersId", entity.getUsersId());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("taskExecutionsId", entity.getTaskExecutionsId());
        params.addValue("state", entity.getState());
        params.addValue("id", entity.getId());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(TaskExecutionUserMappingSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            TaskExecutionUserMappingSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(TaskExecutionUserMappingSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<TaskExecutionUserMapping> findByTaskExecutionsId(Long taskExecutionsId) {
        return jdbcTemplate.query(
            TaskExecutionUserMappingSql.FIND_BY_TASK_EXECUTIONS_ID,
            Map.of("taskExecutionsId", taskExecutionsId),
            rowMapper
        );
    }

    @Override
    public List<TaskExecutionUserMapping> findByUserGroupsId(Long userGroupsId) {
        return jdbcTemplate.query(
            TaskExecutionUserMappingSql.FIND_BY_USER_GROUPS_ID,
            Map.of("userGroupsId", userGroupsId),
            rowMapper
        );
    }

    @Override
    public List<TaskExecutionUserMapping> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            TaskExecutionUserMappingSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<TaskExecutionUserMapping> findByUsersId(Long usersId) {
        return jdbcTemplate.query(
            TaskExecutionUserMappingSql.FIND_BY_USERS_ID,
            Map.of("usersId", usersId),
            rowMapper
        );
    }

    @Override
    public List<TaskExecutionUserMapping> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            TaskExecutionUserMappingSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

}
