package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.TaskExecutionTimersDao;
import com.example.pojogenerator.pojos.TaskExecutionTimers;
import com.example.daoGenerator.dao.mapper.TaskExecutionTimersRowMapper;
import com.example.daoGenerator.dao.sql.TaskExecutionTimersSql;

/**
 * Enhanced JDBC implementation of TaskExecutionTimersDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcTaskExecutionTimersDao implements TaskExecutionTimersDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final TaskExecutionTimersRowMapper rowMapper;

    public JdbcTaskExecutionTimersDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new TaskExecutionTimersRowMapper();
    }

    @Override
    public Optional<TaskExecutionTimers> findById(Long id) {
        try {
            TaskExecutionTimers result = jdbcTemplate.queryForObject(
                TaskExecutionTimersSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(TaskExecutionTimersSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            TaskExecutionTimersSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<TaskExecutionTimers> findAll() {
        return jdbcTemplate.query(TaskExecutionTimersSql.FIND_ALL, rowMapper);
    }

    @Override
    public TaskExecutionTimers save(TaskExecutionTimers entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private TaskExecutionTimers insert(TaskExecutionTimers entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            TaskExecutionTimersSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private TaskExecutionTimers update(TaskExecutionTimers entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(TaskExecutionTimersSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(TaskExecutionTimersSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<TaskExecutionTimers> findByTaskExecutionsId(Long taskExecutionsId) {
        return jdbcTemplate.query(
            TaskExecutionTimersSql.FIND_BY_TASK_EXECUTIONS_ID,
            Map.of("taskExecutionsId", taskExecutionsId),
            rowMapper
        );
    }

    @Override
    public List<TaskExecutionTimers> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            TaskExecutionTimersSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<TaskExecutionTimers> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            TaskExecutionTimersSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(TaskExecutionTimers entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("reason", entity.getReason());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("resumedAt", entity.getResumedAt());
        params.addValue("comment", entity.getComment());
        params.addValue("pausedAt", entity.getPausedAt());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("taskExecutionsId", entity.getTaskExecutionsId());

        return params;
    }

}
