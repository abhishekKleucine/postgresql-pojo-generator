package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.FacilitieDao;
import com.example.pojogenerator.pojos.Facilitie;
import com.example.daoGenerator.dao.mapper.FacilitieRowMapper;
import com.example.daoGenerator.dao.sql.FacilitieSql;

/**
 * Enhanced JDBC implementation of FacilitieDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcFacilitieDao implements FacilitieDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final FacilitieRowMapper rowMapper;

    public JdbcFacilitieDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new FacilitieRowMapper();
    }

    @Override
    public Optional<Facilitie> findById(Long id) {
        try {
            Facilitie result = jdbcTemplate.queryForObject(
                FacilitieSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(FacilitieSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            FacilitieSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<Facilitie> findAll() {
        return jdbcTemplate.query(FacilitieSql.FIND_ALL, rowMapper);
    }

    @Override
    public Facilitie save(Facilitie entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private Facilitie insert(Facilitie entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            FacilitieSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private Facilitie update(Facilitie entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(FacilitieSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(FacilitieSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<Facilitie> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            FacilitieSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<Facilitie> findByOrganisationsId(Long organisationsId) {
        return jdbcTemplate.query(
            FacilitieSql.FIND_BY_ORGANISATIONS_ID,
            Map.of("organisationsId", organisationsId),
            rowMapper
        );
    }

    @Override
    public List<Facilitie> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            FacilitieSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(Facilitie entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("organisationsId", entity.getOrganisationsId());
        params.addValue("dateFormat", entity.getDateFormat());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("timeZone", entity.getTimeZone());
        params.addValue("logoUrl", entity.getLogoUrl());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("archived", entity.getArchived());
        params.addValue("dateTimeFormat", entity.getDateTimeFormat());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("timeFormat", entity.getTimeFormat());
        params.addValue("name", entity.getName());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());

        return params;
    }

}
