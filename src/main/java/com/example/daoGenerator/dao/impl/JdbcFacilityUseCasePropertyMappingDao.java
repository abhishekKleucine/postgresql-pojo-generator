package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.FacilityUseCasePropertyMappingDao;
import com.example.pojogenerator.pojos.FacilityUseCasePropertyMapping;
import com.example.daoGenerator.dao.mapper.FacilityUseCasePropertyMappingRowMapper;
import com.example.daoGenerator.dao.sql.FacilityUseCasePropertyMappingSql;

/**
 * Enhanced JDBC implementation of FacilityUseCasePropertyMappingDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcFacilityUseCasePropertyMappingDao implements FacilityUseCasePropertyMappingDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final FacilityUseCasePropertyMappingRowMapper rowMapper;

    public JdbcFacilityUseCasePropertyMappingDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new FacilityUseCasePropertyMappingRowMapper();
    }

    @Override
    public Optional<FacilityUseCasePropertyMapping> findById(Long id) {
        try {
            FacilityUseCasePropertyMapping result = jdbcTemplate.queryForObject(
                FacilityUseCasePropertyMappingSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(FacilityUseCasePropertyMappingSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            FacilityUseCasePropertyMappingSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<FacilityUseCasePropertyMapping> findAll() {
        return jdbcTemplate.query(FacilityUseCasePropertyMappingSql.FIND_ALL, rowMapper);
    }

    @Override
    public FacilityUseCasePropertyMapping save(FacilityUseCasePropertyMapping entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private FacilityUseCasePropertyMapping insert(FacilityUseCasePropertyMapping entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            FacilityUseCasePropertyMappingSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private FacilityUseCasePropertyMapping update(FacilityUseCasePropertyMapping entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(FacilityUseCasePropertyMappingSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(FacilityUseCasePropertyMappingSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<FacilityUseCasePropertyMapping> findByUseCasesId(Long useCasesId) {
        return jdbcTemplate.query(
            FacilityUseCasePropertyMappingSql.FIND_BY_USE_CASES_ID,
            Map.of("useCasesId", useCasesId),
            rowMapper
        );
    }

    @Override
    public List<FacilityUseCasePropertyMapping> findByPropertiesId(Long propertiesId) {
        return jdbcTemplate.query(
            FacilityUseCasePropertyMappingSql.FIND_BY_PROPERTIES_ID,
            Map.of("propertiesId", propertiesId),
            rowMapper
        );
    }

    @Override
    public List<FacilityUseCasePropertyMapping> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            FacilityUseCasePropertyMappingSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<FacilityUseCasePropertyMapping> findByFacilitiesId(Long facilitiesId) {
        return jdbcTemplate.query(
            FacilityUseCasePropertyMappingSql.FIND_BY_FACILITIES_ID,
            Map.of("facilitiesId", facilitiesId),
            rowMapper
        );
    }

    @Override
    public List<FacilityUseCasePropertyMapping> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            FacilityUseCasePropertyMappingSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(FacilityUseCasePropertyMapping entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("propertiesId", entity.getPropertiesId());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("facilitiesId", entity.getFacilitiesId());
        params.addValue("orderTree", entity.getOrderTree());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("useCasesId", entity.getUseCasesId());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("labelAlias", entity.getLabelAlias());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("placeHolderAlias", entity.getPlaceHolderAlias());
        params.addValue("isMandatory", entity.getIsMandatory());

        return params;
    }

}
