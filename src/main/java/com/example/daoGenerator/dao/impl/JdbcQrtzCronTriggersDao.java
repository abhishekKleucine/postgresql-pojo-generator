package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.QrtzCronTriggersDao;
import com.example.pojogenerator.pojos.QrtzCronTriggers;
import com.example.daoGenerator.dao.mapper.QrtzCronTriggersRowMapper;
import com.example.daoGenerator.dao.sql.QrtzCronTriggersSql;

/**
 * Enhanced JDBC implementation of QrtzCronTriggersDao
 * Key Type: COMPOSITE_KEY
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcQrtzCronTriggersDao implements QrtzCronTriggersDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final QrtzCronTriggersRowMapper rowMapper;

    public JdbcQrtzCronTriggersDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new QrtzCronTriggersRowMapper();
    }

    @Override
    public Optional<QrtzCronTriggers> findByTriggerNameAndSchedNameAndTriggerGroup(String triggerName, String schedName, String triggerGroup) {
        try {
            QrtzCronTriggers result = jdbcTemplate.queryForObject(
                QrtzCronTriggersSql.FIND_BY_TRIGGER_NAME_AND_SCHED_NAME_AND_TRIGGER_GROUP,
                Map.of("triggerName", triggerName, "schedName", schedName, "triggerGroup", triggerGroup),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteByTriggerNameAndSchedNameAndTriggerGroup(String triggerName, String schedName, String triggerGroup) {
        jdbcTemplate.update(QrtzCronTriggersSql.DELETE_BY_TRIGGER_NAME_AND_SCHED_NAME_AND_TRIGGER_GROUP, Map.of("triggerName", triggerName, "schedName", schedName, "triggerGroup", triggerGroup));
    }

    @Override
    public boolean existsByTriggerNameAndSchedNameAndTriggerGroup(String triggerName, String schedName, String triggerGroup) {
        Integer count = jdbcTemplate.queryForObject(
            QrtzCronTriggersSql.EXISTS_BY_TRIGGER_NAME_AND_SCHED_NAME_AND_TRIGGER_GROUP,
            Map.of("triggerName", triggerName, "schedName", schedName, "triggerGroup", triggerGroup),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<QrtzCronTriggers> findAll() {
        return jdbcTemplate.query(QrtzCronTriggersSql.FIND_ALL, rowMapper);
    }

    @Override
    public QrtzCronTriggers save(QrtzCronTriggers entity) {
        if (existsByTriggerNameAndSchedNameAndTriggerGroup(entity.getTriggerName(), entity.getSchedName(), entity.getTriggerGroup())) {
            return update(entity);
        } else {
            return insert(entity);
        }
    }

    private QrtzCronTriggers insert(QrtzCronTriggers entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();

        MapSqlParameterSource params = createParameterMap(entity);
        jdbcTemplate.update(QrtzCronTriggersSql.INSERT, params);
        return entity;
    }

    private QrtzCronTriggers update(QrtzCronTriggers entity) {
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(QrtzCronTriggersSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(QrtzCronTriggersSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<QrtzCronTriggers> findByTriggerName(Long triggerName) {
        return jdbcTemplate.query(
            QrtzCronTriggersSql.FIND_BY_TRIGGER_NAME,
            Map.of("triggerName", triggerName),
            rowMapper
        );
    }

    @Override
    public List<QrtzCronTriggers> findBySchedName(Long schedName) {
        return jdbcTemplate.query(
            QrtzCronTriggersSql.FIND_BY_SCHED_NAME,
            Map.of("schedName", schedName),
            rowMapper
        );
    }

    @Override
    public List<QrtzCronTriggers> findByTriggerGroup(Long triggerGroup) {
        return jdbcTemplate.query(
            QrtzCronTriggersSql.FIND_BY_TRIGGER_GROUP,
            Map.of("triggerGroup", triggerGroup),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(QrtzCronTriggers entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("cronExpression", entity.getCronExpression());
        params.addValue("triggerGroup", entity.getTriggerGroup());
        params.addValue("triggerName", entity.getTriggerName());
        params.addValue("timeZoneId", entity.getTimeZoneId());
        params.addValue("schedName", entity.getSchedName());

        return params;
    }

}
