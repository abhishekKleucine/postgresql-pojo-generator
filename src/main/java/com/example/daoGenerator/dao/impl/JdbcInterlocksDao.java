package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.InterlocksDao;
import com.example.pojogenerator.pojos.Interlocks;
import com.example.daoGenerator.dao.mapper.InterlocksRowMapper;
import com.example.daoGenerator.dao.sql.InterlocksSql;

/**
 * JDBC implementation of InterlocksDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcInterlocksDao implements InterlocksDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final InterlocksRowMapper rowMapper;

    public JdbcInterlocksDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new InterlocksRowMapper();
    }

    @Override
    public Optional<Interlocks> findById(Long id) {
        try {
            Interlocks result = jdbcTemplate.queryForObject(
                InterlocksSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<Interlocks> findAll() {
        return jdbcTemplate.query(InterlocksSql.FIND_ALL, rowMapper);
    }

    @Override
    public Interlocks save(Interlocks entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private Interlocks insert(Interlocks entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            InterlocksSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private Interlocks update(Interlocks entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            InterlocksSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(Interlocks entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("targetEntityId", entity.getTargetEntityId());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("targetEntityType", entity.getTargetEntityType());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        if (entity.getValidations() != null) {
            params.addValue("validations", entity.getValidations().toString());
        } else {
            params.addValue("validations", null);
        }

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(InterlocksSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            InterlocksSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(InterlocksSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<Interlocks> findByTargetEntityId(Long targetEntityId) {
        return jdbcTemplate.query(
            InterlocksSql.FIND_BY_TARGET_ENTITY_ID,
            Map.of("targetEntityId", targetEntityId),
            rowMapper
        );
    }

}
