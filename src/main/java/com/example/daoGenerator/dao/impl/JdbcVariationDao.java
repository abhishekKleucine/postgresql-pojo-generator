package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.VariationDao;
import com.example.pojogenerator.pojos.Variation;
import com.example.daoGenerator.dao.mapper.VariationRowMapper;
import com.example.daoGenerator.dao.sql.VariationSql;

/**
 * Enhanced JDBC implementation of VariationDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcVariationDao implements VariationDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final VariationRowMapper rowMapper;

    public JdbcVariationDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new VariationRowMapper();
    }

    @Override
    public Optional<Variation> findById(Long id) {
        try {
            Variation result = jdbcTemplate.queryForObject(
                VariationSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(VariationSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            VariationSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<Variation> findAll() {
        return jdbcTemplate.query(VariationSql.FIND_ALL, rowMapper);
    }

    @Override
    public Variation save(Variation entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private Variation insert(Variation entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            VariationSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private Variation update(Variation entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(VariationSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(VariationSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<Variation> findByParameterValuesId(Long parameterValuesId) {
        return jdbcTemplate.query(
            VariationSql.FIND_BY_PARAMETER_VALUES_ID,
            Map.of("parameterValuesId", parameterValuesId),
            rowMapper
        );
    }

    @Override
    public List<Variation> findByJobsId(Long jobsId) {
        return jdbcTemplate.query(
            VariationSql.FIND_BY_JOBS_ID,
            Map.of("jobsId", jobsId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(Variation entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        if (entity.getOldDetails() != null) {
            params.addValue("oldDetails", entity.getOldDetails().toString());
        } else {
            params.addValue("oldDetails", null);
        }
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("description", entity.getDescription());
        if (entity.getNewDetails() != null) {
            params.addValue("newDetails", entity.getNewDetails().toString());
        } else {
            params.addValue("newDetails", null);
        }
        params.addValue("type", entity.getType());
        params.addValue("variationNumber", entity.getVariationNumber());
        params.addValue("parameterValuesId", entity.getParameterValuesId());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("configId", entity.getConfigId());
        params.addValue("name", entity.getName());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("jobsId", entity.getJobsId());

        return params;
    }

}
