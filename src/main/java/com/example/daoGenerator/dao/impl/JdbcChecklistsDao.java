package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ChecklistsDao;
import com.example.pojogenerator.pojos.Checklists;
import com.example.daoGenerator.dao.mapper.ChecklistsRowMapper;
import com.example.daoGenerator.dao.sql.ChecklistsSql;

/**
 * Enhanced JDBC implementation of ChecklistsDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcChecklistsDao implements ChecklistsDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ChecklistsRowMapper rowMapper;

    public JdbcChecklistsDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ChecklistsRowMapper();
    }

    @Override
    public Optional<Checklists> findById(Long id) {
        try {
            Checklists result = jdbcTemplate.queryForObject(
                ChecklistsSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ChecklistsSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ChecklistsSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<Checklists> findAll() {
        return jdbcTemplate.query(ChecklistsSql.FIND_ALL, rowMapper);
    }

    @Override
    public Checklists save(Checklists entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private Checklists insert(Checklists entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ChecklistsSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private Checklists update(Checklists entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(ChecklistsSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ChecklistsSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<Checklists> findByUseCasesId(Long useCasesId) {
        return jdbcTemplate.query(
            ChecklistsSql.FIND_BY_USE_CASES_ID,
            Map.of("useCasesId", useCasesId),
            rowMapper
        );
    }

    @Override
    public List<Checklists> findByReleasedBy(Long releasedBy) {
        return jdbcTemplate.query(
            ChecklistsSql.FIND_BY_RELEASED_BY,
            Map.of("releasedBy", releasedBy),
            rowMapper
        );
    }

    @Override
    public List<Checklists> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            ChecklistsSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<Checklists> findByOrganisationsId(Long organisationsId) {
        return jdbcTemplate.query(
            ChecklistsSql.FIND_BY_ORGANISATIONS_ID,
            Map.of("organisationsId", organisationsId),
            rowMapper
        );
    }

    @Override
    public List<Checklists> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            ChecklistsSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    @Override
    public List<Checklists> findByVersionsId(Long versionsId) {
        return jdbcTemplate.query(
            ChecklistsSql.FIND_BY_VERSIONS_ID,
            Map.of("versionsId", versionsId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(Checklists entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("releasedAt", entity.getReleasedAt());
        params.addValue("code", entity.getCode());
        params.addValue("organisationsId", entity.getOrganisationsId());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("releasedBy", entity.getReleasedBy());
        params.addValue("useCasesId", entity.getUseCasesId());
        params.addValue("reviewCycle", entity.getReviewCycle());
        params.addValue("description", entity.getDescription());
        if (entity.getJobLogColumns() != null) {
            params.addValue("jobLogColumns", entity.getJobLogColumns().toString());
        } else {
            params.addValue("jobLogColumns", null);
        }
        params.addValue("archived", entity.getArchived());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("name", entity.getName());
        params.addValue("isGlobal", entity.getIsGlobal());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("colorCode", entity.getColorCode());
        params.addValue("id", entity.getId());
        params.addValue("state", entity.getState());
        params.addValue("versionsId", entity.getVersionsId());

        return params;
    }

}
