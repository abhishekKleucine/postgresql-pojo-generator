package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.RelationDao;
import com.example.pojogenerator.pojos.Relation;
import com.example.daoGenerator.dao.mapper.RelationRowMapper;
import com.example.daoGenerator.dao.sql.RelationSql;

/**
 * Enhanced JDBC implementation of RelationDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcRelationDao implements RelationDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final RelationRowMapper rowMapper;

    public JdbcRelationDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new RelationRowMapper();
    }

    @Override
    public Optional<Relation> findById(Long id) {
        try {
            Relation result = jdbcTemplate.queryForObject(
                RelationSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(RelationSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            RelationSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<Relation> findAll() {
        return jdbcTemplate.query(RelationSql.FIND_ALL, rowMapper);
    }

    @Override
    public Relation save(Relation entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private Relation insert(Relation entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            RelationSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private Relation update(Relation entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(RelationSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(RelationSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<Relation> findByChecklistsId(Long checklistsId) {
        return jdbcTemplate.query(
            RelationSql.FIND_BY_CHECKLISTS_ID,
            Map.of("checklistsId", checklistsId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(Relation entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        if (entity.getVariables() != null) {
            params.addValue("variables", entity.getVariables().toString());
        } else {
            params.addValue("variables", null);
        }
        params.addValue("objectTypeId", entity.getObjectTypeId());
        params.addValue("orderTree", entity.getOrderTree());
        params.addValue("displayName", entity.getDisplayName());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("externalId", entity.getExternalId());
        params.addValue("checklistsId", entity.getChecklistsId());
        params.addValue("collection", entity.getCollection());
        params.addValue("urlPath", entity.getUrlPath());
        params.addValue("cardinality", entity.getCardinality());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        if (entity.getValidations() != null) {
            params.addValue("validations", entity.getValidations().toString());
        } else {
            params.addValue("validations", null);
        }
        params.addValue("isMandatory", entity.getIsMandatory());

        return params;
    }

}
