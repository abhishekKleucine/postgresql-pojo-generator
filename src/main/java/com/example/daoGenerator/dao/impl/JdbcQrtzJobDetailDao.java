package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.QrtzJobDetailDao;
import com.example.pojogenerator.pojos.QrtzJobDetail;
import com.example.daoGenerator.dao.mapper.QrtzJobDetailRowMapper;
import com.example.daoGenerator.dao.sql.QrtzJobDetailSql;

/**
 * Enhanced JDBC implementation of QrtzJobDetailDao
 * Key Type: COMPOSITE_KEY
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcQrtzJobDetailDao implements QrtzJobDetailDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final QrtzJobDetailRowMapper rowMapper;

    public JdbcQrtzJobDetailDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new QrtzJobDetailRowMapper();
    }

    @Override
    public Optional<QrtzJobDetail> findBySchedNameAndJobNameAndJobGroup(String schedName, String jobName, String jobGroup) {
        try {
            QrtzJobDetail result = jdbcTemplate.queryForObject(
                QrtzJobDetailSql.FIND_BY_SCHED_NAME_AND_JOB_NAME_AND_JOB_GROUP,
                Map.of("schedName", schedName, "jobName", jobName, "jobGroup", jobGroup),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteBySchedNameAndJobNameAndJobGroup(String schedName, String jobName, String jobGroup) {
        jdbcTemplate.update(QrtzJobDetailSql.DELETE_BY_SCHED_NAME_AND_JOB_NAME_AND_JOB_GROUP, Map.of("schedName", schedName, "jobName", jobName, "jobGroup", jobGroup));
    }

    @Override
    public boolean existsBySchedNameAndJobNameAndJobGroup(String schedName, String jobName, String jobGroup) {
        Integer count = jdbcTemplate.queryForObject(
            QrtzJobDetailSql.EXISTS_BY_SCHED_NAME_AND_JOB_NAME_AND_JOB_GROUP,
            Map.of("schedName", schedName, "jobName", jobName, "jobGroup", jobGroup),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<QrtzJobDetail> findAll() {
        return jdbcTemplate.query(QrtzJobDetailSql.FIND_ALL, rowMapper);
    }

    @Override
    public QrtzJobDetail save(QrtzJobDetail entity) {
        if (existsBySchedNameAndJobNameAndJobGroup(entity.getSchedName(), entity.getJobName(), entity.getJobGroup())) {
            return update(entity);
        } else {
            return insert(entity);
        }
    }

    private QrtzJobDetail insert(QrtzJobDetail entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();

        MapSqlParameterSource params = createParameterMap(entity);
        jdbcTemplate.update(QrtzJobDetailSql.INSERT, params);
        return entity;
    }

    private QrtzJobDetail update(QrtzJobDetail entity) {
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(QrtzJobDetailSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(QrtzJobDetailSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    private MapSqlParameterSource createParameterMap(QrtzJobDetail entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("jobName", entity.getJobName());
        params.addValue("jobData", entity.getJobData());
        params.addValue("jobClassName", entity.getJobClassName());
        params.addValue("requestsRecovery", entity.getRequestsRecovery());
        params.addValue("description", entity.getDescription());
        params.addValue("isDurable", entity.getIsDurable());
        params.addValue("schedName", entity.getSchedName());
        params.addValue("jobGroup", entity.getJobGroup());
        params.addValue("isNonconcurrent", entity.getIsNonconcurrent());
        params.addValue("isUpdateData", entity.getIsUpdateData());

        return params;
    }

}
