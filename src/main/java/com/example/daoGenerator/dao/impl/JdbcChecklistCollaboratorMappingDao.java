package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ChecklistCollaboratorMappingDao;
import com.example.pojogenerator.pojos.ChecklistCollaboratorMapping;
import com.example.daoGenerator.dao.mapper.ChecklistCollaboratorMappingRowMapper;
import com.example.daoGenerator.dao.sql.ChecklistCollaboratorMappingSql;

/**
 * Enhanced JDBC implementation of ChecklistCollaboratorMappingDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcChecklistCollaboratorMappingDao implements ChecklistCollaboratorMappingDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ChecklistCollaboratorMappingRowMapper rowMapper;

    public JdbcChecklistCollaboratorMappingDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ChecklistCollaboratorMappingRowMapper();
    }

    @Override
    public Optional<ChecklistCollaboratorMapping> findById(Long id) {
        try {
            ChecklistCollaboratorMapping result = jdbcTemplate.queryForObject(
                ChecklistCollaboratorMappingSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ChecklistCollaboratorMappingSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ChecklistCollaboratorMappingSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<ChecklistCollaboratorMapping> findAll() {
        return jdbcTemplate.query(ChecklistCollaboratorMappingSql.FIND_ALL, rowMapper);
    }

    @Override
    public ChecklistCollaboratorMapping save(ChecklistCollaboratorMapping entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ChecklistCollaboratorMapping insert(ChecklistCollaboratorMapping entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ChecklistCollaboratorMappingSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ChecklistCollaboratorMapping update(ChecklistCollaboratorMapping entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(ChecklistCollaboratorMappingSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ChecklistCollaboratorMappingSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<ChecklistCollaboratorMapping> findByChecklistsId(Long checklistsId) {
        return jdbcTemplate.query(
            ChecklistCollaboratorMappingSql.FIND_BY_CHECKLISTS_ID,
            Map.of("checklistsId", checklistsId),
            rowMapper
        );
    }

    @Override
    public List<ChecklistCollaboratorMapping> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            ChecklistCollaboratorMappingSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<ChecklistCollaboratorMapping> findByUsersId(Long usersId) {
        return jdbcTemplate.query(
            ChecklistCollaboratorMappingSql.FIND_BY_USERS_ID,
            Map.of("usersId", usersId),
            rowMapper
        );
    }

    @Override
    public List<ChecklistCollaboratorMapping> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            ChecklistCollaboratorMappingSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(ChecklistCollaboratorMapping entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("phase", entity.getPhase());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("orderTree", entity.getOrderTree());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("usersId", entity.getUsersId());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("checklistsId", entity.getChecklistsId());
        params.addValue("id", entity.getId());
        params.addValue("state", entity.getState());
        params.addValue("type", entity.getType());
        params.addValue("phaseType", entity.getPhaseType());

        return params;
    }

}
