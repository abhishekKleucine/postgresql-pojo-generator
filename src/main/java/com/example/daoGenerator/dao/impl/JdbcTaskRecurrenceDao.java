package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.TaskRecurrenceDao;
import com.example.pojogenerator.pojos.TaskRecurrence;
import com.example.daoGenerator.dao.mapper.TaskRecurrenceRowMapper;
import com.example.daoGenerator.dao.sql.TaskRecurrenceSql;

/**
 * Enhanced JDBC implementation of TaskRecurrenceDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcTaskRecurrenceDao implements TaskRecurrenceDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final TaskRecurrenceRowMapper rowMapper;

    public JdbcTaskRecurrenceDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new TaskRecurrenceRowMapper();
    }

    @Override
    public Optional<TaskRecurrence> findById(Long id) {
        try {
            TaskRecurrence result = jdbcTemplate.queryForObject(
                TaskRecurrenceSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(TaskRecurrenceSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            TaskRecurrenceSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<TaskRecurrence> findAll() {
        return jdbcTemplate.query(TaskRecurrenceSql.FIND_ALL, rowMapper);
    }

    @Override
    public TaskRecurrence save(TaskRecurrence entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private TaskRecurrence insert(TaskRecurrence entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            TaskRecurrenceSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private TaskRecurrence update(TaskRecurrence entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(TaskRecurrenceSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(TaskRecurrenceSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    private MapSqlParameterSource createParameterMap(TaskRecurrence entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        if (entity.getNegativeDueDateToleranceDuration() != null) {
            params.addValue("negativeDueDateToleranceDuration", entity.getNegativeDueDateToleranceDuration().toString());
        } else {
            params.addValue("negativeDueDateToleranceDuration", null);
        }
        params.addValue("positiveStartDateToleranceInterval", entity.getPositiveStartDateToleranceInterval());
        params.addValue("modifiedAt", entity.getModifiedAt());
        if (entity.getPositiveStartDateToleranceDuration() != null) {
            params.addValue("positiveStartDateToleranceDuration", entity.getPositiveStartDateToleranceDuration().toString());
        } else {
            params.addValue("positiveStartDateToleranceDuration", null);
        }
        params.addValue("negativeDueDateToleranceInterval", entity.getNegativeDueDateToleranceInterval());
        if (entity.getNegativeStartDateToleranceDuration() != null) {
            params.addValue("negativeStartDateToleranceDuration", entity.getNegativeStartDateToleranceDuration().toString());
        } else {
            params.addValue("negativeStartDateToleranceDuration", null);
        }
        if (entity.getStartDateDuration() != null) {
            params.addValue("startDateDuration", entity.getStartDateDuration().toString());
        } else {
            params.addValue("startDateDuration", null);
        }
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("positiveDueDateToleranceInterval", entity.getPositiveDueDateToleranceInterval());
        params.addValue("startDateInterval", entity.getStartDateInterval());
        params.addValue("createdBy", entity.getCreatedBy());
        if (entity.getDueDateDuration() != null) {
            params.addValue("dueDateDuration", entity.getDueDateDuration().toString());
        } else {
            params.addValue("dueDateDuration", null);
        }
        if (entity.getPositiveDueDateToleranceDuration() != null) {
            params.addValue("positiveDueDateToleranceDuration", entity.getPositiveDueDateToleranceDuration().toString());
        } else {
            params.addValue("positiveDueDateToleranceDuration", null);
        }
        params.addValue("dueDateInterval", entity.getDueDateInterval());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("negativeStartDateToleranceInterval", entity.getNegativeStartDateToleranceInterval());

        return params;
    }

}
