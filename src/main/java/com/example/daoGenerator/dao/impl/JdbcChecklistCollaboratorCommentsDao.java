package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ChecklistCollaboratorCommentsDao;
import com.example.pojogenerator.pojos.ChecklistCollaboratorComments;
import com.example.daoGenerator.dao.mapper.ChecklistCollaboratorCommentsRowMapper;
import com.example.daoGenerator.dao.sql.ChecklistCollaboratorCommentsSql;

/**
 * Enhanced JDBC implementation of ChecklistCollaboratorCommentsDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcChecklistCollaboratorCommentsDao implements ChecklistCollaboratorCommentsDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ChecklistCollaboratorCommentsRowMapper rowMapper;

    public JdbcChecklistCollaboratorCommentsDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ChecklistCollaboratorCommentsRowMapper();
    }

    @Override
    public Optional<ChecklistCollaboratorComments> findById(Long id) {
        try {
            ChecklistCollaboratorComments result = jdbcTemplate.queryForObject(
                ChecklistCollaboratorCommentsSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ChecklistCollaboratorCommentsSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ChecklistCollaboratorCommentsSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<ChecklistCollaboratorComments> findAll() {
        return jdbcTemplate.query(ChecklistCollaboratorCommentsSql.FIND_ALL, rowMapper);
    }

    @Override
    public ChecklistCollaboratorComments save(ChecklistCollaboratorComments entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ChecklistCollaboratorComments insert(ChecklistCollaboratorComments entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ChecklistCollaboratorCommentsSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ChecklistCollaboratorComments update(ChecklistCollaboratorComments entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(ChecklistCollaboratorCommentsSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ChecklistCollaboratorCommentsSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<ChecklistCollaboratorComments> findByChecklistCollaboratorMappingsId(Long checklistCollaboratorMappingsId) {
        return jdbcTemplate.query(
            ChecklistCollaboratorCommentsSql.FIND_BY_CHECKLIST_COLLABORATOR_MAPPINGS_ID,
            Map.of("checklistCollaboratorMappingsId", checklistCollaboratorMappingsId),
            rowMapper
        );
    }

    @Override
    public List<ChecklistCollaboratorComments> findByChecklistsId(Long checklistsId) {
        return jdbcTemplate.query(
            ChecklistCollaboratorCommentsSql.FIND_BY_CHECKLISTS_ID,
            Map.of("checklistsId", checklistsId),
            rowMapper
        );
    }

    @Override
    public List<ChecklistCollaboratorComments> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            ChecklistCollaboratorCommentsSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<ChecklistCollaboratorComments> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            ChecklistCollaboratorCommentsSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(ChecklistCollaboratorComments entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("comments", entity.getComments());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("checklistCollaboratorMappingsId", entity.getChecklistCollaboratorMappingsId());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("checklistsId", entity.getChecklistsId());
        params.addValue("id", entity.getId());
        params.addValue("reviewState", entity.getReviewState());

        return params;
    }

}
