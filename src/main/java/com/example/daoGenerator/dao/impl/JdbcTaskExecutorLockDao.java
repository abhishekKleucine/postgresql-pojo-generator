package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.TaskExecutorLockDao;
import com.example.pojogenerator.pojos.TaskExecutorLock;
import com.example.daoGenerator.dao.mapper.TaskExecutorLockRowMapper;
import com.example.daoGenerator.dao.sql.TaskExecutorLockSql;

/**
 * Enhanced JDBC implementation of TaskExecutorLockDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcTaskExecutorLockDao implements TaskExecutorLockDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final TaskExecutorLockRowMapper rowMapper;

    public JdbcTaskExecutorLockDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new TaskExecutorLockRowMapper();
    }

    @Override
    public Optional<TaskExecutorLock> findById(Long id) {
        try {
            TaskExecutorLock result = jdbcTemplate.queryForObject(
                TaskExecutorLockSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(TaskExecutorLockSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            TaskExecutorLockSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<TaskExecutorLock> findAll() {
        return jdbcTemplate.query(TaskExecutorLockSql.FIND_ALL, rowMapper);
    }

    @Override
    public TaskExecutorLock save(TaskExecutorLock entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private TaskExecutorLock insert(TaskExecutorLock entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            TaskExecutorLockSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private TaskExecutorLock update(TaskExecutorLock entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(TaskExecutorLockSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(TaskExecutorLockSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<TaskExecutorLock> findByTasksId(Long tasksId) {
        return jdbcTemplate.query(
            TaskExecutorLockSql.FIND_BY_TASKS_ID,
            Map.of("tasksId", tasksId),
            rowMapper
        );
    }

    @Override
    public List<TaskExecutorLock> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            TaskExecutorLockSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<TaskExecutorLock> findByReferencedTasksId(Long referencedTasksId) {
        return jdbcTemplate.query(
            TaskExecutorLockSql.FIND_BY_REFERENCED_TASKS_ID,
            Map.of("referencedTasksId", referencedTasksId),
            rowMapper
        );
    }

    @Override
    public List<TaskExecutorLock> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            TaskExecutorLockSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(TaskExecutorLock entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("tasksId", entity.getTasksId());
        params.addValue("referencedTasksId", entity.getReferencedTasksId());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("lockType", entity.getLockType());

        return params;
    }

}
