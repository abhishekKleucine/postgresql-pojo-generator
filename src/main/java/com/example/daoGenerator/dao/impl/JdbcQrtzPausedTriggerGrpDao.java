package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.QrtzPausedTriggerGrpDao;
import com.example.pojogenerator.pojos.QrtzPausedTriggerGrp;
import com.example.daoGenerator.dao.mapper.QrtzPausedTriggerGrpRowMapper;
import com.example.daoGenerator.dao.sql.QrtzPausedTriggerGrpSql;

/**
 * Enhanced JDBC implementation of QrtzPausedTriggerGrpDao
 * Key Type: COMPOSITE_KEY
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcQrtzPausedTriggerGrpDao implements QrtzPausedTriggerGrpDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final QrtzPausedTriggerGrpRowMapper rowMapper;

    public JdbcQrtzPausedTriggerGrpDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new QrtzPausedTriggerGrpRowMapper();
    }

    @Override
    public Optional<QrtzPausedTriggerGrp> findBySchedNameAndTriggerGroup(String schedName, String triggerGroup) {
        try {
            QrtzPausedTriggerGrp result = jdbcTemplate.queryForObject(
                QrtzPausedTriggerGrpSql.FIND_BY_SCHED_NAME_AND_TRIGGER_GROUP,
                Map.of("schedName", schedName, "triggerGroup", triggerGroup),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteBySchedNameAndTriggerGroup(String schedName, String triggerGroup) {
        jdbcTemplate.update(QrtzPausedTriggerGrpSql.DELETE_BY_SCHED_NAME_AND_TRIGGER_GROUP, Map.of("schedName", schedName, "triggerGroup", triggerGroup));
    }

    @Override
    public boolean existsBySchedNameAndTriggerGroup(String schedName, String triggerGroup) {
        Integer count = jdbcTemplate.queryForObject(
            QrtzPausedTriggerGrpSql.EXISTS_BY_SCHED_NAME_AND_TRIGGER_GROUP,
            Map.of("schedName", schedName, "triggerGroup", triggerGroup),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<QrtzPausedTriggerGrp> findAll() {
        return jdbcTemplate.query(QrtzPausedTriggerGrpSql.FIND_ALL, rowMapper);
    }

    @Override
    public QrtzPausedTriggerGrp save(QrtzPausedTriggerGrp entity) {
        if (existsBySchedNameAndTriggerGroup(entity.getSchedName(), entity.getTriggerGroup())) {
            return update(entity);
        } else {
            return insert(entity);
        }
    }

    private QrtzPausedTriggerGrp insert(QrtzPausedTriggerGrp entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();

        MapSqlParameterSource params = createParameterMap(entity);
        jdbcTemplate.update(QrtzPausedTriggerGrpSql.INSERT, params);
        return entity;
    }

    private QrtzPausedTriggerGrp update(QrtzPausedTriggerGrp entity) {
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(QrtzPausedTriggerGrpSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(QrtzPausedTriggerGrpSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    private MapSqlParameterSource createParameterMap(QrtzPausedTriggerGrp entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("triggerGroup", entity.getTriggerGroup());
        params.addValue("schedName", entity.getSchedName());

        return params;
    }

}
