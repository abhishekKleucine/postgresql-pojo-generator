package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.QrtzSimpleTriggersDao;
import com.example.pojogenerator.pojos.QrtzSimpleTriggers;
import com.example.daoGenerator.dao.mapper.QrtzSimpleTriggersRowMapper;
import com.example.daoGenerator.dao.sql.QrtzSimpleTriggersSql;

/**
 * JDBC implementation of QrtzSimpleTriggersDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcQrtzSimpleTriggersDao implements QrtzSimpleTriggersDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final QrtzSimpleTriggersRowMapper rowMapper;

    public JdbcQrtzSimpleTriggersDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new QrtzSimpleTriggersRowMapper();
    }

    @Override
    public Optional<QrtzSimpleTriggers> findById(Long id) {
        try {
            QrtzSimpleTriggers result = jdbcTemplate.queryForObject(
                QrtzSimpleTriggersSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<QrtzSimpleTriggers> findAll() {
        return jdbcTemplate.query(QrtzSimpleTriggersSql.FIND_ALL, rowMapper);
    }

    @Override
    public QrtzSimpleTriggers save(QrtzSimpleTriggers entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private QrtzSimpleTriggers insert(QrtzSimpleTriggers entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            QrtzSimpleTriggersSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private QrtzSimpleTriggers update(QrtzSimpleTriggers entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            QrtzSimpleTriggersSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(QrtzSimpleTriggers entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("triggerGroup", entity.getTriggerGroup());
        params.addValue("triggerName", entity.getTriggerName());
        params.addValue("repeatInterval", entity.getRepeatInterval());
        params.addValue("schedName", entity.getSchedName());
        params.addValue("timesTriggered", entity.getTimesTriggered());
        params.addValue("repeatCount", entity.getRepeatCount());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(QrtzSimpleTriggersSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            QrtzSimpleTriggersSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(QrtzSimpleTriggersSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<QrtzSimpleTriggers> findByTriggerName(Long triggerName) {
        return jdbcTemplate.query(
            QrtzSimpleTriggersSql.FIND_BY_TRIGGER_NAME,
            Map.of("triggerName", triggerName),
            rowMapper
        );
    }

    @Override
    public List<QrtzSimpleTriggers> findBySchedName(Long schedName) {
        return jdbcTemplate.query(
            QrtzSimpleTriggersSql.FIND_BY_SCHED_NAME,
            Map.of("schedName", schedName),
            rowMapper
        );
    }

    @Override
    public List<QrtzSimpleTriggers> findByTriggerGroup(Long triggerGroup) {
        return jdbcTemplate.query(
            QrtzSimpleTriggersSql.FIND_BY_TRIGGER_GROUP,
            Map.of("triggerGroup", triggerGroup),
            rowMapper
        );
    }

}
