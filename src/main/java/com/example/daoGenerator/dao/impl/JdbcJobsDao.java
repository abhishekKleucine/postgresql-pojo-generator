package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.JobsDao;
import com.example.pojogenerator.pojos.Jobs;
import com.example.daoGenerator.dao.mapper.JobsRowMapper;
import com.example.daoGenerator.dao.sql.JobsSql;

/**
 * Enhanced JDBC implementation of JobsDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcJobsDao implements JobsDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final JobsRowMapper rowMapper;

    public JdbcJobsDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new JobsRowMapper();
    }

    @Override
    public Optional<Jobs> findById(Long id) {
        try {
            Jobs result = jdbcTemplate.queryForObject(
                JobsSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(JobsSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            JobsSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<Jobs> findAll() {
        return jdbcTemplate.query(JobsSql.FIND_ALL, rowMapper);
    }

    @Override
    public Jobs save(Jobs entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private Jobs insert(Jobs entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            JobsSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private Jobs update(Jobs entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(JobsSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(JobsSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<Jobs> findByUseCasesId(Long useCasesId) {
        return jdbcTemplate.query(
            JobsSql.FIND_BY_USE_CASES_ID,
            Map.of("useCasesId", useCasesId),
            rowMapper
        );
    }

    @Override
    public List<Jobs> findByChecklistsId(Long checklistsId) {
        return jdbcTemplate.query(
            JobsSql.FIND_BY_CHECKLISTS_ID,
            Map.of("checklistsId", checklistsId),
            rowMapper
        );
    }

    @Override
    public List<Jobs> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            JobsSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<Jobs> findByOrganisationsId(Long organisationsId) {
        return jdbcTemplate.query(
            JobsSql.FIND_BY_ORGANISATIONS_ID,
            Map.of("organisationsId", organisationsId),
            rowMapper
        );
    }

    @Override
    public List<Jobs> findByFacilitiesId(Long facilitiesId) {
        return jdbcTemplate.query(
            JobsSql.FIND_BY_FACILITIES_ID,
            Map.of("facilitiesId", facilitiesId),
            rowMapper
        );
    }

    @Override
    public List<Jobs> findByEndedBy(Long endedBy) {
        return jdbcTemplate.query(
            JobsSql.FIND_BY_ENDED_BY,
            Map.of("endedBy", endedBy),
            rowMapper
        );
    }

    @Override
    public List<Jobs> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            JobsSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    @Override
    public List<Jobs> findByStartedBy(Long startedBy) {
        return jdbcTemplate.query(
            JobsSql.FIND_BY_STARTED_BY,
            Map.of("startedBy", startedBy),
            rowMapper
        );
    }

    @Override
    public List<Jobs> findBySchedulersId(Long schedulersId) {
        return jdbcTemplate.query(
            JobsSql.FIND_BY_SCHEDULERS_ID,
            Map.of("schedulersId", schedulersId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(Jobs entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("schedulersId", entity.getSchedulersId());
        params.addValue("expectedStartDate", entity.getExpectedStartDate());
        params.addValue("facilitiesId", entity.getFacilitiesId());
        params.addValue("code", entity.getCode());
        params.addValue("organisationsId", entity.getOrganisationsId());
        params.addValue("startedBy", entity.getStartedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("useCasesId", entity.getUseCasesId());
        params.addValue("checklistAncestorId", entity.getChecklistAncestorId());
        params.addValue("startedAt", entity.getStartedAt());
        params.addValue("checklistsId", entity.getChecklistsId());
        params.addValue("endedBy", entity.getEndedBy());
        params.addValue("isScheduled", entity.getIsScheduled());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("expectedEndDate", entity.getExpectedEndDate());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("endedAt", entity.getEndedAt());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("state", entity.getState());

        return params;
    }

}
