package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.PasswordPoliciesDao;
import com.example.pojogenerator.pojos.PasswordPolicies;
import com.example.daoGenerator.dao.mapper.PasswordPoliciesRowMapper;
import com.example.daoGenerator.dao.sql.PasswordPoliciesSql;

/**
 * JDBC implementation of PasswordPoliciesDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcPasswordPoliciesDao implements PasswordPoliciesDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final PasswordPoliciesRowMapper rowMapper;

    public JdbcPasswordPoliciesDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new PasswordPoliciesRowMapper();
    }

    @Override
    public Optional<PasswordPolicies> findById(Long id) {
        try {
            PasswordPolicies result = jdbcTemplate.queryForObject(
                PasswordPoliciesSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<PasswordPolicies> findAll() {
        return jdbcTemplate.query(PasswordPoliciesSql.FIND_ALL, rowMapper);
    }

    @Override
    public PasswordPolicies save(PasswordPolicies entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private PasswordPolicies insert(PasswordPolicies entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            PasswordPoliciesSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private PasswordPolicies update(PasswordPolicies entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            PasswordPoliciesSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(PasswordPolicies entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("minimumPasswordLength", entity.getMinimumPasswordLength());
        params.addValue("passwordExpiration", entity.getPasswordExpiration());
        params.addValue("organisationsId", entity.getOrganisationsId());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("maximumPasswordAge", entity.getMaximumPasswordAge());
        params.addValue("minimumLowercaseCharacters", entity.getMinimumLowercaseCharacters());
        params.addValue("minimumPasswordHistory", entity.getMinimumPasswordHistory());
        params.addValue("minimumNumericCharacters", entity.getMinimumNumericCharacters());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("allowPasswordSimilarToUsernameOrEmail", entity.getAllowPasswordSimilarToUsernameOrEmail());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("minimumUppercaseCharacters", entity.getMinimumUppercaseCharacters());
        params.addValue("id", entity.getId());
        params.addValue("minimumSpecialCharacters", entity.getMinimumSpecialCharacters());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(PasswordPoliciesSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            PasswordPoliciesSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(PasswordPoliciesSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<PasswordPolicies> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            PasswordPoliciesSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<PasswordPolicies> findByOrganisationsId(Long organisationsId) {
        return jdbcTemplate.query(
            PasswordPoliciesSql.FIND_BY_ORGANISATIONS_ID,
            Map.of("organisationsId", organisationsId),
            rowMapper
        );
    }

    @Override
    public List<PasswordPolicies> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            PasswordPoliciesSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

}
