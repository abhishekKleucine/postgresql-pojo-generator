package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ChecklistFacilityMappingDao;
import com.example.pojogenerator.pojos.ChecklistFacilityMapping;
import com.example.daoGenerator.dao.mapper.ChecklistFacilityMappingRowMapper;
import com.example.daoGenerator.dao.sql.ChecklistFacilityMappingSql;

/**
 * JDBC implementation of ChecklistFacilityMappingDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcChecklistFacilityMappingDao implements ChecklistFacilityMappingDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ChecklistFacilityMappingRowMapper rowMapper;

    public JdbcChecklistFacilityMappingDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ChecklistFacilityMappingRowMapper();
    }

    @Override
    public Optional<ChecklistFacilityMapping> findById(Long id) {
        try {
            ChecklistFacilityMapping result = jdbcTemplate.queryForObject(
                ChecklistFacilityMappingSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<ChecklistFacilityMapping> findAll() {
        return jdbcTemplate.query(ChecklistFacilityMappingSql.FIND_ALL, rowMapper);
    }

    @Override
    public ChecklistFacilityMapping save(ChecklistFacilityMapping entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ChecklistFacilityMapping insert(ChecklistFacilityMapping entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ChecklistFacilityMappingSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ChecklistFacilityMapping update(ChecklistFacilityMapping entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            ChecklistFacilityMappingSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(ChecklistFacilityMapping entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("facilitiesId", entity.getFacilitiesId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("checklistsId", entity.getChecklistsId());
        params.addValue("modifiedBy", entity.getModifiedBy());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ChecklistFacilityMappingSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ChecklistFacilityMappingSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ChecklistFacilityMappingSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<ChecklistFacilityMapping> findByChecklistsId(Long checklistsId) {
        return jdbcTemplate.query(
            ChecklistFacilityMappingSql.FIND_BY_CHECKLISTS_ID,
            Map.of("checklistsId", checklistsId),
            rowMapper
        );
    }

    @Override
    public List<ChecklistFacilityMapping> findByFacilitiesId(Long facilitiesId) {
        return jdbcTemplate.query(
            ChecklistFacilityMappingSql.FIND_BY_FACILITIES_ID,
            Map.of("facilitiesId", facilitiesId),
            rowMapper
        );
    }

}
