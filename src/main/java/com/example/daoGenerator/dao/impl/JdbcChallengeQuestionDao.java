package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ChallengeQuestionDao;
import com.example.pojogenerator.pojos.ChallengeQuestion;
import com.example.daoGenerator.dao.mapper.ChallengeQuestionRowMapper;
import com.example.daoGenerator.dao.sql.ChallengeQuestionSql;

/**
 * Enhanced JDBC implementation of ChallengeQuestionDao
 * Key Type: CUSTOM_SINGLE_KEY
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcChallengeQuestionDao implements ChallengeQuestionDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ChallengeQuestionRowMapper rowMapper;

    public JdbcChallengeQuestionDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ChallengeQuestionRowMapper();
    }

    @Override
    public Optional<ChallengeQuestion> findById(Integer id) {
        try {
            ChallengeQuestion result = jdbcTemplate.queryForObject(
                ChallengeQuestionSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Integer id) {
        jdbcTemplate.update(ChallengeQuestionSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Integer id) {
        Integer count = jdbcTemplate.queryForObject(
            ChallengeQuestionSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<ChallengeQuestion> findAll() {
        return jdbcTemplate.query(ChallengeQuestionSql.FIND_ALL, rowMapper);
    }

    @Override
    public ChallengeQuestion save(ChallengeQuestion entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ChallengeQuestion insert(ChallengeQuestion entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        jdbcTemplate.update(ChallengeQuestionSql.INSERT, params);
        return entity;
    }

    private ChallengeQuestion update(ChallengeQuestion entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(ChallengeQuestionSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ChallengeQuestionSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    private MapSqlParameterSource createParameterMap(ChallengeQuestion entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("archived", entity.getArchived());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("question", entity.getQuestion());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());

        return params;
    }

}
