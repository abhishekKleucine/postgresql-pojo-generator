package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.TasksDao;
import com.example.pojogenerator.pojos.Tasks;
import com.example.daoGenerator.dao.mapper.TasksRowMapper;
import com.example.daoGenerator.dao.sql.TasksSql;

/**
 * Enhanced JDBC implementation of TasksDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcTasksDao implements TasksDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final TasksRowMapper rowMapper;

    public JdbcTasksDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new TasksRowMapper();
    }

    @Override
    public Optional<Tasks> findById(Long id) {
        try {
            Tasks result = jdbcTemplate.queryForObject(
                TasksSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(TasksSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            TasksSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<Tasks> findAll() {
        return jdbcTemplate.query(TasksSql.FIND_ALL, rowMapper);
    }

    @Override
    public Tasks save(Tasks entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private Tasks insert(Tasks entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            TasksSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private Tasks update(Tasks entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(TasksSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(TasksSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<Tasks> findByTaskRecurrencesId(Long taskRecurrencesId) {
        return jdbcTemplate.query(
            TasksSql.FIND_BY_TASK_RECURRENCES_ID,
            Map.of("taskRecurrencesId", taskRecurrencesId),
            rowMapper
        );
    }

    @Override
    public List<Tasks> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            TasksSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<Tasks> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            TasksSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    @Override
    public List<Tasks> findByStagesId(Long stagesId) {
        return jdbcTemplate.query(
            TasksSql.FIND_BY_STAGES_ID,
            Map.of("stagesId", stagesId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(Tasks entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("orderTree", entity.getOrderTree());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("minPeriod", entity.getMinPeriod());
        params.addValue("taskRecurrencesId", entity.getTaskRecurrencesId());
        params.addValue("enableScheduling", entity.getEnableScheduling());
        params.addValue("hasExecutorLock", entity.getHasExecutorLock());
        params.addValue("stagesId", entity.getStagesId());
        params.addValue("taskSchedulesId", entity.getTaskSchedulesId());
        params.addValue("maxPeriod", entity.getMaxPeriod());
        params.addValue("hasInterlocks", entity.getHasInterlocks());
        params.addValue("isSoloTask", entity.getIsSoloTask());
        params.addValue("archived", entity.getArchived());
        params.addValue("isTimed", entity.getIsTimed());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("hasBulkVerification", entity.getHasBulkVerification());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("hasStop", entity.getHasStop());
        params.addValue("enableRecurrence", entity.getEnableRecurrence());
        params.addValue("name", entity.getName());
        params.addValue("timerOperator", entity.getTimerOperator());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("isMandatory", entity.getIsMandatory());

        return params;
    }

}
