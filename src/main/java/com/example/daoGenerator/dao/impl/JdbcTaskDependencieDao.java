package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.TaskDependencieDao;
import com.example.pojogenerator.pojos.TaskDependencie;
import com.example.daoGenerator.dao.mapper.TaskDependencieRowMapper;
import com.example.daoGenerator.dao.sql.TaskDependencieSql;

/**
 * Enhanced JDBC implementation of TaskDependencieDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcTaskDependencieDao implements TaskDependencieDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final TaskDependencieRowMapper rowMapper;

    public JdbcTaskDependencieDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new TaskDependencieRowMapper();
    }

    @Override
    public Optional<TaskDependencie> findById(Long id) {
        try {
            TaskDependencie result = jdbcTemplate.queryForObject(
                TaskDependencieSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(TaskDependencieSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            TaskDependencieSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<TaskDependencie> findAll() {
        return jdbcTemplate.query(TaskDependencieSql.FIND_ALL, rowMapper);
    }

    @Override
    public TaskDependencie save(TaskDependencie entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private TaskDependencie insert(TaskDependencie entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            TaskDependencieSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private TaskDependencie update(TaskDependencie entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(TaskDependencieSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(TaskDependencieSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<TaskDependencie> findByDependentTaskId(Long dependentTaskId) {
        return jdbcTemplate.query(
            TaskDependencieSql.FIND_BY_DEPENDENT_TASK_ID,
            Map.of("dependentTaskId", dependentTaskId),
            rowMapper
        );
    }

    @Override
    public List<TaskDependencie> findByPrerequisiteTaskId(Long prerequisiteTaskId) {
        return jdbcTemplate.query(
            TaskDependencieSql.FIND_BY_PREREQUISITE_TASK_ID,
            Map.of("prerequisiteTaskId", prerequisiteTaskId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(TaskDependencie entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("dependentTaskId", entity.getDependentTaskId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("prerequisiteTaskId", entity.getPrerequisiteTaskId());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());

        return params;
    }

}
