package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ServiceDao;
import com.example.pojogenerator.pojos.Service;
import com.example.daoGenerator.dao.mapper.ServiceRowMapper;
import com.example.daoGenerator.dao.sql.ServiceSql;

/**
 * Enhanced JDBC implementation of ServiceDao
 * Key Type: CUSTOM_SINGLE_KEY
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcServiceDao implements ServiceDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ServiceRowMapper rowMapper;

    public JdbcServiceDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ServiceRowMapper();
    }

    @Override
    public Optional<Service> findById(String id) {
        try {
            Service result = jdbcTemplate.queryForObject(
                ServiceSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(String id) {
        jdbcTemplate.update(ServiceSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(String id) {
        Integer count = jdbcTemplate.queryForObject(
            ServiceSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<Service> findAll() {
        return jdbcTemplate.query(ServiceSql.FIND_ALL, rowMapper);
    }

    @Override
    public Service save(Service entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private Service insert(Service entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        jdbcTemplate.update(ServiceSql.INSERT, params);
        return entity;
    }

    private Service update(Service entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(ServiceSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ServiceSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<Service> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            ServiceSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<Service> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            ServiceSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(Service entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("errorCodeRange", entity.getErrorCodeRange());
        params.addValue("archived", entity.getArchived());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("name", entity.getName());
        params.addValue("resetPasswordPath", entity.getResetPasswordPath());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("registrationPath", entity.getRegistrationPath());
        params.addValue("id", entity.getId());
        params.addValue("loginPath", entity.getLoginPath());

        return params;
    }

}
