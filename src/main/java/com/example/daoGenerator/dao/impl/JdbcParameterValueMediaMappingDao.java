package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ParameterValueMediaMappingDao;
import com.example.pojogenerator.pojos.ParameterValueMediaMapping;
import com.example.daoGenerator.dao.mapper.ParameterValueMediaMappingRowMapper;
import com.example.daoGenerator.dao.sql.ParameterValueMediaMappingSql;

/**
 * JDBC implementation of ParameterValueMediaMappingDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcParameterValueMediaMappingDao implements ParameterValueMediaMappingDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ParameterValueMediaMappingRowMapper rowMapper;

    public JdbcParameterValueMediaMappingDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ParameterValueMediaMappingRowMapper();
    }

    @Override
    public Optional<ParameterValueMediaMapping> findById(Long id) {
        try {
            ParameterValueMediaMapping result = jdbcTemplate.queryForObject(
                ParameterValueMediaMappingSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<ParameterValueMediaMapping> findAll() {
        return jdbcTemplate.query(ParameterValueMediaMappingSql.FIND_ALL, rowMapper);
    }

    @Override
    public ParameterValueMediaMapping save(ParameterValueMediaMapping entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ParameterValueMediaMapping insert(ParameterValueMediaMapping entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ParameterValueMediaMappingSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ParameterValueMediaMapping update(ParameterValueMediaMapping entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            ParameterValueMediaMappingSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(ParameterValueMediaMapping entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("parameterValuesId", entity.getParameterValuesId());
        params.addValue("archived", entity.getArchived());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("mediasId", entity.getMediasId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("modifiedBy", entity.getModifiedBy());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ParameterValueMediaMappingSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ParameterValueMediaMappingSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ParameterValueMediaMappingSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<ParameterValueMediaMapping> findByParameterValuesId(Long parameterValuesId) {
        return jdbcTemplate.query(
            ParameterValueMediaMappingSql.FIND_BY_PARAMETER_VALUES_ID,
            Map.of("parameterValuesId", parameterValuesId),
            rowMapper
        );
    }

    @Override
    public List<ParameterValueMediaMapping> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            ParameterValueMediaMappingSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<ParameterValueMediaMapping> findByMediasId(Long mediasId) {
        return jdbcTemplate.query(
            ParameterValueMediaMappingSql.FIND_BY_MEDIAS_ID,
            Map.of("mediasId", mediasId),
            rowMapper
        );
    }

    @Override
    public List<ParameterValueMediaMapping> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            ParameterValueMediaMappingSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

}
