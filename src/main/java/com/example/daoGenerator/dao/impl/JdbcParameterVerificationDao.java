package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ParameterVerificationDao;
import com.example.pojogenerator.pojos.ParameterVerification;
import com.example.daoGenerator.dao.mapper.ParameterVerificationRowMapper;
import com.example.daoGenerator.dao.sql.ParameterVerificationSql;

/**
 * Enhanced JDBC implementation of ParameterVerificationDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcParameterVerificationDao implements ParameterVerificationDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ParameterVerificationRowMapper rowMapper;

    public JdbcParameterVerificationDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ParameterVerificationRowMapper();
    }

    @Override
    public Optional<ParameterVerification> findById(Long id) {
        try {
            ParameterVerification result = jdbcTemplate.queryForObject(
                ParameterVerificationSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ParameterVerificationSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ParameterVerificationSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<ParameterVerification> findAll() {
        return jdbcTemplate.query(ParameterVerificationSql.FIND_ALL, rowMapper);
    }

    @Override
    public ParameterVerification save(ParameterVerification entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ParameterVerification insert(ParameterVerification entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ParameterVerificationSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ParameterVerification update(ParameterVerification entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(ParameterVerificationSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ParameterVerificationSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<ParameterVerification> findByParameterValuesId(Long parameterValuesId) {
        return jdbcTemplate.query(
            ParameterVerificationSql.FIND_BY_PARAMETER_VALUES_ID,
            Map.of("parameterValuesId", parameterValuesId),
            rowMapper
        );
    }

    @Override
    public List<ParameterVerification> findByUserGroupsId(Long userGroupsId) {
        return jdbcTemplate.query(
            ParameterVerificationSql.FIND_BY_USER_GROUPS_ID,
            Map.of("userGroupsId", userGroupsId),
            rowMapper
        );
    }

    @Override
    public List<ParameterVerification> findByJobsId(Long jobsId) {
        return jdbcTemplate.query(
            ParameterVerificationSql.FIND_BY_JOBS_ID,
            Map.of("jobsId", jobsId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(ParameterVerification entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("comments", entity.getComments());
        params.addValue("verificationStatus", entity.getVerificationStatus());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("parameterValuesId", entity.getParameterValuesId());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("userGroupsId", entity.getUserGroupsId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("usersId", entity.getUsersId());
        params.addValue("verificationType", entity.getVerificationType());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("jobsId", entity.getJobsId());
        params.addValue("isBulk", entity.getIsBulk());

        return params;
    }

}
