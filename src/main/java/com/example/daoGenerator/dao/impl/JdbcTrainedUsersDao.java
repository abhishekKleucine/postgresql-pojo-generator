package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.TrainedUsersDao;
import com.example.pojogenerator.pojos.TrainedUsers;
import com.example.daoGenerator.dao.mapper.TrainedUsersRowMapper;
import com.example.daoGenerator.dao.sql.TrainedUsersSql;

/**
 * Enhanced JDBC implementation of TrainedUsersDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcTrainedUsersDao implements TrainedUsersDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final TrainedUsersRowMapper rowMapper;

    public JdbcTrainedUsersDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new TrainedUsersRowMapper();
    }

    @Override
    public Optional<TrainedUsers> findById(Long id) {
        try {
            TrainedUsers result = jdbcTemplate.queryForObject(
                TrainedUsersSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(TrainedUsersSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            TrainedUsersSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<TrainedUsers> findAll() {
        return jdbcTemplate.query(TrainedUsersSql.FIND_ALL, rowMapper);
    }

    @Override
    public TrainedUsers save(TrainedUsers entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private TrainedUsers insert(TrainedUsers entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            TrainedUsersSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private TrainedUsers update(TrainedUsers entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(TrainedUsersSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(TrainedUsersSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<TrainedUsers> findByUserGroupsId(Long userGroupsId) {
        return jdbcTemplate.query(
            TrainedUsersSql.FIND_BY_USER_GROUPS_ID,
            Map.of("userGroupsId", userGroupsId),
            rowMapper
        );
    }

    @Override
    public List<TrainedUsers> findByChecklistsId(Long checklistsId) {
        return jdbcTemplate.query(
            TrainedUsersSql.FIND_BY_CHECKLISTS_ID,
            Map.of("checklistsId", checklistsId),
            rowMapper
        );
    }

    @Override
    public List<TrainedUsers> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            TrainedUsersSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<TrainedUsers> findByUsersId(Long usersId) {
        return jdbcTemplate.query(
            TrainedUsersSql.FIND_BY_USERS_ID,
            Map.of("usersId", usersId),
            rowMapper
        );
    }

    @Override
    public List<TrainedUsers> findByFacilitiesId(Long facilitiesId) {
        return jdbcTemplate.query(
            TrainedUsersSql.FIND_BY_FACILITIES_ID,
            Map.of("facilitiesId", facilitiesId),
            rowMapper
        );
    }

    @Override
    public List<TrainedUsers> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            TrainedUsersSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(TrainedUsers entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("facilitiesId", entity.getFacilitiesId());
        params.addValue("userGroupsId", entity.getUserGroupsId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("usersId", entity.getUsersId());
        params.addValue("checklistsId", entity.getChecklistsId());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());

        return params;
    }

}
