package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ScopeGroupDao;
import com.example.pojogenerator.pojos.ScopeGroup;
import com.example.daoGenerator.dao.mapper.ScopeGroupRowMapper;
import com.example.daoGenerator.dao.sql.ScopeGroupSql;

/**
 * Enhanced JDBC implementation of ScopeGroupDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcScopeGroupDao implements ScopeGroupDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ScopeGroupRowMapper rowMapper;

    public JdbcScopeGroupDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ScopeGroupRowMapper();
    }

    @Override
    public Optional<ScopeGroup> findById(Long id) {
        try {
            ScopeGroup result = jdbcTemplate.queryForObject(
                ScopeGroupSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ScopeGroupSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ScopeGroupSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<ScopeGroup> findAll() {
        return jdbcTemplate.query(ScopeGroupSql.FIND_ALL, rowMapper);
    }

    @Override
    public ScopeGroup save(ScopeGroup entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ScopeGroup insert(ScopeGroup entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ScopeGroupSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ScopeGroup update(ScopeGroup entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(ScopeGroupSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ScopeGroupSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<ScopeGroup> findByServicesId(Long servicesId) {
        return jdbcTemplate.query(
            ScopeGroupSql.FIND_BY_SERVICES_ID,
            Map.of("servicesId", servicesId),
            rowMapper
        );
    }

    @Override
    public List<ScopeGroup> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            ScopeGroupSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<ScopeGroup> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            ScopeGroupSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(ScopeGroup entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("archived", entity.getArchived());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("orderTree", entity.getOrderTree());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("name", entity.getName());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("servicesId", entity.getServicesId());

        return params;
    }

}
