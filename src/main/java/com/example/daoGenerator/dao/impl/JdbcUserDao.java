package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.UserDao;
import com.example.pojogenerator.pojos.User;
import com.example.daoGenerator.dao.mapper.UserRowMapper;
import com.example.daoGenerator.dao.sql.UserSql;

/**
 * Enhanced JDBC implementation of UserDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcUserDao implements UserDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final UserRowMapper rowMapper;

    public JdbcUserDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new UserRowMapper();
    }

    @Override
    public Optional<User> findById(Long id) {
        try {
            User result = jdbcTemplate.queryForObject(
                UserSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(UserSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            UserSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<User> findAll() {
        return jdbcTemplate.query(UserSql.FIND_ALL, rowMapper);
    }

    @Override
    public User save(User entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private User insert(User entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            UserSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private User update(User entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(UserSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(UserSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<User> findByChallengeQuestionsId(Long challengeQuestionsId) {
        return jdbcTemplate.query(
            UserSql.FIND_BY_CHALLENGE_QUESTIONS_ID,
            Map.of("challengeQuestionsId", challengeQuestionsId),
            rowMapper
        );
    }

    @Override
    public List<User> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            UserSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<User> findByOrganisationsId(Long organisationsId) {
        return jdbcTemplate.query(
            UserSql.FIND_BY_ORGANISATIONS_ID,
            Map.of("organisationsId", organisationsId),
            rowMapper
        );
    }

    @Override
    public List<User> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            UserSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(User entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("lastName", entity.getLastName());
        params.addValue("organisationsId", entity.getOrganisationsId());
        params.addValue("failedAdditionalVerificationAttempts", entity.getFailedAdditionalVerificationAttempts());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("failedChallengeQuestionAttempts", entity.getFailedChallengeQuestionAttempts());
        params.addValue("lockedAt", entity.getLockedAt());
        params.addValue("employeeId", entity.getEmployeeId());
        params.addValue("failedLoginAttempts", entity.getFailedLoginAttempts());
        params.addValue("type", entity.getType());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("firstName", entity.getFirstName());
        params.addValue("archived", entity.getArchived());
        params.addValue("passwordUpdatedAt", entity.getPasswordUpdatedAt());
        params.addValue("challengeQuestionsAnswer", entity.getChallengeQuestionsAnswer());
        params.addValue("password", entity.getPassword());
        params.addValue("challengeQuestionsId", entity.getChallengeQuestionsId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("isSystemUser", entity.getIsSystemUser());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("state", entity.getState());
        params.addValue("department", entity.getDepartment());
        params.addValue("email", entity.getEmail());
        params.addValue("username", entity.getUsername());

        return params;
    }

}
