package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.QrtzFiredTriggersDao;
import com.example.pojogenerator.pojos.QrtzFiredTriggers;
import com.example.daoGenerator.dao.mapper.QrtzFiredTriggersRowMapper;
import com.example.daoGenerator.dao.sql.QrtzFiredTriggersSql;

/**
 * JDBC implementation of QrtzFiredTriggersDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcQrtzFiredTriggersDao implements QrtzFiredTriggersDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final QrtzFiredTriggersRowMapper rowMapper;

    public JdbcQrtzFiredTriggersDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new QrtzFiredTriggersRowMapper();
    }

    @Override
    public Optional<QrtzFiredTriggers> findById(Long id) {
        try {
            QrtzFiredTriggers result = jdbcTemplate.queryForObject(
                QrtzFiredTriggersSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<QrtzFiredTriggers> findAll() {
        return jdbcTemplate.query(QrtzFiredTriggersSql.FIND_ALL, rowMapper);
    }

    @Override
    public QrtzFiredTriggers save(QrtzFiredTriggers entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private QrtzFiredTriggers insert(QrtzFiredTriggers entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            QrtzFiredTriggersSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private QrtzFiredTriggers update(QrtzFiredTriggers entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            QrtzFiredTriggersSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(QrtzFiredTriggers entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("jobName", entity.getJobName());
        params.addValue("triggerName", entity.getTriggerName());
        params.addValue("instanceName", entity.getInstanceName());
        params.addValue("requestsRecovery", entity.getRequestsRecovery());
        params.addValue("jobGroup", entity.getJobGroup());
        params.addValue("priority", entity.getPriority());
        params.addValue("isNonconcurrent", entity.getIsNonconcurrent());
        params.addValue("entryId", entity.getEntryId());
        params.addValue("firedTime", entity.getFiredTime());
        params.addValue("triggerGroup", entity.getTriggerGroup());
        params.addValue("schedTime", entity.getSchedTime());
        params.addValue("schedName", entity.getSchedName());
        params.addValue("state", entity.getState());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(QrtzFiredTriggersSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            QrtzFiredTriggersSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(QrtzFiredTriggersSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

}
