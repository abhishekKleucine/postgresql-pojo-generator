package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.UserRolesMappingDao;
import com.example.pojogenerator.pojos.UserRolesMapping;
import com.example.daoGenerator.dao.mapper.UserRolesMappingRowMapper;
import com.example.daoGenerator.dao.sql.UserRolesMappingSql;

/**
 * Enhanced JDBC implementation of UserRolesMappingDao
 * Key Type: COMPOSITE_KEY
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcUserRolesMappingDao implements UserRolesMappingDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final UserRolesMappingRowMapper rowMapper;

    public JdbcUserRolesMappingDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new UserRolesMappingRowMapper();
    }

    @Override
    public Optional<UserRolesMapping> findByRolesIdAndUsersId(Long rolesId, Long usersId) {
        try {
            UserRolesMapping result = jdbcTemplate.queryForObject(
                UserRolesMappingSql.FIND_BY_ROLES_ID_AND_USERS_ID,
                Map.of("rolesId", rolesId, "usersId", usersId),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteByRolesIdAndUsersId(Long rolesId, Long usersId) {
        jdbcTemplate.update(UserRolesMappingSql.DELETE_BY_ROLES_ID_AND_USERS_ID, Map.of("rolesId", rolesId, "usersId", usersId));
    }

    @Override
    public boolean existsByRolesIdAndUsersId(Long rolesId, Long usersId) {
        Integer count = jdbcTemplate.queryForObject(
            UserRolesMappingSql.EXISTS_BY_ROLES_ID_AND_USERS_ID,
            Map.of("rolesId", rolesId, "usersId", usersId),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<UserRolesMapping> findAll() {
        return jdbcTemplate.query(UserRolesMappingSql.FIND_ALL, rowMapper);
    }

    @Override
    public UserRolesMapping save(UserRolesMapping entity) {
        if (existsByRolesIdAndUsersId(entity.getRolesId(), entity.getUsersId())) {
            return update(entity);
        } else {
            return insert(entity);
        }
    }

    private UserRolesMapping insert(UserRolesMapping entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }

        MapSqlParameterSource params = createParameterMap(entity);
        jdbcTemplate.update(UserRolesMappingSql.INSERT, params);
        return entity;
    }

    private UserRolesMapping update(UserRolesMapping entity) {
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(UserRolesMappingSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(UserRolesMappingSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<UserRolesMapping> findByRolesId(Long rolesId) {
        return jdbcTemplate.query(
            UserRolesMappingSql.FIND_BY_ROLES_ID,
            Map.of("rolesId", rolesId),
            rowMapper
        );
    }

    @Override
    public List<UserRolesMapping> findByUsersId(Long usersId) {
        return jdbcTemplate.query(
            UserRolesMappingSql.FIND_BY_USERS_ID,
            Map.of("usersId", usersId),
            rowMapper
        );
    }

    @Override
    public List<UserRolesMapping> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            UserRolesMappingSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(UserRolesMapping entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("usersId", entity.getUsersId());
        params.addValue("rolesId", entity.getRolesId());

        return params;
    }

}
