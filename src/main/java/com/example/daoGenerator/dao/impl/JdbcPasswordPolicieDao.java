package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.PasswordPolicieDao;
import com.example.pojogenerator.pojos.PasswordPolicie;
import com.example.daoGenerator.dao.mapper.PasswordPolicieRowMapper;
import com.example.daoGenerator.dao.sql.PasswordPolicieSql;

/**
 * Enhanced JDBC implementation of PasswordPolicieDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcPasswordPolicieDao implements PasswordPolicieDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final PasswordPolicieRowMapper rowMapper;

    public JdbcPasswordPolicieDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new PasswordPolicieRowMapper();
    }

    @Override
    public Optional<PasswordPolicie> findById(Long id) {
        try {
            PasswordPolicie result = jdbcTemplate.queryForObject(
                PasswordPolicieSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(PasswordPolicieSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            PasswordPolicieSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<PasswordPolicie> findAll() {
        return jdbcTemplate.query(PasswordPolicieSql.FIND_ALL, rowMapper);
    }

    @Override
    public PasswordPolicie save(PasswordPolicie entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private PasswordPolicie insert(PasswordPolicie entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            PasswordPolicieSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private PasswordPolicie update(PasswordPolicie entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(PasswordPolicieSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(PasswordPolicieSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<PasswordPolicie> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            PasswordPolicieSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<PasswordPolicie> findByOrganisationsId(Long organisationsId) {
        return jdbcTemplate.query(
            PasswordPolicieSql.FIND_BY_ORGANISATIONS_ID,
            Map.of("organisationsId", organisationsId),
            rowMapper
        );
    }

    @Override
    public List<PasswordPolicie> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            PasswordPolicieSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(PasswordPolicie entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("minimumPasswordLength", entity.getMinimumPasswordLength());
        params.addValue("passwordExpiration", entity.getPasswordExpiration());
        params.addValue("organisationsId", entity.getOrganisationsId());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("maximumPasswordAge", entity.getMaximumPasswordAge());
        params.addValue("minimumLowercaseCharacters", entity.getMinimumLowercaseCharacters());
        params.addValue("minimumPasswordHistory", entity.getMinimumPasswordHistory());
        params.addValue("minimumNumericCharacters", entity.getMinimumNumericCharacters());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("allowPasswordSimilarToUsernameOrEmail", entity.getAllowPasswordSimilarToUsernameOrEmail());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("minimumUppercaseCharacters", entity.getMinimumUppercaseCharacters());
        params.addValue("id", entity.getId());
        params.addValue("minimumSpecialCharacters", entity.getMinimumSpecialCharacters());

        return params;
    }

}
