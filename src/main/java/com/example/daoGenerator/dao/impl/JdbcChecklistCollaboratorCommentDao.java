package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ChecklistCollaboratorCommentDao;
import com.example.pojogenerator.pojos.ChecklistCollaboratorComment;
import com.example.daoGenerator.dao.mapper.ChecklistCollaboratorCommentRowMapper;
import com.example.daoGenerator.dao.sql.ChecklistCollaboratorCommentSql;

/**
 * Enhanced JDBC implementation of ChecklistCollaboratorCommentDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcChecklistCollaboratorCommentDao implements ChecklistCollaboratorCommentDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ChecklistCollaboratorCommentRowMapper rowMapper;

    public JdbcChecklistCollaboratorCommentDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ChecklistCollaboratorCommentRowMapper();
    }

    @Override
    public Optional<ChecklistCollaboratorComment> findById(Long id) {
        try {
            ChecklistCollaboratorComment result = jdbcTemplate.queryForObject(
                ChecklistCollaboratorCommentSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ChecklistCollaboratorCommentSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ChecklistCollaboratorCommentSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<ChecklistCollaboratorComment> findAll() {
        return jdbcTemplate.query(ChecklistCollaboratorCommentSql.FIND_ALL, rowMapper);
    }

    @Override
    public ChecklistCollaboratorComment save(ChecklistCollaboratorComment entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ChecklistCollaboratorComment insert(ChecklistCollaboratorComment entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ChecklistCollaboratorCommentSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ChecklistCollaboratorComment update(ChecklistCollaboratorComment entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(ChecklistCollaboratorCommentSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ChecklistCollaboratorCommentSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<ChecklistCollaboratorComment> findByChecklistCollaboratorMappingsId(Long checklistCollaboratorMappingsId) {
        return jdbcTemplate.query(
            ChecklistCollaboratorCommentSql.FIND_BY_CHECKLIST_COLLABORATOR_MAPPINGS_ID,
            Map.of("checklistCollaboratorMappingsId", checklistCollaboratorMappingsId),
            rowMapper
        );
    }

    @Override
    public List<ChecklistCollaboratorComment> findByChecklistsId(Long checklistsId) {
        return jdbcTemplate.query(
            ChecklistCollaboratorCommentSql.FIND_BY_CHECKLISTS_ID,
            Map.of("checklistsId", checklistsId),
            rowMapper
        );
    }

    @Override
    public List<ChecklistCollaboratorComment> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            ChecklistCollaboratorCommentSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<ChecklistCollaboratorComment> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            ChecklistCollaboratorCommentSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(ChecklistCollaboratorComment entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("comments", entity.getComments());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("checklistCollaboratorMappingsId", entity.getChecklistCollaboratorMappingsId());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("checklistsId", entity.getChecklistsId());
        params.addValue("id", entity.getId());
        params.addValue("reviewState", entity.getReviewState());

        return params;
    }

}
