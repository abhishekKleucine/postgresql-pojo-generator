package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.CorrectionsDao;
import com.example.pojogenerator.pojos.Corrections;
import com.example.daoGenerator.dao.mapper.CorrectionsRowMapper;
import com.example.daoGenerator.dao.sql.CorrectionsSql;

/**
 * Enhanced JDBC implementation of CorrectionsDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcCorrectionsDao implements CorrectionsDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final CorrectionsRowMapper rowMapper;

    public JdbcCorrectionsDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new CorrectionsRowMapper();
    }

    @Override
    public Optional<Corrections> findById(Long id) {
        try {
            Corrections result = jdbcTemplate.queryForObject(
                CorrectionsSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(CorrectionsSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            CorrectionsSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<Corrections> findAll() {
        return jdbcTemplate.query(CorrectionsSql.FIND_ALL, rowMapper);
    }

    @Override
    public Corrections save(Corrections entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private Corrections insert(Corrections entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            CorrectionsSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private Corrections update(Corrections entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(CorrectionsSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(CorrectionsSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<Corrections> findByTaskExecutionsId(Long taskExecutionsId) {
        return jdbcTemplate.query(
            CorrectionsSql.FIND_BY_TASK_EXECUTIONS_ID,
            Map.of("taskExecutionsId", taskExecutionsId),
            rowMapper
        );
    }

    @Override
    public List<Corrections> findByParameterValuesId(Long parameterValuesId) {
        return jdbcTemplate.query(
            CorrectionsSql.FIND_BY_PARAMETER_VALUES_ID,
            Map.of("parameterValuesId", parameterValuesId),
            rowMapper
        );
    }

    @Override
    public List<Corrections> findByFacilitiesId(Long facilitiesId) {
        return jdbcTemplate.query(
            CorrectionsSql.FIND_BY_FACILITIES_ID,
            Map.of("facilitiesId", facilitiesId),
            rowMapper
        );
    }

    @Override
    public List<Corrections> findByJobsId(Long jobsId) {
        return jdbcTemplate.query(
            CorrectionsSql.FIND_BY_JOBS_ID,
            Map.of("jobsId", jobsId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(Corrections entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("reviewersReason", entity.getReviewersReason());
        params.addValue("newValue", entity.getNewValue());
        params.addValue("facilitiesId", entity.getFacilitiesId());
        params.addValue("code", entity.getCode());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("correctorsReason", entity.getCorrectorsReason());
        params.addValue("initiatorsReason", entity.getInitiatorsReason());
        params.addValue("previousState", entity.getPreviousState());
        params.addValue("parameterValuesId", entity.getParameterValuesId());
        params.addValue("createdAt", entity.getCreatedAt());
        if (entity.getNewChoices() != null) {
            params.addValue("newChoices", entity.getNewChoices().toString());
        } else {
            params.addValue("newChoices", null);
        }
        params.addValue("createdBy", entity.getCreatedBy());
        if (entity.getOldChoices() != null) {
            params.addValue("oldChoices", entity.getOldChoices().toString());
        } else {
            params.addValue("oldChoices", null);
        }
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("oldValue", entity.getOldValue());
        params.addValue("taskExecutionsId", entity.getTaskExecutionsId());
        params.addValue("jobsId", entity.getJobsId());
        params.addValue("status", entity.getStatus());

        return params;
    }

}
