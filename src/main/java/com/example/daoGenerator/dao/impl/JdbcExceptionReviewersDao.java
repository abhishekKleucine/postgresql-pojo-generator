package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.ExceptionReviewersDao;
import com.example.pojogenerator.pojos.ExceptionReviewers;
import com.example.daoGenerator.dao.mapper.ExceptionReviewersRowMapper;
import com.example.daoGenerator.dao.sql.ExceptionReviewersSql;

/**
 * Enhanced JDBC implementation of ExceptionReviewersDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcExceptionReviewersDao implements ExceptionReviewersDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final ExceptionReviewersRowMapper rowMapper;

    public JdbcExceptionReviewersDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new ExceptionReviewersRowMapper();
    }

    @Override
    public Optional<ExceptionReviewers> findById(Long id) {
        try {
            ExceptionReviewers result = jdbcTemplate.queryForObject(
                ExceptionReviewersSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(ExceptionReviewersSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            ExceptionReviewersSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<ExceptionReviewers> findAll() {
        return jdbcTemplate.query(ExceptionReviewersSql.FIND_ALL, rowMapper);
    }

    @Override
    public ExceptionReviewers save(ExceptionReviewers entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private ExceptionReviewers insert(ExceptionReviewers entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            ExceptionReviewersSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private ExceptionReviewers update(ExceptionReviewers entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(ExceptionReviewersSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(ExceptionReviewersSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<ExceptionReviewers> findByExceptionsId(Long exceptionsId) {
        return jdbcTemplate.query(
            ExceptionReviewersSql.FIND_BY_EXCEPTIONS_ID,
            Map.of("exceptionsId", exceptionsId),
            rowMapper
        );
    }

    @Override
    public List<ExceptionReviewers> findByUserGroupsId(Long userGroupsId) {
        return jdbcTemplate.query(
            ExceptionReviewersSql.FIND_BY_USER_GROUPS_ID,
            Map.of("userGroupsId", userGroupsId),
            rowMapper
        );
    }

    @Override
    public List<ExceptionReviewers> findByUsersId(Long usersId) {
        return jdbcTemplate.query(
            ExceptionReviewersSql.FIND_BY_USERS_ID,
            Map.of("usersId", usersId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(ExceptionReviewers entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("actionPerformed", entity.getActionPerformed());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("userGroupsId", entity.getUserGroupsId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("usersId", entity.getUsersId());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());
        params.addValue("exceptionsId", entity.getExceptionsId());

        return params;
    }

}
