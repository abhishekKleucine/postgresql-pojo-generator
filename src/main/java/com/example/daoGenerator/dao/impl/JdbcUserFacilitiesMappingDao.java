package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.UserFacilitiesMappingDao;
import com.example.pojogenerator.pojos.UserFacilitiesMapping;
import com.example.daoGenerator.dao.mapper.UserFacilitiesMappingRowMapper;
import com.example.daoGenerator.dao.sql.UserFacilitiesMappingSql;

/**
 * JDBC implementation of UserFacilitiesMappingDao
 * Generated by JDBC DAO Generator
 */
@Repository
public class JdbcUserFacilitiesMappingDao implements UserFacilitiesMappingDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final UserFacilitiesMappingRowMapper rowMapper;

    public JdbcUserFacilitiesMappingDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new UserFacilitiesMappingRowMapper();
    }

    @Override
    public Optional<UserFacilitiesMapping> findById(Long id) {
        try {
            UserFacilitiesMapping result = jdbcTemplate.queryForObject(
                UserFacilitiesMappingSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public List<UserFacilitiesMapping> findAll() {
        return jdbcTemplate.query(UserFacilitiesMappingSql.FIND_ALL, rowMapper);
    }

    @Override
    public UserFacilitiesMapping save(UserFacilitiesMapping entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private UserFacilitiesMapping insert(UserFacilitiesMapping entity) {
        // Set audit fields
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        // Execute INSERT with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            UserFacilitiesMappingSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private UserFacilitiesMapping update(UserFacilitiesMapping entity) {
        // Update audit fields
        entity.setModifiedAt(System.currentTimeMillis());

        // Execute UPDATE with parameter mapping
        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(
            UserFacilitiesMappingSql.UPDATE,
            params
        );

        if (rowsAffected == 0) {
            throw new RuntimeException("Entity with id " + entity.getId() + " not found for update");
        }
        return entity;
    }

    private MapSqlParameterSource createParameterMap(UserFacilitiesMapping entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("facilitiesId", entity.getFacilitiesId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("usersId", entity.getUsersId());

        return params;
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(UserFacilitiesMappingSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            UserFacilitiesMappingSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(UserFacilitiesMappingSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<UserFacilitiesMapping> findByUsersId(Long usersId) {
        return jdbcTemplate.query(
            UserFacilitiesMappingSql.FIND_BY_USERS_ID,
            Map.of("usersId", usersId),
            rowMapper
        );
    }

    @Override
    public List<UserFacilitiesMapping> findByFacilitiesId(Long facilitiesId) {
        return jdbcTemplate.query(
            UserFacilitiesMappingSql.FIND_BY_FACILITIES_ID,
            Map.of("facilitiesId", facilitiesId),
            rowMapper
        );
    }

    @Override
    public List<UserFacilitiesMapping> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            UserFacilitiesMappingSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

}
