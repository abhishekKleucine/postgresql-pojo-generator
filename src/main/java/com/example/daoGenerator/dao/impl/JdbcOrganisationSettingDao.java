package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.OrganisationSettingDao;
import com.example.pojogenerator.pojos.OrganisationSetting;
import com.example.daoGenerator.dao.mapper.OrganisationSettingRowMapper;
import com.example.daoGenerator.dao.sql.OrganisationSettingSql;

/**
 * Enhanced JDBC implementation of OrganisationSettingDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcOrganisationSettingDao implements OrganisationSettingDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final OrganisationSettingRowMapper rowMapper;

    public JdbcOrganisationSettingDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new OrganisationSettingRowMapper();
    }

    @Override
    public Optional<OrganisationSetting> findById(Long id) {
        try {
            OrganisationSetting result = jdbcTemplate.queryForObject(
                OrganisationSettingSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(OrganisationSettingSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            OrganisationSettingSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<OrganisationSetting> findAll() {
        return jdbcTemplate.query(OrganisationSettingSql.FIND_ALL, rowMapper);
    }

    @Override
    public OrganisationSetting save(OrganisationSetting entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private OrganisationSetting insert(OrganisationSetting entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            OrganisationSettingSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private OrganisationSetting update(OrganisationSetting entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(OrganisationSettingSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(OrganisationSettingSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<OrganisationSetting> findByModifiedBy(Long modifiedBy) {
        return jdbcTemplate.query(
            OrganisationSettingSql.FIND_BY_MODIFIED_BY,
            Map.of("modifiedBy", modifiedBy),
            rowMapper
        );
    }

    @Override
    public List<OrganisationSetting> findByOrganisationsId(Long organisationsId) {
        return jdbcTemplate.query(
            OrganisationSettingSql.FIND_BY_ORGANISATIONS_ID,
            Map.of("organisationsId", organisationsId),
            rowMapper
        );
    }

    @Override
    public List<OrganisationSetting> findByCreatedBy(Long createdBy) {
        return jdbcTemplate.query(
            OrganisationSettingSql.FIND_BY_CREATED_BY,
            Map.of("createdBy", createdBy),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(OrganisationSetting entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("autoUnlockAfter", entity.getAutoUnlockAfter());
        params.addValue("organisationsId", entity.getOrganisationsId());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("registrationTokenExpiration", entity.getRegistrationTokenExpiration());
        if (entity.getExtras() != null) {
            params.addValue("extras", entity.getExtras().toString());
        } else {
            params.addValue("extras", null);
        }
        params.addValue("logoUrl", entity.getLogoUrl());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("maxFailedChallengeQuestionAttempts", entity.getMaxFailedChallengeQuestionAttempts());
        params.addValue("sessionIdleTimeout", entity.getSessionIdleTimeout());
        params.addValue("maxFailedAdditionalVerificationAttempts", entity.getMaxFailedAdditionalVerificationAttempts());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("maxFailedLoginAttempts", entity.getMaxFailedLoginAttempts());
        params.addValue("id", entity.getId());
        params.addValue("passwordResetTokenExpiration", entity.getPasswordResetTokenExpiration());
        if (entity.getFeatureFlags() != null) {
            params.addValue("featureFlags", entity.getFeatureFlags().toString());
        } else {
            params.addValue("featureFlags", null);
        }

        return params;
    }

}
