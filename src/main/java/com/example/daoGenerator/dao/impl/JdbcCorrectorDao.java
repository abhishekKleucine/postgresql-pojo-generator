package com.example.daoGenerator.dao.impl;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.springframework.dao.EmptyResultDataAccessException;
import org.springframework.jdbc.core.namedparam.MapSqlParameterSource;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;
import com.example.daoGenerator.dao.interfaces.CorrectorDao;
import com.example.pojogenerator.pojos.Corrector;
import com.example.daoGenerator.dao.mapper.CorrectorRowMapper;
import com.example.daoGenerator.dao.sql.CorrectorSql;

/**
 * Enhanced JDBC implementation of CorrectorDao
 * Key Type: SINGLE_ID
 * Generated by Enhanced JDBC DAO Generator
 */
@Repository
public class JdbcCorrectorDao implements CorrectorDao {

    private final NamedParameterJdbcTemplate jdbcTemplate;
    private final CorrectorRowMapper rowMapper;

    public JdbcCorrectorDao(NamedParameterJdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
        this.rowMapper = new CorrectorRowMapper();
    }

    @Override
    public Optional<Corrector> findById(Long id) {
        try {
            Corrector result = jdbcTemplate.queryForObject(
                CorrectorSql.FIND_BY_ID,
                Map.of("id", id),
                rowMapper
            );
            return Optional.ofNullable(result);
        } catch (EmptyResultDataAccessException e) {
            return Optional.empty();
        }
    }

    @Override
    public void deleteById(Long id) {
        jdbcTemplate.update(CorrectorSql.DELETE_BY_ID, Map.of("id", id));
    }

    @Override
    public boolean existsById(Long id) {
        Integer count = jdbcTemplate.queryForObject(
            CorrectorSql.EXISTS_BY_ID,
            Map.of("id", id),
            Integer.class
        );
        return count != null && count > 0;
    }

    @Override
    public List<Corrector> findAll() {
        return jdbcTemplate.query(CorrectorSql.FIND_ALL, rowMapper);
    }

    @Override
    public Corrector save(Corrector entity) {
        if (entity.getId() == null) {
            return insert(entity);
        } else {
            return update(entity);
        }
    }

    private Corrector insert(Corrector entity) {
        // Set audit fields if they exist
        long now = System.currentTimeMillis();
        if (entity.getCreatedAt() == null) {
            entity.setCreatedAt(now);
        }
        entity.setModifiedAt(now);

        MapSqlParameterSource params = createParameterMap(entity);
        Long generatedId = jdbcTemplate.queryForObject(
            CorrectorSql.INSERT,
            params,
            Long.class
        );
        entity.setId(generatedId);
        return entity;
    }

    private Corrector update(Corrector entity) {
        entity.setModifiedAt(System.currentTimeMillis());

        MapSqlParameterSource params = createParameterMap(entity);
        int rowsAffected = jdbcTemplate.update(CorrectorSql.UPDATE, params);
        
        if (rowsAffected == 0) {
            throw new RuntimeException("Entity not found for update");
        }
        return entity;
    }

    @Override
    public long count() {
        Long result = jdbcTemplate.queryForObject(CorrectorSql.COUNT, Map.of(), Long.class);
        return result != null ? result : 0L;
    }

    @Override
    public List<Corrector> findByUserGroupsId(Long userGroupsId) {
        return jdbcTemplate.query(
            CorrectorSql.FIND_BY_USER_GROUPS_ID,
            Map.of("userGroupsId", userGroupsId),
            rowMapper
        );
    }

    @Override
    public List<Corrector> findByCorrectionsId(Long correctionsId) {
        return jdbcTemplate.query(
            CorrectorSql.FIND_BY_CORRECTIONS_ID,
            Map.of("correctionsId", correctionsId),
            rowMapper
        );
    }

    @Override
    public List<Corrector> findByUsersId(Long usersId) {
        return jdbcTemplate.query(
            CorrectorSql.FIND_BY_USERS_ID,
            Map.of("usersId", usersId),
            rowMapper
        );
    }

    private MapSqlParameterSource createParameterMap(Corrector entity) {
        MapSqlParameterSource params = new MapSqlParameterSource();

        params.addValue("correctionsId", entity.getCorrectionsId());
        params.addValue("actionPerformed", entity.getActionPerformed());
        params.addValue("createdAt", entity.getCreatedAt());
        params.addValue("userGroupsId", entity.getUserGroupsId());
        params.addValue("createdBy", entity.getCreatedBy());
        params.addValue("modifiedAt", entity.getModifiedAt());
        params.addValue("usersId", entity.getUsersId());
        params.addValue("modifiedBy", entity.getModifiedBy());
        params.addValue("id", entity.getId());

        return params;
    }

}
