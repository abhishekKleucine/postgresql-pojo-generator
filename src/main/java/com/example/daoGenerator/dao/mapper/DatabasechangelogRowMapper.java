package com.example.daoGenerator.dao.mapper;

import java.sql.ResultSet;
import java.sql.SQLException;
import org.springframework.jdbc.core.RowMapper;
import com.example.pojogenerator.pojos.Databasechangelog;

/**
 * Row mapper for Databasechangelog entity
 * Generated by JDBC DAO Generator
 */
public class DatabasechangelogRowMapper implements RowMapper<Databasechangelog> {

    @Override
    public Databasechangelog mapRow(ResultSet rs, int rowNum) throws SQLException {
        Databasechangelog entity = new Databasechangelog();

        entity.setComments(rs.getString("comments"));
        Timestamp dateexecutedTs = rs.getTimestamp("dateexecuted");
        if (dateexecutedTs != null) {
            entity.setDateexecuted(dateexecutedTs.toLocalDateTime());
        }
        entity.setAuthor(rs.getString("author"));
        entity.setOrderexecuted(rs.getInt("orderexecuted"));
        entity.setDescription(rs.getString("description"));
        entity.setContexts(rs.getString("contexts"));
        entity.setExectype(rs.getString("exectype"));
        entity.setLiquibase(rs.getString("liquibase"));
        entity.setLabels(rs.getString("labels"));
        entity.setFilename(rs.getString("filename"));
        entity.setMd5sum(rs.getString("md5sum"));
        entity.setDeploymentId(rs.getString("deployment_id"));
        entity.setId(rs.getString("id"));
        entity.setTag(rs.getString("tag"));

        return entity;
    }
}
