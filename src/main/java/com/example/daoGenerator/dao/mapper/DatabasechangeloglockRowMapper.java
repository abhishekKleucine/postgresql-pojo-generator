package com.example.daoGenerator.dao.mapper;

import java.sql.ResultSet;
import java.sql.SQLException;
import org.springframework.jdbc.core.RowMapper;
import com.example.pojogenerator.pojos.Databasechangeloglock;

/**
 * Row mapper for Databasechangeloglock entity
 * Generated by JDBC DAO Generator
 */
public class DatabasechangeloglockRowMapper implements RowMapper<Databasechangeloglock> {

    @Override
    public Databasechangeloglock mapRow(ResultSet rs, int rowNum) throws SQLException {
        Databasechangeloglock entity = new Databasechangeloglock();

        entity.setLockedby(rs.getString("lockedby"));
        entity.setId(rs.getInt("id"));
        entity.setLocked(rs.getBoolean("locked"));
        Timestamp lockgrantedTs = rs.getTimestamp("lockgranted");
        if (lockgrantedTs != null) {
            entity.setLockgranted(lockgrantedTs.toLocalDateTime());
        }

        return entity;
    }
}
